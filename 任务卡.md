# STOA 任务卡

## 012 Phase-473 清单门禁对 HEAD 已经失效

- 状态：**待开工** ｜ 日期：2026-09-20 ｜ 来源：009 的 B-2 做不下去
- 分档：中活，但触红线（`docs/security/` 证据链）⇒ 立卡 + 独立审计

### 事实（实测）

```
$ python scripts/generate_phase473_private_store_inventory.py
phase473-private-store-inventory: unreviewed mutating source: <54 个文件>
exit=1
```

`REVIEWED_MUTATING_FILES` 按文件 SHA 钉了一份「已评审」清单，002~009 动过的 54 个文件全都漂了。
⇒ **在 HEAD 上这个生成器跑不动。**

它不是死的：字节确定性测试拿 `candidate_sha`（`b43c71b`）那棵树重生成再逐字节比对，那条路是通的。
**通的是历史那一格，HEAD 这一格是断的。**

### 由此挡住了什么（009 的实际代价）

009 想在 `account_profile` 分支的 `required_roots` 里登记 `EMAIL#/EMAIL_CLAIM`。
做得到、也跑通了，但要让字节确定性那道闸接受，必须把 `candidate_sha` 重钉到含改动的提交；
而那棵树又会被这 54 个过期 SHA 拦下。**只能回退。**

⚠️ **当时没有走的那条捷径**：刷新那 54 个 SHA 让生成器跑起来。
那等于把 002~009 里别人改的东西一并无审背书 —— 第 9 条底层判据点名的
「只堵住被发现的那个洞」。**不许用这个办法关这张卡。**

### 必须有的判据

- 生成器在 HEAD 上退 0，且 `--check` 也退 0
- 🔴 **重钉 `candidate_sha` 不等于关卡**：那 54 个文件的改动要**逐个说清为什么算已评审**，
  这一步是人判，写进审计意见
- 阴性对照：随便挑一个文件改一个字节 ⇒ 生成器必须**变红**（证明它重新在拦）
- 009 回退掉的 `EMAIL#/EMAIL_CLAIM` 登记，在本卡里补回去

### 已经有的替代闸（别重复造）

`tests/test_phase473_account_deletion_seal.py::test_封印里的分支契约与生成器的源头逐字一致`
—— 009 补的，钉住封印 JSON 与生成器 `BRANCH_CONTRACTS` 的一致性。
字节确定性那道闸钉的是候选根，**契约被单独改时它读不出来**，这条补的正是那个盲区。

---

## 011 门禁读数一直跑在错的解释器上（已修，但教训要留着）

- 状态：**已修** ｜ 日期：2026-09-20 ｜ 来源：007 独立审计的 C7

### 事实

仓里一直有 `.venv312`（**3.12.13，与 CI 的 `python-version: 3.12.13` 逐位一致**），
而 `uv run` 取的是默认 `.venv`（3.13.13）。整个 002–009 期间的后端门禁读数都跑在 3.13 上，
于是 `tests/test_lambda_dist_build.py` 的 **8 条恒红**被判定为「环境差异，与任何卡无关」。

**判定本身没错，但代价是真实的**：主会话把「8 条红是正常的」**写进了每一个 agent 的 prompt
和这份任务卡** —— 等于教所有人把一个数字当噪声。**真红了会被当环境问题放过。**

用 `.venv312` 跑同一份代码：**3405 passed / 0 failed**。

### 修法与一次退回

先试了收紧 `pyproject.toml` 的 `requires-python = ">=3.12,<3.13"` ——
`uv sync` 随即丢掉 3.13 专属的解析分支，**`uv.lock` 少了 461 行**，
触发 `tests/test_dependency_policy.py` 的依赖例外闸（它红得对）。**退回。**

最终只加一个 `.python-version`（内容 `3.12.13`），`uv.lock` 与 `pyproject.toml` 都不动。
现在裸 `uv run pytest` 就是全绿。

### 教训

**「这条红是环境问题」是一个需要被质疑的结论，不是一个可以复用的前提。**
它一旦写进 prompt 就会被下游原样继承，而继承它的人没有验证过它。

---

## 010 教师账号走的是另一条路，新规矩一条都没跟上

- 状态：**待开工** ｜ 日期：2026-09-20
- 来源：需求方提醒「一直关注家长和学生账号，别忘了教师账号」，主会话据此核查
- 分档：大活（合并两条建号路径）⇒ 红线卡

### 实测：后端有两条独立的建号路径

1. **通用**（002 的 `account_provisioning_service`）—— 四个角色都能开，邀请／直接分配两种
2. **教师申请激活**（`teacher_application_service` + `teacher_identity_provider`）——
   公开提交申请 → 审核人批准铸造单次令牌 → 凭令牌激活

002-A+E 的审计判定第 2 条**不是绕过**（要审核人铸造的令牌），这个结论成立。
**但它们现在不共享任何新规矩**：

| 规矩 | 通用建号 | 教师激活 |
| --- | --- | --- |
| 账号编号（`T26-0001`） | ✅ | 🔴 **没有** —— 它自己 `put_user`（:443 / :480），完全绕开编号体系 |
| 生日 / 未成年判定（卡 008） | ✅ | ❌ 无 |
| 首次登录强制改密（卡 006） | ✅ | ❌ 无 |
| 邀请 TTL | 整数 epoch，生效 | 🔴 **存 ISO 字符串（:207），TTL 从来没生效过** |
| 邮箱唯一性 | 有（虽然是假的，见卡 009） | 未核 |

🔴 **最重的一条**：`account_numbering_service` 里**早就给教师留了 `T` 前缀**，
而通过教师申请激活建出来的账号**根本没有编号** —— 需求方的设计里「教师有教师号」，
这条今天是空的。

### 拟定方案

把教师激活收口到通用建号路径上：令牌校验与审核语义保持不变（那是教师流程特有的、且审计认可的），
但**建号那一步改调 `account_provisioning_service`**，于是编号、生日、强制改密、唯一性一次全部跟上。

`expires_at` 的 ISO 字符串一并改成整数 epoch —— 这条债从 002 就记着没人动。

### 必须有的判据

- 教师激活建出来的账号**有 `T` 前缀的编号**
- 教师邀请行的 `expires_at` 是**整数**，且投毒（改回 ISO）会红
- 两条建号路径**共用同一个唯一性判定**（与卡 009 的复合键对齐）
- 阴性对照：审核人令牌的单次使用、审核门槛**行为不变**

### 009 带出来的两条补充（2026-09-20）

**范围比原先写的大**：`user_repo.put_user` 带 `attribute_not_exists`，是**只建不更**，
所以每一处调用都是建号点。实测有 **6 处函数**在不取占位行的情况下建号，不是审计说的 3 条路径：

```
privileged_identity_service.py   change_admin_status / _reconcile_admin / _restore_admin
public_identity_service.py       start_or_resume_public_registration / _resume_public_registration
teacher_application_service.py   _resume_activation
```

现状已被 `tests/test_account_provisioning.py::test_还有哪些路径在不取占位行的情况下建号` 钉住，
**新增第七处即红**。本卡收口教师那条时，记得同步改这条测试的期望集合 —— 它会逼你改。

**另一条判据**（009 的 A-1 修法暴露的）：收口后这些路径会开始写占位行，
于是**删号必须能把地址还回去**。`account_deletion_repo._email_claim_release()` 已经做了，
但它只认 `SK == "PROFILE"` 且 `user_id` 相符的行 —— 教师账号的 profile 形状若不同，这条会静默失效。
⇒ 判据加一条：**教师账号删号后 `get_claim(email, role) is None`，且同对可重开**。

### 待并入本卡的 C-4（009 第二轮审计发现，2026-09-20）

`account_provisioning_service._release_failed_account` 与销户是**同一个形状**，但没做同样的改：
占位行若已易主，整笔补偿会被条件拒 → 外层 `except Exception: return` 吞掉 →
profile 仍是 `account_status=provisioning`、仍挂着原地址，**失败的开号从未被停放**。

可达性很窄（要求占位行在原子建号之后、失败之前易主），所以评的是 C 不是 B。
但 009 给销户写的那套道理在这里逐字成立：**先读一次再决定挂不挂 Delete**，
让「不是我的」表现为跳过而不是整笔被拒。

⇒ **本卡收口教师建号时一并处理**（同一个文件，分两次改会互相冲突）。
由主会话在合卡 010 时做，agent 不必管这条。

---

## 009 唯一键改成 (邮箱, 角色)，并做成真的条件写 ✅（审计有条件放行 → A-1 已修 → 放行）

- 状态：**已合并**｜B-2 因既有故障做不成，已另开条目；B-1 钉住现状，正题在卡 010
- 日期：2026-09-20 ｜ 分档：中活，但**地基级**

### 为什么这条的优先级变了

需求方 2026-09-20 先确认「邮箱是唯一键」，随后追问「希望至少两个字段确定唯一性」。

⚠️ **主会话答复时纠正了一个反直觉的点**：**复合唯一键 `(邮箱, X)` 会让唯一性变弱不变强** ——
它的含义是「同一邮箱可以出现多次，只要 X 不同」。所以「要两个字段」在技术上等价于问
**「同一个人能不能持有多个账号」**。

而这个问题的答案由需求方自己给出了 —— **教师**。学校里一位老师同时是本校学生的家长很常见，
邮箱单键会让他**要么当老师、要么当家长，不能兼有**。

⇒ **唯一键定为 `(邮箱, 角色)`**：

| 组合 | 判定 |
| --- | --- |
| 同一邮箱 + 不同角色 | ✅ 允许（老师兼家长） |
| 同一邮箱 + 同一角色 | ❌ 拒绝（这才是真正的重复账号） |
| 编号 | 仍全局唯一，但是**产物**不是输入 |

**手机号不做唯一键**（一家共用一个号太常见，会把正常家庭挡在门外）。
它可以做**认领时的第二验证因子** —— 那是**验证身份**，不是**保证唯一**，两件事。

学生现阶段用自己的真邮箱，将来可能换成统一域名 —— 换了这套机制也不用改，只是地址由我们生成。

⚠️ **主会话在答复时纠正了需求方的一个前提**：
「编号 + 邮箱」这个组合**技术上不成立** —— 编号是账号建成**之后**由条件写分配的**产物**，
建号那一刻手上还没有编号，没法拿它查重。**编号只能是结果，不能是输入。**

⇒ 邮箱成了**唯一的**唯一键。而它现在是假的。

### 问题（两份独立审计各自发现，002-C 的 C-4 与 002-D 的 B6）

```
account_provisioning_service.py:474    if user_repo.get_user_by_email(email): raise 409
user_repo.py:210-219                   IndexName="GSI-Email", Limit=1     ← 预读，不是条件写
```

**GSI 是最终一致的**，预读不是条件写。002-C 的审计实测并发：

```
PROBE_C accepted invites: 2 ['S26-0001', 'S26-0002']
PROBE_C profiles with that email: 2
```

两个管理员同秒邀请同一邮箱 ⇒ **建出 2 个账号、烧掉 2 个编号**（编号永不回收）。

**还有一条相关的**（002-D 的顺手发现，主会话核实属实）：
`get_user_by_email` 查 GSI-Email 时 `Limit=1` 且**不带 FilterExpression**，
而邀请行也写 `email`（`account_provisioning_service.py:387`）⇒
**同一邮箱下同时有 PROFILE 行和邀请行，返回哪条不由调用方决定**。
🔴 **而夹具正好把它遮住了**：`tests/test_account_provisioning.py:263` 的假表硬写了
`item.get("SK") == "PROFILE"` 过滤，**真索引没有这个过滤**。

**第三条**（主会话追查出来的相互作用）：002-C 的 A-1 补偿设计是「把地址还回去」
（把 PROFILE 的 email 改写成不含 `@` 的值），**但邀请行仍带着原邮箱留在 GSI-Email 里** ——
若失败发生在 `create_invitation` 之后，重试查重会命中那条邀请行，**补偿被抵消**。
002-C 收尾的三条补偿测试**失败点全在 `create_invitation` 之前**，这一格没有覆盖。

### 拟定方案

加 `EMAIL#<规范化地址>#<角色>` 占位行做**条件写**，与编号分配同一套机制
（`ACCOUNT_NUMBER#` 那套已被独立审计投毒验证过在承力，照抄它的形状）。
占位行与 profile 行在**同一个 `TransactWriteItems`** 里写，要么都成要么都不成。

`get_user_by_email` 加 `entity_type` 过滤，并把假表改成如实建模索引内容
（**不再硬写 `SK == "PROFILE"`**）。

补偿路径要一并释放占位行与邀请行上的地址。

### 必须有的判据

- 并发两次用**同一邮箱 + 同一角色**建号 ⇒ **只有一个成功**（且是存储层拒的，不是预读拒的）
- 🔴 **阴性对照**：同一邮箱 + **不同角色** ⇒ **两个都成功**（老师兼家长的那个场景）
- 投毒：**摘掉占位行的条件写** ⇒ 上面那条必须红
- 邀请行存在时 `get_user_by_email` **不会返回它**
- 补偿之后同一邮箱可重开，**包括失败发生在 `create_invitation` 之后**的那一格
- 大小写不同视为同一账号（已有测试，别弄坏）

### 独立审计（2026-09-20）：有条件放行 → A-1 已修 → 放行

审计确认在承力的：四路竞争探针（关掉条件写 ⇒ `accepted=2, burned=2`，复现原 bug；
关掉预读 ⇒ `accepted=1`）证明**拦住的是存储层不是预读**；复合键是真复合；
`Limit` 先于 `FilterExpression` 被如实建模；16 条毒 + 1 条阴性对照，全部还原并附 SHA。

它同时纠正了主会话的一处定性（C-1）：`Limit=1` 那处改的是**少放**（地址被邀请行残留挡住），
不是多放 —— 它去翻了 `5c0ca2e`，确认 009 之前的 `get_user_by_email` 就带 `Limit=1`
且**根本没有 FilterExpression**。

#### 🔴 A-1（本卡引入的回归，已修）：删号之后占位行留下，那对地址永久卡死

审计探针读数：

```
opened      student_1998f2… number=S26-0001
claims      ['EMAIL#leaver@example.ch#student']
tombstone   has email? False   has role? False
claims left ['EMAIL#leaver@example.ch#student']
pre-read    get_user_by_email_and_role -> None      ← 管理员看到「地址空闲」
re-open     REFUSED 409 {'code': 'account_exists'}  ← 却被拒
profiles still carrying that address: 0             ← 没有任何账号持有它
```

评 A 级的四条理由都成立：009 之前墓碑不带 `email`，地址是**自由的**，是本卡把它弄没的；
入口 `DELETE /auth/me` 是自助路由且已上线；症状不可诊断（409 但查无此账号）；
且它与本仓自己写在 `account_email_claim_repo.py:15` 的前提直接矛盾 ——
**「编号永不回收，地址永远回收」**。

**为什么只能在那一刻修**：变墓碑的那笔写会抹掉 `email`，而占位行的键正是从 email 派生的。
墓碑一落，全表再没有任何一行认得那个地址 —— 事后补救这条路根本不存在。

**否掉的另一条路（有读数）**：让清扫按 `account_id` 认领占位行。
扫了一遍生产，带 `account_id` 的行有两类：`EMAIL_CLAIM` 与 **`ACCOUNT_NUMBER`**。
把 `account_id` 加进 `_targets_user`，清扫会连账号编号一起删掉 ——
正好砸掉「编号永不回收」那个更重的不变量。**这条路不能走。**

修法：`account_deletion_repo._email_claim_release()`，把占位行的 `Delete` 挂进
profile 变墓碑的**同一笔事务**，条件复用仓储已有的 `RELEASE_CONDITION`
（`attribute_not_exists(PK) OR account_id = :account_id`，幂等且删不到别人的）。

#### ⚠️ 修法本身投毒时发现的第二个坑（第 9 条底层判据：对修法再投一次毒）

只把 `RELEASE_CONDITION` 当事务条件挂上去是**不够的**：它唯一会触发的失败是
「占位行记在别人名下」，而在事务里条件被拒**会连墓碑一起取消**，分支拿到冲突后重试
—— 重试再做同样一笔，**删号永久卡死，症状是无限 retryable**。

而这个状态今天就可达：**B-1 未修**，`put_user` 是 `attribute_not_exists` 的只建不更，
另有 6 处函数用它建号且不写占位行，完全可能出现「profile 持有该对、占位行却属于另一账号」。

⇒ 改为先强一致读一次，只在确属自己时才挂 `Delete`；条件保留作为**读与提交之间**的竞争裁判。
重试因此能收敛。

#### 判据与投毒读数

| 毒 | 改了什么 | 读数 |
| --- | --- | --- |
| p1 | 摘掉事务里的释放操作 | 红 1 |
| p2 | 去掉归属判断，只看占位行存不存在 | 红 1 |
| p3 | `RELEASE_CONDITION` 去掉归属子句 | 红 1 |
| p8 | 反向检查恒返回空 | 红 2 |
| p9 | 不扫占位行 | 红 2 |
| p11 | 新增一处未登记的建号点 | 红 1 |
| p12 | 把取占位行那条降级成裸 `put_user` | 红 1 |
| 阴性 ×3 | 只改注释 | **全绿** |

全部还原并核过 SHA。后端全量：**3414 passed / 0 failed**（基线 3407 + 新增 7 条）。

#### B 级处置

| 项 | 处置 |
| --- | --- |
| B-1 三条路径裸 `put_user` | **实测是 6 处函数不是 3 条**。教师那条是卡 010 的正题，拆到两张卡会导致两边都说不清规则 ⇒ 按审计给的另一选项**钉住现状**：`test_还有哪些路径在不取占位行的情况下建号`，新增第七处即红（p11/p12 验过） |
| B-2 在封印里登记 `EMAIL#` | **做不成，非不为**。见下 |
| B-3 没有测试覆盖归属子句 | 已补 `test_释放占位行的条件不接受别人的账号`（p3 验过） |
| B-4 `--verify` 只有单向 | 已补反向扫描 `orphan_claims()`，`--verify` 现在双向（p8/p9 验过） |
| B-5 把生产 `--apply` 读数贴进卡 | 见下「生产读数」 |

#### 🔴 B-2 为什么做不成：它被一个既有故障堵死了

`required_roots` 里加 `EMAIL#/EMAIL_CLAIM` 我做了，也跑通了，**然后回退了**。原因：

1. 封印 JSON 是 `generate_phase473_private_store_inventory.py` 生成的，不能手改 ——
   手改只动 registry 一处，生成器还会把 roots 铺进 19 条 row 记录。
2. 字节确定性测试是拿**候选根 `candidate_sha` 那棵源码树 + 那份旧脚本**重生成再逐字节比对。
   改了生成器，就必须把 `candidate_sha` 重钉到含改动的提交。
3. 而在 HEAD 上跑生成器会被 `REVIEWED_MUTATING_FILES` 拦下 —— **54 个文件的 SHA 已过期**
   （002~009 动过的）。要重钉就得刷新那 54 个 SHA，**那等于把别人的改动一并无审背书**。

⇒ 这是「只堵被发现的那个洞」的典型诱惑，**不做**。

**性质说清楚：这是文档缺口不是行为缺口。** `required_roots` 只参与漂移比对、
不驱动清扫；分支确实会释放占位行，有 p1/p2/p3 为证。

作为替代，补了一条**眼下唯一能发现漂移的闸**：
`test_封印里的分支契约与生成器的源头逐字一致`（p5/p6 各验过）——
字节确定性那道闸钉的是候选根，契约单独被改时它读不出来。

**⇒ 「修复 Phase-473 清单门禁（54 个过期 SHA）」应当另起一卡。**

#### 生产读数（B-5）

`--apply` 那一轮（2026-09-20，账号 562923011260，`arn:…user/Wu_Xiaohui`）：

```
5 profiles, 5 without a claim row → wrote 5 claim rows
```

本次 A-1 修完后的双向核对，两条独立路径：

```
# 路径一：直接扫表
5 条占位行，对应 5 个 profile，account_status 全 active，0 孤儿

# 路径二：脚本
$ python scripts/backfill_account_email_claims.py --verify
5 profiles, 0 without a claim row
verify exit=0
```

⇒ 我跑的那轮 `--apply` **没有留下孤儿占位行**，期间也没有账号被删。
审计点名「无法验证的生产风险」已排除。

### 第二轮独立审计（针对 A-1 的修法本身，2026-09-20）：有条件放行 → 已处置

审计用 `moto` 的内存 DynamoDB 自造探针（真条件写、真事务取消语义），不依赖我写的任何测试或假表。
它量到的两条关键读数：修法在 `e934ed0` 上让「销户 → 占位行消失 → 同对可重开」成立，
而**同一探针跑在父提交 `d11408c` 上占位行留下、重开被拒** —— A-1 与它的闭合都是量出来的。
它还替我核实了两处否决理由成立（`required_roots` 只做形状校验；`ACCOUNT_NUMBER` 行确实带 `account_id`）。

#### 🔴 A-1′（已修）：修复的承力链路，全套测试一条都没执行过

**判据**：在 `account_deletion_service.py` 的 profile 臂第一行插 `raise SystemExit`，跑全量。

**读数**：`3414 passed / 0 failed` —— **一条都没红。**
佐证：把那一臂整个换成 `delete_owned_row`（占位行不释放、墓碑根本不写），全量同样全绿。

**成因**：我新加的 4 条测试全部**直接调仓储函数**，绕开了 `_account_profile_branch`。
而生产调的是分支。**A-1 当初就是这么溜过去的** —— 009 加了占位行，销户侧没人问过它。
这是「闸的缺口」不是行为缺陷（修法本身经得起 11 条投毒），但本项目自己的规矩是
「没红过的闸不算闸」，**这里连闸都没有**，而销户 + 鉴权是红线模块。

**修法**：`FakeAccountTable` 补 `scan`（原先没有，`scan_owned_private_rows` 走不通），
新增 `test_销户分支自己跑到底才算把地址还回去` —— 真跑分支直到 `complete`，
断言占位行消失**且分支确实收敛**（不是永远 retryable）。

投毒：p16 插 `raise` ⇒ 红；p17 换 `delete_owned_row` ⇒ 红；
p18 跨模块摘掉仓储层的释放 ⇒ 红（证明这条测试真穿到了底）；阴性只改注释 ⇒ 绿。

#### B 级处置

| 项 | 处置 |
| --- | --- |
| B-1 两个 `scan` 是最终一致读，`--verify` 可能假红 | **已修**：两个方向都加 `ConsistentRead=True`（基表 Scan 支持） |
| B-2 孤儿检出来了但没有清除路径 | **按审计倾向取 (b)：只报不治**。见下 |

**B-2 的处置口径（写死在这里）**：`orphan_claims()` 只报告、永不删除。
出现孤儿时的处置是**人工核对后手删该行**，因为「删哪一行」是关于真人的判断，不是脚本能答的。
报告会把 `PK`（含地址）与 `account_id` 打出来 —— A-1 原本「409 但查无此账号、不可诊断」
的性质因此已经解除，这是这次修复真正的兜底。手删命令：

```bash
aws dynamodb delete-item --table-name stoa-main --region eu-central-2 \
  --key '{"PK":{"S":"EMAIL#<地址>#<角色>"},"SK":{"S":"EMAIL_CLAIM"}}' \
  --condition-expression 'account_id = :a' \
  --expression-attribute-values '{":a":{"S":"<报告里的 account_id>"}}'
```

⚠️ **条件写不可省** —— 判断错了要表现为事务被拒，不是删错东西。

#### C 级处置

| # | 处置 |
| --- | --- |
| C-2 `except ValueError` 吞得太宽 | **已修**。原先「占位行仓储不可用」会被当成「没有占位行可还」，而那恰好导致地址永久卡死。改成只吞「键拼不出来」，依赖错误照常上抛（分支会兜成 retryable 重试）。补测试 `test_占位行读不出来时销户必须失败而不是当成没有`，p20 验过会红 |
| C-6 卡上计数写错 | **已修**：实际新增 7 条、全量 3414，卡上原写 6 条 / 3413 |
| C-7 docstring 写 “Three creation paths” | **已修**：改成实测的 6 处 |
| C-9 `hook` 旁路静默跳过释放 | **加注释说明**。生产走不到（boto3 `Table` 没有该属性），但要让后来加这个属性的假表知道自己接管了什么 |
| C-1 两个前置判断没测试承力 | **接受**。墓碑 Put 自己的条件（`owner_id/student_id/user_id` 三选一）在存储层兜底，摘掉前置判断也删不到别人的行 |
| C-3 异常契约变宽 | **接受**。追过调用方，`account_deletion_service` 的 `except Exception` 兜成 retryable，无新逃逸路径 |
| C-4 `_release_failed_account` 是同一个形状但没同步改 | **接受但记账**。可达性很窄（要求占位行在原子建号之后、失败之前易主）。⚠️ 该文件卡 010 正在改，**收口教师建号时一并处理**，已写进卡 010 |
| C-5 `--verify` 把三件事并成一个退出码 | **接受**。stdout 仍分三段列出；只看退出码会丢区分，但三者都意味着「要人看」 |

审计同时纠了我一处事实错误：它说 `FakeAccountTable` 已实现 `scan` —— **没有**，我补的。
不影响它的结论。

---

## 008 让系统知道谁是未成年 ✅（审计有条件放行 → 三条 B 级已修）

- 状态：**待开工** ｜ 日期：2026-09-20 ｜ 需求方已拍板
- 分档：大活（动账号模型 + 关联语义）⇒ 红线卡，需独立审计

### 背景

002 当时的决定是「未成年处理不要了暂时」，于是 `age` 只剩在旧的公开注册模型里，
而注册已经 410。**现在系统根本不知道谁是未成年**：没有年龄、没有生日、没有 `is_minor`。

需求方 2026-09-20 改了这条：**未成年要系统知道。**

### 为什么这件事必须先于「亲子关联的同意语义」

亲子关联现在是**双向可发起 + 需对方确认**。这条自助流程的正当性建立在
「双方都有能力同意」之上 —— 而让一个未成年学生点「确认」来建立一段对他有约束力的监护关系，
**这个同意站不住**。

管理员的 `assign_link` 不需要确认、直接 active，正好适合未成年。
但要区别对待，前提是系统知道谁是未成年 —— 所以这张卡是那件事的前置。

### 拟定方案（待细化）

1. 账号上加一个能判定未成年的字段。**生日比年龄好**：年龄会过期，生日不会，
   而「今天是不是未成年」要能随时重算。
2. 建号/邀请时由管理员填；受邀人自助完成注册时也要填（两条建号路径都要覆盖）。
3. **未成年的亲子关联只能由管理员建**，自助 `request_link` / `confirm_link` 对未成年一侧拒绝。
4. 成年判定的阈值要可配（不同法域不同），不要硬编码 18。

### 不做清单

- **不恢复旧的公开注册里那套未成年校验**（那条路已 410）
- **不做家长同意书/签名**这类合规流程
- **不强制「必须先有家长账号」** —— 学校批量开学生号、家长号晚几周到位是常见场景

---

## 007 支付与计费全面冻结 ✅（审计不放行 → 已修 → 放行）

- 状态：**待开工** ｜ 日期：2026-09-20 ｜ 需求方已拍板
- 分档：大活（22 条后端路由 + 6 个前端页面）

### 决定

业务是分配制、暂不做 2C，**支付与页面上的收费内容全面冻结**。
**不删代码，只加注释说明冻结**；**页面上不出现任何收费相关内容**。

### ⚠️ 一件容易误判的事

runtime config 里 `features.payments: False`，看起来支付已经关了 —— **但它只是几句文案**。
查过消费方：`enablePayment` 只在 `PaymentSettingsPage` 里改 badge 和提示语，
**不拦路由、不拦页面**。那 4 个账单页今天线上可以直接打开。

### 波及面（实测）

**后端 22 条路由**：`/parents/me/subscription/*`(6)、`/admin/subscriptions/*`(9)、
`/admin/billing/*`(2)、`POST /billing/webhooks/stripe`(1)，其余散在 admin。
代码分布在 `routers/{admin,parents,billing}.py`。

**前端**：`pages/billing/` 四个页面（BillingPage / CheckoutResultPage / PaymentSettingsPage /
VirtualCheckoutPage）+ 2 个 admin 页 + `AppRouter.tsx` 的路由注册 + 导航项。

### 方案

- **照仓里既定口径**：模块级开关常量 + `_decommissioned(code)` 抛 **410**，
  与 `public_registration_closed` / `password_recovery_closed` 同款。冻结码 `billing_frozen`。
- **不删代码**，被冻的地方留注释写明为什么冻、恢复时要做什么。
- **前端移除入口与路由注册**，页面文件留着；导航里价格/账单项摘掉。
- 🔴 **`manual_override` 一并冻** —— 分配制下权益应跟着账号类型走，
  留着它就是留一条绕过所有计费判定的后门。
- Stripe webhook 一并冻（`payments: False`，Stripe 从没发过东西，见卡 004 的读数）。

### 🔴 必须有的闭合判据

**一条测试断言「凡带 billing/subscription 语义的路由，都在冻结名单里」** ——
这样以后新增一条支付路由却忘了冻结，**门禁会红**。
判据不能只是「名单里有 22 条」，那样新增一条不加进名单也不会红。

### 连带效果

003 里两条待需求方决策的问题**自动消解**（冻结后没有活路径能走到）：
多家长各自买单时只有一家额度被消费、`manual_override` 变成无限额通行证。
那两条钉住现状的测试改为钉「冻结态」。

### 补做：法律页文案（2026-09-20，审计遗留的两条前端债）

审计当时留了两条：**前端没有闭合判据**（「没有页面显示计费内容」只靠人工 grep 撑着），
以及 **`/terms` 与 `/privacy` 仍在四种语言里描述订阅、退款与结账**。两条一起补了。

**1. 条款页的 subscriptions 一节已撤下渲染**，照 `AppRouter.tsx` 的办法只注释不删：
`TermsPage.tsx` 的 `sectionKeys` 去掉 `'subscriptions'`，**文案原样留在 `legal.json` 四语里**。

**2. 另有两处是并列清单里的一个词位**，删词并修连词（机械编辑，不是替需求方拟新的法律立场；
冻结期间确实既无订阅也无支付处理方，改后更符合事实）。

🔴 **解冻时要还原的原文**（照抄回去即可）：

| 键 | 语言 | 解冻后应还原成 |
| --- | --- | --- |
| `terms.description` | de | `Die Bedingungen für die Nutzung von STOA Lernunterstützung, Abonnements und Servicezugang.` |
| | en | `The conditions for using STOA learning support, subscriptions, and service access.` |
| | fr | `Les conditions d'utilisation du soutien d'apprentissage STOA, des abonnements et de l'accès au service.` |
| | it | `Le condizioni per usare il supporto all'apprendimento, gli abbonamenti e l'accesso al servizio STOA.` |
| `privacy.sections.thirdParties.body` | de | `Hosting-, Authentifizierungs-, Analyse-, Monitoring-, Zahlungs- und Supportdienstleister können begrenzte Daten im Rahmen geeigneter Vereinbarungen verarbeiten.` |
| | en | `Hosting, authentication, analytics, monitoring, payment, and support partners may process limited data under appropriate service agreements.` |
| | fr | `Les prestataires d'hébergement, d'authentification, d'analyse, de supervision, de paiement et d'assistance peuvent traiter des données limitées dans le cadre d'accords appropriés.` |
| | it | `Fornitori di hosting, autenticazione, analisi, monitoraggio, pagamento e assistenza possono trattare dati limitati secondo accordi appropriati.` |

⚠️ **待需求方确认（不阻塞）**：上面这两处动的是**法律文案**。我按「冻结期间陈述应与事实一致」
做了一版，但法律文本的最终措辞该由你或律师定。**不答复不阻塞任何事**，解冻时照上表还原即可。

**3. 闭合判据补上了**：`tests/component/legalCopyFrozen.test.tsx` —— 真渲染两个法律页 × 四种语言，
读 DOM 文本判冻结词。既有的 `billingFrozen.test.ts` 读的是**路由注册与导航表**，
它答不了「页面可达但文案在讲订阅」这一格，而条款页当时正是这一格。

投毒读数：把 `subscriptions` 放回渲染列表 ⇒ **红 4**（四种语言的条款页）；
把法语第三方文案改回含 `paiement` 的原文 ⇒ **红 1**（只红法语隐私页，判据是精确的）；
阴性对照只改注释 ⇒ **全绿**。均已还原并核过 SHA。

前端七道门禁全绿，**154 tests**（原 145 + 新增 9）。

---

## 006 管理面的两条结构性缺口 ✅

- 状态：**已合并上线**（2026-09-19）｜ 红线卡
- 来源：002-D 独立审计的 B7 后半 + 002-D 的 A2 实现方报的结构性旁路

**第一条：管理员互相重置密码。** 口径选的是**全禁**（重置自己允许、重置非 admin 不受影响）。
理由不是「更安全所以选它」，而是危害与 status 那条不同：
**重置端点把临时密码明文回给调用方**，所以 admin→admin 重置是**当场拿到另一个特权主体的可用凭据**，
没有目标同意、没有通知；而特权能力是**按账号授予**的（`provision_privileged_admin` 就在同一个 router 里），
所以这可能是**提权不是平移**。「加重痕 + 要 reason」不改变「凭据已到手」这个既成事实 ——
何况这条端点**本来就强制 reason**。

🔴 **「所有管理员都进不来」那个死局，主会话用 AWS 实测回答了**：

```
cognito-idp:AdminSetUserPassword  on  userpool/eu-central-2_Ss93YQzjJ
  Wu_Xiaohui     allowed      （1 把 access key）
  Deng_Zhiyuan   allowed      （2 把 access key）
  Apo            implicitDeny
```

**两个人**能走 AWS 侧 break-glass，不是单点。破局手段在**另一个信任域**，
有 CloudTrail 独立审计，**而拿着被攻陷的管理员会话的人够不到它** —— 这是目的，不是副作用。

⚠️ **该路径固有的一个缺口（未解决）**：走 AWS 侧直接重置时，本地 `must_change_password` 标记
不会被抬起来，所以那个账号拿临时密码登录后**不会被强制改密**。要补得动 infra 或加运维脚本。

**第二条：`DELETE /auth/me` 绕开 Actor 解析。** 保持不拦（拦住销户反而可能困住人），
但**让这条旁路不再隐形**。判据刻意**不落在名单上**：
- 从 `app.routes` 走 FastAPI 真正执行的依赖树，算出「非 public 且依赖树里没有 `get_actor`」的集合，
  断言这些路由**必须依赖 `get_deletion_command`** —— 即「可以不走 Actor」的唯一理由是
  「这条路由就是那条销户命令本身」。**测试里没有任何名单可供追加。**
- 另一条给这个理由提供依据：`inspect.signature` 断言 `get_deletion_command` 的每个形参都是
  `Depends(...)`，依赖集合恰好是 `{get_verified_token, get_identity_repository}` ——
  **它不接收任何调用方能送进来的参数**，所以只能作用于令牌绑定的那一个账号。

🔴 **规定的那一毒验成了**：加一条绕开 `get_actor` 的新路由、**并把它加进 `UNGATED_AUTHENTICATED_ROUTES`**
⇒ 依赖图那条**仍然红**；同一次运行里另外两条被名单安抚成绿 —— **正好证明判据没落在名单上**。

**主会话补做**：`account_peer_admin_password_reset_forbidden` 等五个判据码在前端**一个文案都没有**，
管理员触发时只看到通用错误。补了五码 × 四语，五处 `onError` 收敛成一个查找函数。
投毒（退回通用兜底 / 意语换成英文原文）→ 红，阴性对照绿。

**它报的范围外发现**：
- `test_phase473_account_deletion.py` 那条旧测试的判据是**源码文本 grep**
  （`assert "get_actor" not in inspect.getsource(...)`）—— 注释里提到这个名字就会误伤，
  也拦不住 `getattr(deps, "get_act"+"or")` 的真绕过。新测试覆盖了它想表达的东西，那条建议改判据或删。
- **拒绝码没有任何闭合登记处**：新增一个拒绝码不会被任何门禁发现，前端也不会翻译。结构性缺口。

---

## 005 亲子关联的终态不可逆 ✅

- 状态：**已合并上线**（2026-09-19）｜ 来源：002-F 审计的 B4，唯一一条真实用户会踩到的

**它把问题查得比报告更重**。002-D 的审计当时**没能实测**这一条（夹具缺 `ACCOUNT_NUMBER#` 认领行，
`studentNumber` 解析不出来，第一步就 404，场景构造失败），结论只来自代码读数。
005 把夹具补齐、走完整 HTTP 路径实测：

```
REPRO 3 re-request HTTP: 500 Internal Server Error      ← 不是 409，是 500
REPRO 4 admin assign blocked by: parent link already exists
REPRO 5 same-status transition: ValueError
REPRO 6 pending blocks admin assign forever
```

**500 的成因**：`request_child_link` 只捕获 `ParentLinkError`，仓层的 `ParentLinkConflict` 从路由漏出去了。

**方案**：rejected 冷却 7 天、pending 过期 14 天、管理员可覆写（rejected 一律可、过期 pending 可、active 不可）。

🔴 **两个关键的实现决定**：
1. **键位从不腾空**，靠新增的 `reclaim_link` 显式重写两行，条件是
   `attribute_exists(PK) AND #status=:expected AND link_updated_at=:expected` ——
   `create_link` 的 `attribute_not_exists` **一个字没动**。
   有一条测试直接去翻 `table.transactions`，断言创建那两笔的条件表达式**逐字等于原文**。
   重用自己的并发仲裁靠 `link_updated_at` 栅栏，且 `reclaim_link` 里加了
   `if linked_at == expected: raise ValueError` —— **复用同一个戳就等于没有栅栏**。
2. 🔴 **刻意不给链接行加 TTL**，这是个反向决定但理由硬：TTL 删除**不是事务性的、逐行删**，
   链接行是双向两行，**删掉一侧就剩半截链接** —— `create_link` 被幸存那行挡住、
   `reclaim_link` 因「not fully stored」拒绝，**正好又变成这张卡要修的那种堵死**。
   过期改为在读写路径上按 `link_updated_at` 判定，并加 `test_链接行不带_expires_at` 钉住。
   （顺带：`teacher_application_service.py:207` 那个 ISO 字符串 TTL 的坑它没踩，因为根本没写这个属性；那条债还在。）

**它报的范围外发现**：
- **活着的 pending 仍会挡住管理员分配，最长 14 天**。限流只把它从「无限骚扰」降成「每天 5 次」，没消掉。
  要不要让管理员也能覆写活着的 pending（反正他本来就能凭空建 active），**待决策**。
- 🔴 **`admin.py` 的 `_revoke_parent_link_side` 把 active 撤销成 `rejected`** ——
  于是「管理员撤销关联」会附带给这对组合扣上 7 天不能自助重建。
  **语义上撤销和拒绝混用了同一个状态**，值得单开一卡拆开。
- `tests/test_account_admin_endpoints.py` 里还有一条测试在真的碰 socket（异常被吞了，测试是过的）。

---

## 004 测试套件会连生产表，本地门禁读数因此不可信

- 状态：**热修已做一半，余下待办** ｜ 日期：2026-09-19
- 来源：003 实现方在干活时发现并自报，主会话核实
- 分档：中活

### 🔴 事实

`uv run pytest -q` 会真的向 `eu-central-2` 的 `stoa-main` 表发 `get_item` / `query`。
成因是 002 引入的：`security/authorization.py` 的 `facts_for` 走到新链接表时，
`parent_link_repo.get_table()` 返回**真的 boto3 Table**，而测试里没有任何人 stub 它 ——
`parent_link_repo` 用的是 `from stoa.db.dynamodb import get_table` 的**模块级绑定**，
别处打 `stoa.db.dynamodb.get_table` 或 `parents.get_table` 的桩**都够不着它**。

**003 实现方明说**：它在发现这件事之前跑过一次不带封锁的 pytest，
**确实对生产表发生了只读请求**（`get_item` / `query`，无写入）。

### 🔴 由此坐实：主会话之前的门禁读数是假的，并直接导致了一次生产事故

| 读数 | 真相 |
| --- | --- |
| 主会话报的基线 `8 failed / 3251 passed` | **靠连生产表才绿** |
| HEAD 原始代码 + `--disable-socket` | **12 failed / 3247 passed** |

多出的那 4 条（`test_parent_children` 2 条、`test_conversations` 1 条、`test_students` 1 条）
**正是 002 推上去后 CI 挂掉的那 4 条** —— CI 没有 AWS 凭据，一读就抛，
于是一次干净的拒绝变成 **503**。

⚠️ **主会话的判断失误记在这里**：本地 8 条失败被正确判定为「Python 3.13 vs 3.12 的环境差异」，
但据此推出「CI 会绿」是**推断不是读数**。CI 与本地有**两处**系统性差异，
主会话只想到了一处、而且只想到了一个方向：

1. **跨仓测试读的是对方的 main**。前端 `check:api-contract` 拉后端 main 这条已写进任务卡，
   但**后端 `test_account_access_decommission.py` 读 `stoa-infra/stacks/auth_stack.py`**
   这条同形依赖没被发现 —— 而它的方向是反的（infra 必须先推）。
   结果：002 推送时 infra 排在最后，这 3 条在 CI 上必红。
2. **CI 没有 AWS 凭据**，而本地有。上面那 4 条因此只在 CI 红。

### 已做（002 热修的一部分，已随热修上线）

两处「读不出来就 503」改成退回「没有链接」（**fail-closed**，正是第二张表存在之前的答案）：

- `security/authorization.py` 的 `_parent_link_facts` —— A3 引入，影响 conversations / students
- `routers/parents.py` 的 `_list_children_for_parent` 调 `active_children()` —— 002-F/D 引入，影响 parent_children

两处注释都写明了代价：**存储坏掉会表现成「没有链接」而不是报错**，
得靠存储自己的告警发现，不能指望这里。补了 3 条测试，投毒（去掉 try/except）会红。

### 待办（随 003 一起或单独排）

1. **`tests/conftest.py` 的 autouse fixture**（003 实现方已写好，随 003 待审）：
   默认把 `parent_link_repo.get_table` 换成空的内存表，写入直接 AssertionError。
   它同时把那 4 条 pre-existing 失败修绿。
2. 🔴 **全局封锁 socket**：`pytest-socket` **装了却没在 `pyproject.toml` 的 `addopts` 里启用**。
   加上 `addopts = ["--disable-socket", "--allow-unix-socket"]`，
   让「测试连外网」从一件没人知道的事变成一件跑不起来的事。
3. **把「后端 → infra」这条跨仓依赖写进部署次序**（已补进 002 的上线前清单）。
4. `parent_link_service.active_link()` **不接受注入的 `table`**，是这件事的根因之一。
   修它要动 002 刚审计放行的稳定件，另排。

---

## 003 亲子关联新表的下游收口：计费、额度与管理视图

- 状态：**已合并上线**（2026-09-19，经独立审计「不放行」→ 收尾 → 放行）｜ 红线卡
- 来源：002-D 的 A3 修复方（范围外发现第 1 条），主会话核实属实
- 分档：**大活**（动计费、跨 3 个服务、改条件写）⇒ 立卡 + 计划先过目 + **红线卡，需独立审计**

### ✅ 交付与审计结论

独立审计**不放行**，两条 A 级已修。**审计独立复核并认可**的部分：
旧表路径产出的 operations 与 HEAD **逐字符相同**；事务 21 条（上限 25，新/旧/混合都是 21）；
九个原子性探针全部 refused；「status=active + 身份字段 + 既有两个 fence ≈ 旧路径的 version」这个论证成立。

🔴 **审计最有价值的一条读数：投毒方向是偏的。**
实现方自报「12 次投毒全红」属实，**但那 12 个点是它自己选的**。审计另跑了 5 个**放宽方向**的变异，
全量 3274 条**读数与基线一字不差**：

| 变异 | 当时 | 收尾后 |
| --- | --- | --- |
| `M18` entitlement 的旧表优先条件放宽 | GREEN | 🔴 RED |
| `M19` 删掉 teacher_support 的候选早退 | GREEN | 🔴 RED |
| `M14` 丢掉「优先持 grant 的家长」 | GREEN | 🔴 RED |
| `M13` 非双向时软失败 | GREEN | 🔴 RED |
| `M9` 不 pop version | GREEN | 🔴 RED |

**这张卡的红线恰恰是「只许多拒不许多放」，而放宽方向一个都没被捕获。** 收尾把这五条设为硬判据。

**A1 跨家长身份泄露**（003 引入的回归）：`list_parent_child_entitlements(A)` 会返回 `parentId = B`，
而这个字段**直接出到家长自己能调的线上接口**（`subscription_service.py:244` / :1708 / :1767）。
修法是**抹掉**而不是改成请求方 —— 理由值得记：
「写成 A 的 `parentId` 等于断言 A 的账单产生了这个权益，前端拿它去关联 A 的 billing 会得出错的结论，
**把一个泄漏换成一个谎**」。抹掉后这一行与「没有登记家长」完全一致，是自洽的。
在源头 `list_parent_child_entitlements` 收口，比在三处出口各修一遍更难被绕开。

**A2 `relationship_source` 陈旧 → 教师支持永久 503**：用户看到「你是 teacher_supported、教师支持已包含」，
一点「请老师帮忙」就 503，**重试一万次也一样，钱照收**。
根因是「scope 按当前哪张表说了算解出来，条件却按 grant 当年记了哪张表下」，两者没人对账。
选了方向 1（条件跟着当前判定走）。当前判定 = BINDING 且 grant 也记 BINDING 时
**仍用 grant 里存的 version，与 HEAD 逐字符相同** —— 审计认可的「旧表路径不变」因此保住。
两张表都不认 ⇒ **`plan_denied` 终态**，不再进 4 次重试循环。

**B1 三处口径统一成一个判官**（`relationship_authority`），配 12 格真值表，
每格额外断言三方一致（`scope is not None` ⟺ `source == paid_beneficiary_grant` ⟺ `effectivePlan == teacher_supported`）。
⚠️ 收尾在两格上**与审计原判不同并说明了**：`parent_binding_status` 字段缺失统一按「不 active」（收紧）；
「profile 声称 active 但两行根本不存在」做成**终态拒绝**而非下滑到 link 表（那正是 M13 的方向）。

**B4** link 路径补上 `relationship='child'` 的 fence —— 此前 `relationship` 是**调用方自由填的字符串**，
家长可以填 `neighbour` 把学生送进付费池，而旧路径会挡掉。

### 🔴 两条待需求方决策（已钉测试，未改行为）

**B2 多家长各自买单**：A、B 两家各自为同一个孩子买了单时，**只有「持 grant 的那家」的额度被消费，
另一家白付**，且当前无任何提示。`manual_override` 不在偏好排序里。

**B3 `manual_override` 顺着 link 传递**：被 override 的家长可以通过自助 request+confirm
链上**任意数量**学生，每个都直接拿 `family` 档，**不过事务围栏、不占 1~3 人上限**。
**我方倾向**：`manual_override` 应当和 `paid_grant` 一样要求一条显式 grant，
否则 override 就是一张无限额通行证。

### 范围外发现（未改）

1. 🔴 **`routers/admin.py:3111-3141` 的提交顺序仍是错的**（A2 的可达性来源）：
   `transition_parent_student_relationship_status` **先落库**，`_revoke_parent_link_side` 失败只回 409，
   旧 binding 的撤销已经生效。A2 的后果已堵死，但「半撤销」这个中间态还在。
2. 🔴 **`account_operations_service._child_operation_row`（家长自己可调）把整个 entitlement dict
   原样塞进响应，没过 A1 的投影**。今天不漏只因为它枚举孩子不看 link 表 ——
   **一旦有人把 link 孩子并进这个列表，A1 的洞立刻重开。**
3. admin 侧 `get_admin_billing` 也被 A1 的投影抹掉了 `parentId`，客服少一条线索（可回退的取舍）。
4. `ParentLinkRequestCommand.relationship` 仍是自由文本（计费面已 fence，展示面还在）。

### 问题

002 把亲子关系改成多对多，A3 已让**鉴权层**认新表。但下游没跟上：

| 服务 | 读旧表处数 | 读新表处数 |
| --- | --- | --- |
| `entitlement_service` | 3 | **0** |
| `paid_entitlement_service` | 5 | **0** |
| `teacher_support_allowance_service` | 2 | **0** |

⇒ **只通过新表建关联的家庭，家长点得进页面，但学生不进付费额度池、不占教师支持额度。**
这是「一套新数据模型落地时只改了谁能看，没改谁该付费」的典型形态。

另有两处视图分裂：`GET /admin/parent-bindings*` 只读旧表而 `GET /admin/users` 的
`linkedAccounts` 读新表（**管理员在两个页面看到两套关系**）；
`parents.py:_list_children_for_parent` 是第二处计算「我的孩子有谁」的地方。

### 🔴 难点不在读，在条件写

`paid_entitlement_service.py:716-731` 与 `teacher_support_allowance_service.py:370-374`
都是 `TransactWriteItems` 里的 `ConditionCheck`：

```
Key: USER#{student}/PROFILE
ConditionExpression: ... AND #parent_id=:parent_id AND #parent_binding_status=:active
```

它们**与扣款写入原子地**断言「这对父子此刻仍然成立」，防的是并发改绑时扣错人的额度。

新表的关联落在**不同的 item**（`STUDENT#{student}/PARENT#{parent}`）上，
而一个事务里**所有 ConditionCheck 必须同时成立** —— **没有办法表达「旧表成立 OR 新表成立」。**
所以不能简单地把条件放宽，那样等于把这道原子闸拆掉。

### 拟定方案（待批准）

**按关系的来源选条件，而不是放宽条件。**

1. 读取阶段判定这对父子由哪张表授权（复用 A3 已建的 `_parent_link_facts` 同款判定，
   **不另写一份**）。
2. 来自旧表 ⇒ 沿用现有 `ConditionCheck`（**一个字节都不改**）。
3. 来自新表 ⇒ 换成对 `STUDENT#{student}/PARENT#{parent}` 那一行的
   `ConditionCheck`，断言 `status = active`。`TransactWriteItems` 允许对同一事务内
   **不同 item** 各下一个条件，所以原子性一点不打折。
4. `entitlement_service:116` 的 `list_parent_student_bindings` 改成与
   `active_children()` 取并集。

**代价**：事务多一个 item（上限 25，当前用量需先量）。

### 必须有的判据（合并条件）

- 只通过新表建关联 ⇒ 学生**进**付费额度池、**占**教师支持额度
- 预读之后、事务提交之前把链接改成 `rejected` ⇒ **事务必须失败**（原子闸还在）
- 旧表建的关联，行为**与改动前逐字节相同**（阴性对照）
- 投毒：把新加的 `ConditionCheck` 摘掉 ⇒ 上面第二条必须变红

### 不做清单

- **不拆旧表**。`parent_binding_status` 被 8 个模块读，退场另立卡。
- **不动 Stripe webhook 与结算逻辑本身**，只改「谁算这个家长的孩子」。
- **不合并 `_list_children_for_parent`**（视图分裂那两条另开）。

### 待确认

**问题**：一个学生同时被旧表和新表关联到**不同家长**时，额度算谁的？
**不答复会阻塞什么**：不阻塞开工，但影响第 1 步的判定顺序。
**我方倾向**：旧表优先（与 A3 的 `facts_for` 顺序一致，且旧表是付费关系的既有真源），
并在发现这种双关联时留一条审计痕，便于事后清理。

---

## 002 账户体系改造：邀请／分配制 + 管理后台 + 统一编号

- 状态：**开发中** ｜ 日期：2026-09-19 起
- 提出：「登录注册改为邀请制或管理员制，业务对内暂不对 C 端开放」
  ＋「需要账户管理后台，admin 按类型看所有账户、可直接分配账号」
- 设计方案：`stoa-docs/改造方案/002-账户体系改造.md`
- 前身：`001-认证系统迁移到-Authentik.md` **已否决**（自托管月成本 $117~280 全是 AWS
  基础设施费，而非软件授权费；Cognito 当前 $0）

**已定的决策**（2026-09-19）

| # | 决定 |
| --- | --- |
| 1 | 编号：学生 `S26-0001` / 教师 `T26-0001` / 家长 `P26-0001` / 管理员 `A26-0001` |
| 2 | 家长**独立编号**，与学生**多对多**，双向可发起关联 |
| 3 | 改密走**旧密码 + 邮箱验证码**，不引入手机号与短信 |
| 4 | 密码最小长度维持 **8**（大小写+数字已是现状） |
| 5 | 首版发邀请**只给 admin** |
| 6 | **取消**未成年／家长信息强制校验 |
| 7 | **下线**忘记密码自助找回（`account_recovery=NONE`），只能改密或找管理员重置 |

**子卡与状态**

| 子卡 | 内容 | 红线 | 状态 |
| --- | --- | --- | --- |
| 002-A | 关掉自助注册 | 是 | ✅ 完成，**审计已放行**（A1 已清，A2/A3 转 002-D 与收尾卡）|
| 002-B | 编号体系 | 否 | ✅ 已完成 |
| 002-C | 通用邀请／分配服务 | 是 | ✅ **审计放行条件已全部满足** |
| 002-D | 管理后台 **+ 全部端点接线** | 是 | ✅ **审计不放行的三条 A 级已全部闭环**，B 级除 password-reset 互保外已处置 |
| 002-E | 下线找回密码 + 邮箱验证码改密 | 是 | ✅ 完成，**审计已放行且收尾闭环** |
| 002-F | 家长-学生多对多关联 | 是 | ✅ 服务层完成，**审计已放行**；端点归 002-D |

**并行计划**（按文件集不重叠切分）

- **第一轮**：002-B（纯新增服务层） ∥ 002-A+E（都动 `auth_stack.py` 与 `routers/auth.py`，
  文件重叠**必须同一个 agent 串行做**）
- **第二轮**：002-C ∥ 002-F（都依赖 B 的编号，但彼此文件不重叠）
- **第三轮**：002-D（依赖 B 与 C）

**红线安排**：A / C / D / E / F 五张子卡完成后**各自另起独立审计 agent**，
与实现 agent 上下文不共享。成因见 001 的 G 组记录——上一轮三个要命的问题
（注册 500 加孤儿账号、BUG-06 只有 math 会炸、BUG-04 跨公式完全失效）
**全是独立审计抓的，主会话自己一个都没复核出来**。

### 🔴🔴 Cognito 用户组：infra 零声明，而它是每个请求的承力件

**这条是 002-C 审计挖出来的，但主会话核实后结论与审计相反，两边都记下来。**

**量到的**（三条 grep / 读码读数，可复现）：
1. `stoa-infra/` **全仓零个** `CfnUserPoolGroup` / `UserPoolGroup` 声明 —— 四个组
   `students` / `teachers` / `parents` / `admins` **都不在 CDK 管理之下**。
2. `account_provisioning_service.py:38` 的 `ROLE_GROUPS` 要这四个组；
   认领与分配都会调 `admin_add_user_to_group`，组不存在则 `ResourceNotFoundException`
   → 被 `except Exception` 吞成 503 → 落进审计 A-1 的第三格（Cognito 用户已建、令牌已烧、账号永久作废）。
3. 🔴 **`src/stoa/security/identity.py:148-150`**：

```python
recognized = [_GROUP_ROLES[group] for group in token.groups if group in _GROUP_ROLES]
if len(recognized) != 1:
    raise SecurityDecisionError(SecurityErrorCode.IDENTITY_CONFLICT)
```

   **每一个已认证请求**都要求 token 里恰好带一个被认识的组。
   组是**授权的承力件，不是装饰**。

**审计的推断**：「生产缺 `students`/`parents`/`admins` 三个组，002-C 对这三个角色 100% 失败」。

**主会话的反推（也是推断，不是读数）**：上面第 3 条说明，**任何一个角色的用户能正常用 API，
就证明那个角色的组已经存在**。系统现在是活的、有 5 个测试账号在用 ——
所以这些角色对应的组**很可能已经手工建好了**，审计那条推断多半不成立。

⚠️ **但危险因此换了个形态，反而更值得警惕**：

> **四个组是手工建的、不在 CDK 里，于是 `cdk deploy` 不知道它们存在。**
> 如果照「补齐 infra」的直觉往 `auth_stack.py` 里加四条 `CfnUserPoolGroup`，
> CloudFormation 会去**创建同名组**，撞上已存在的 → 报 `Group already exists`
> → **整个 AuthStack 回滚**。而删掉重建会**清空所有用户的组成员身份**，
> 按上面第 3 条，那等于**全体用户当场失去 API 访问权**。

**所以这一条主会话刻意没有动代码**，因为对的做法取决于生产实况，而那个读数只有你能取。

**需要你跑的只读命令**（配好 SSO 之后，本机任意目录）：

```bash
aws cognito-idp list-groups --user-pool-id <pool-id> --profile stoa --region eu-central-2
```

**按读数分三种情况**：

| 读数 | 该怎么办 |
| --- | --- |
| 四个组都在 | 用 `cdk import` 把它们纳入 CDK 管理，**不要**直接 `cdk deploy` 新建 |
| 只有部分在（例如只有 `teachers`） | 缺的那几个**先手工建**（与已有的同样在 CDK 之外），上线后再统一 import；或只对缺的几个走 CDK 新建 |
| 一个都没有 | 才可以直接在 `auth_stack.py` 里加四条 `CfnUserPoolGroup` 后 deploy |

**我方倾向**：最终四个组都应纳入 CDK（手工建的资源正是这个 bug 的成因），
但**纳入方式必须是 import 而不是 create**，且这一步单独排，不要和 002 的其他 infra 改动捆在一次 deploy 里。

### 📋 五张子卡的最终状态（2026-09-19 收口）

| 子卡 | 审计结论 | 处置 |
| --- | --- | --- |
| 002-A + 002-E | 有条件放行（3A/5B/7C） | A1 主会话清、A2 转 002-D、A3+B1+B2 收尾 agent、B3 主会话补断言；B4/B5 记录 |
| 002-B | 已完成 | —— |
| 002-C | 有条件放行（1A/5B/6C） | A-1 + B-1~B-5 + C-4 一半 收尾 agent；provider 删除方法主会话补 |
| 002-D | 🔴 **不放行**（3A/7B/7C） | **A1 主会话修 + 建两侧闸**、A2/A3/B2/B3/B5/B7 三个 agent；B4 已由 002-C 顺带修掉 |
| 002-F | 有条件放行（2A/3B/5C） | A1/A2/B1/B2/B3 主会话全部处置，B4 提需求方 |

**两份独立审计各自独立发现了同一条问题两次**：邀请认领时弱密码会烧掉令牌（002-C 的 B-1 / 002-D 的 B4）、
同邮箱重复开户无存储层护栏（002-C 的 C-4 / 002-D 的 B6）。**互不通气的两路审计撞出同一条，
是这条结论可信度的独立印证。**

### 🔴 已知仍敞开的口子（本轮未修，需另立卡）

1. **`POST /admin/users/{id}/password-reset` 上没有管理员互保** —— B7 只做了 status 那一半。
   任何持 `admin_identity_manager` 的管理员仍可重置另一个管理员的密码并直接拿到临时密码。
2. **计费与教师额度仍只认旧亲子表** —— `entitlement_service`(3) / `paid_entitlement_service`(5) /
   `teacher_support_allowance_service`(2) 合计 10 处读旧表、**0 处读新表**。
   只通过新表建关联的家庭，鉴权通了但**订阅权益与额度看不到这个学生**。
3. **`user_repo.get_user_by_email()` 可能读回邀请行而不是 PROFILE 行**（GSI-Email + `Limit=1` 无过滤），
   而夹具硬写了 `SK == "PROFILE"` **把这个可能性遮住了**。
   与 002-C 的 A-1 补偿有相互作用：邀请行仍带原邮箱，晚期失败后重试仍会 409。
4. **同邮箱并发开户**：GSI 最终一致 + `Limit=1` 预读，两个管理员同秒邀请同一邮箱 →
   **建出 2 个账号、烧掉 2 个编号**（编号永不回收）。需 `EMAIL#` 条件写占位行。
5. **`DELETE /auth/me` 绕开 Actor 解析**（走 `get_deletion_command`）⇒
   强制改密、以及**将来任何挂在 `get_actor` 上的闸，都对销户无效**。结构性旁路。
6. **`PROFILE_WRITER_REGISTRY` 的扫描器看不见 `Delete`** —— 那道闸声称守着「谁能写 profile」，
   实际只守得住写和改。
7. **`GET /admin/users` 读放大**：200 行 × 2 个关联 ≈ 2000+ 次 DynamoDB 往返，而 API Gateway 上限 29s。
8. **`check-untranslated.mjs` 不扫 `src/pages/{tutor,admin,parent}`** ——
   那条「基线 25」只覆盖 student 屏，**教师/管理员/家长侧的语言混杂这道闸完全看不见**。
   实例：`TutorProfilePage.tsx` 193 行全文硬编码英文。
9. **`notify_service.py` 另有 4 处 SES 发信硬编码德语**（教师开通、孩子周报等）。
10. **`/analytics/events` 公开未认证即可往 DynamoDB 写行**，`sessionId` 客户端任意指定、无频次限制。
11. **`rejected` 是终态、`pending` 永久占位**，管理员无覆写路径（见 002-F 的 B4，待需求方决策）。
12. **限流的正确性是拓扑依赖的**：它成立只因为 api 这条链路上没有反向代理，
    哪天挂上 CloudFront 就塌成全局开关，**而没有任何测试会因此变红**。

### 🔴 上线前必做清单（散落各处的约束汇总，漏一条就出事）

**部署次序不可调换**：

0. 🔴 **infra 最先推** —— 后端 `tests/test_account_access_decommission.py` 用 AST 读
   `stoa-infra/stacks/auth_stack.py`，CI 拉的是 **infra 的 main**。
   infra 后推 ⇒ 后端 CI 必红（2026-09-19 就是这么挂的，见卡 004）。
   ⚠️ 这与「infra 的 `cdk deploy` 要排在 002-D 的管理员重置端点之后」**并不矛盾**：
   前者说的是**代码推送次序**，后者说的是**部署生效次序**。infra 的 push 可以先，
   但若两者真的冲突，以不让 CI 红为先——反正 deploy job 挂了代码也已经在 main 上。
1. **backend 再推** —— 前端 CI 的 `check:api-contract` 是 `curl` 拉 backend main 的
   `route-authorization-inventory.json` 比对。前端先推必红。
2. **frontend 最后推**。
3. **infra 的 `cdk deploy` 最后，且必须在 002-D 的管理员重置密码端点上线之后** ——
   `account_recovery=NONE` 会拿掉 Cognito 的自助恢复，而产品内的替代路径就是那个端点。
   次序颠倒 = 全体用户忘密码即死路。

**推之前必须确认的事实**（都不是代码能回答的，按优先级）：

- 🔴 **Cognito 用户组** —— `aws cognito-idp list-groups --user-pool-id <pool-id> --profile stoa --region eu-central-2`
  （只读）。组是**每个已认证请求的承力件**，而 infra 零声明。读数决定 infra 怎么改，见上文专节。
- 🔴 **上线后用测试账号实打一遍管理员重置** —— 重置 → 用临时密码登录 → 确认 **200 且
  `user.mustChangePassword === true`**。整个 A2 方案依赖「`Permanent=True` 不触发 challenge」
  这条**没法本地证伪的推断**；若推断错了，`/auth/login` 会在 `auth.py:392` 抛 KeyError → **500**。
- 🔴 **`stoa-frontend/src/i18n/locales/fr/auth.json` 的 35 个新键需懂法语的人复核** ——
  约 30 条是机器译文（事故详情见 002-D 的 A2 一节）。
- 🔴 **Stripe webhook 的历史投递** —— 该端点在网关上一直被 401（本轮已修），
  订阅事件可能从来没落过库。上线后在 Stripe 后台看一眼事件投递历史，确认有没有要补发的。
- **`api.stoaedu.ch` 前面有没有手工挂 CloudFront / edge-optimized 自定义域名** ——
  CDK 里根本没有这个域名。限流的正确性依赖「这条链路上没有反向代理」，挂了就塌成全局开关。
- **`DashboardLayout` 在强制改密态下手动看一眼** —— 那个壳子可能发通知等请求，
  强制态下全部 403，没人在浏览器里跑过，不确定有没有报错 toast 或空白块。


- **线上真实账号数与状态**。用户说「目前只有 5 个账号，全是测试账号」，但**没有核过库**。
  若有人卡在注册或找回密码流程中，这次上线会直接堵死他们，且他们没有自助出路。
- **有几个人持有可用的 AWS 控制台/CLI 凭据**。这是 admin 自己忘密码时唯一的逃生通道，
  而本工作区 SSO（profile `stoa`）至今没配好。
- **SES 真发一封信** —— 至今一次真发都没做过，全是假件覆盖。
  002-E 的 B1 风险正建立在「SES 可能发不出去」上。

**门禁**（本地跑完全绿才推，四仓只有 main、push 即部署，没有 PR 缓冲）：

```bash
cd /Users/xiaohuiwu/project/stoasystem/stoa-backend && uv run ruff check . && uv run pytest -q
```

```bash
cd /Users/xiaohuiwu/project/stoasystem/stoa-frontend && npm run lint && npm run typecheck && npm run check:api-contract && npm run check:untranslated && npm test && npm run test:release && node --test tests/release/publish-web-release.test.mjs
```

⚠️ 本机 Python 是 **3.13**，`tests/test_lambda_dist_build.py` 的 8 条必然红
（它要求 3.12），CI 用 3.12 会绿。**这 8 条是环境差异，不是失败**，但别拿它当借口忽略别的红。

**记录**：

### 002-A + 002-E 认证入口 ✅

**002-A**：`auth_stack.py` `self_sign_up_enabled=False`；`POST /auth/register` 返回 **410**，
闸门常量 `PUBLIC_SELF_REGISTRATION_ENABLED=False`；删掉注册实现与它专用的亲子关系辅助函数；
前端 `/register` 改为「账号由管理员发放」说明页（无任何表单），登录页链接改「如何获取账号」；
i18n `register.closed.*` 八键四语。

**002-E**：`account_recovery=NONE`；`/auth/forgot-password`、`/auth/reset-password` 返回 410；
新增 `POST /auth/password-change/request` 与 `/confirm`；新建 `password_change_code_service.py`（299 行）；
路由清单 **208 → 210**；前端删 `ForgotPasswordPage`/`ResetPasswordPage`，新建 `ChangePasswordPage`
挂 `/settings/password`（ProtectedRoute 内）；四语 173 键完全对齐。

**选 410 而非移除路由的三条理由**：①404 二义，分不清「功能下线」和「部署坏了」，410 语义就是
「曾经有，永久没了」；②**留在路由授权清单里才有人看着它**——移除等于这三个端点从审计视野消失，
留成 410 则仍带 `explicit_route_classification` 声明，谁想改回能用必须先动清单，会被 diff 看见；
③可以断言「一次 Provider 调用都没有」（`provider.calls == []`），路由删了就没法做这个断言。

**验证码的安全设计**：一次性走条件写 `attribute_not_exists(#consumed_at)` + 入口检查双闸；
10 分钟过期不依赖 TTL 清理时延（代码里另判）；限流是**滑动窗口**而非自然小时
（自然小时下 59 分和 61 分各发 5 次 = 两分钟内 10 封），写冲突 fail closed；
**存 PBKDF2-HMAC-SHA256 十万轮**而非裸 SHA-256——6 位码只有 10⁶ 空间，库一旦泄露裸摘要瞬间被爆破；
所有拒绝理由抛同一个无参异常，路由层映射到唯一固定响应体。
**它还加了一条方案里没写的**：单码最多 5 次校验尝试，超了作废——否则攻击者可对同一个码无限猜，
10⁶ 空间在 10 分钟内猜得完。

**投毒 1 它做得比要求更细**：发现这里是**两道闸**（CDK 的 `AllowAdminCreateUserOnly` + 后端 410），
单看 curl 会被另一道掩盖。分两步读：①只翻 CDK 开关 → 门禁红 2 条
（含它自加的一致性闸：后端常量必须与 CDK 声明一致，两闸漂移本身即故障）；
②顺着漂移把后端常量也改 True → curl 变 **501 不再是 4xx**，证明那个 410 确实由这道闸产生。
投毒 2（去一次性）红 2 条、投毒 3（码错与码过期返回不同）红 2 条，还原均 diff 验证。

**它处理了主会话转告的那条回归，方向是收紧不是放松**：公开注册下线后 `auth.py` 不再写任何亲子关系，
原断言「注册必须走那一个规范入口」已无对象，改为三条**否定**断言
（`not in auth_source`），新性质比原来强——auth 路由不是关系写入者，任何形式都不是。
测试函数名保留未改，因为 `docs/security/phase-475-evidence-results.json` 记录了该 node id，
改名会让历史证据档对不上。

**路由清单测试做不到全绿且不是它造成的**：`test_route_authorization_inventory.py` 5 条失败全是
`POST /analytics/events`，来自第三方未提交的 `analytics.py`，且 `inventory_application()`
对真实 app 直接抛异常导致重新生成清单也做不到。它改用等价可判的办法——
搭一个除 analytics 外全部路由的 app 算运行时投影，与 checked JSON 逐条比对：
`runtime(everything but analytics) == checked json: True | entries: 210`。
analytics 一修好这条就会绿，本卡没引入任何漂移。

**一条设计取舍**：「旧密码错误」返回 **400 不是 401**——前端 httpClient 把任何 401 当会话过期
（`clearAuth()` + 跳 `/login`），改密页打错一次旧密码就被踢出登录。会话本身有效，
无效的是请求体里那个凭据，400 语义站得住。**主会话认可。**

**它明说没验证的**：SES 真实发信（只有假件覆盖，**上线前必须真发一次**）；
Cognito 真实行为（`AllowAdminCreateUserOnly=true` 后 `SignUp` 回什么错误码、
`AccountRecovery=NONE` 后 `forgot_password` 实际返回，都只有 synth 模板值作证）；
`cdk synth` 全栈跑不起来（`app.py` 的 Lambda 产物校验在本机 Python 3.13 直接 RuntimeError），
用 `Template.from_stack(AuthStack(...))` 单独合成读的模板；
**线上是否有真实用户卡在注册或找回密码流程中未核实**——若有，这次上线会把他们堵死。

**两条清理项**：`src/app/router/routeGroups.ts` 里还留着 `/forgot-password`（该文件全前端无人 import，
是死配置）；`httpClient.ts` 的 `PUBLIC_AUTH_PATHS` 仍列着两个已下线端点（无害，没人再调）。
新增的 `/auth/password-change/*` **不在**该公开清单里，会正常带 Authorization 头（已确认）。

### 002-C 通用邀请／分配服务 ✅（服务层，端点未加）

新增三个文件，**未改任何既有文件**：`services/account_provisioning_service.py`（679 行）、
`db/repositories/account_invitation_repo.py`（254 行）、`tests/test_account_provisioning.py`（769 行 / 18 条）。
读数：ruff 全过，**18 passed**。教师那套 `teacher_application_service.py` / `teacher_application_repo.py`
**一个字节都没动**，`test_teacher_onboarding.py` 仍全绿。

**令牌只在签发那一刻可读**：存 sha256 摘要，摘要既是 `token_digest` 也是分区键；
明文只出现在签发返回值里，不写库、不写审计、不写日志。
判据是把整张表 dump 成 JSON 断言明文不出现。

**枚举防护三层**：①不存在／已使用／已撤销走同一个 `_invitation_rejected()`，
`409 {"code":"invitation_invalid"}`，测试把 status_code 与 detail 一起序列化后逐字节比对；
②查不到时**不早返回**，而是取一条同形状占位行（`token_digest="absent:<digest>"` 必然比不中），
让两条路径落到同一句 raise；③假表记录访问序列，断言两条路径的
`(方法, PK 前缀, SK)` 列表相等。

**耗时数据它照实写了没美化**：`used=6.83µs / absent=2.96µs，ratio=2.31`。
它明说**没有断言 ratio**——差值几乎全来自「键值读命中要反序列化、未命中不用」这个
任何 KV 存储的固有性质，分支逻辑贡献约 0；断言的是**绝对差 < 500µs**（实测 3.9µs，
比一次 DynamoDB 往返的 1–5ms 抖动低三个数量级，远程不可观测）。

**「一个账号一个编号」的闸是条件写**：`attribute_not_exists(#account_number)`，
走 `account_deletion_repo.transact`（`free_trial_service.py:117` 已有同样先例）。
有一条测试专门把版本号对齐、只让 `account_number` 不满足，使版本 CAS 判不了、
**只有 `attribute_not_exists` 能判**——去掉这行该测试立刻红。

**第一轮投毒抓到假绿（与 002-B 同型）**：投毒「去掉仓储层条件写」第一次跑 **17 条全绿**——
服务层的 status 检查把仓储层的条件写完全遮住了，
**「两个并发请求同时读到 issued」这条真实竞态当时一条测试都没覆盖**。
补并发测试（照 002-B 的教训，屏障放在读完之后、返回之前）后才变红。

**它诚实处理了一条测不了的判据**：`compare_digest` vs `==` 的时间差在 Python 里测不出来
（64 字符摘要，差值淹没在解释器噪声里）。它**没有假装能测**，换成两个真能红的判据——
运行时 spy（monkeypatch `secrets.compare_digest` 断言被调用）+ AST 扫描
（源码里任何操作数含 `token`/`digest` 的 `==` 即判失败）。

七个投毒全红过，还原用 `shasum -a 256 -c` 验证。

**🔴 顺手挖出一个既有 bug（主会话已核实）**：`database_stack.py:31` 的
`time_to_live_attribute="expires_at"`，而 `teacher_application_service.py:207` 存的是
`expires_at.isoformat()` **ISO 字符串**。DynamoDB TTL **只认整数 epoch**，
所以**教师邀请行的 TTL 从来没生效过**，过期邀请一直堆在表里永不回收。
002-C 自己存整数 epoch 避开了，另存 `expires_at_iso` 供展示。**建议另开卡修教师那套。**

**需要主会话拍板、它按自己倾向做了一版的一点**：教师那套刻意**不把令牌交给评审人**
（防止评审人自己变成教师）；它这里**把令牌一次性返回给管理员**。
理由是管理员本来就有「直接分配」这条路，扣着令牌没有任何安全收益，
反而让「建号后把链接发给对方」这个正常动作做不了。**主会话认可此判断**——
管理员已能直接建号并设初始密码，返回令牌不增加其实际权限。

**取舍**：认领是**先烧令牌再建 Cognito 账号**（教师那套相反）。
好处是一次性判据无懈可击、未认证调用者拿不到无限次建号能力；
代价是 provider 故障时令牌作废需重发。没搬教师那套的 `_resume_activation` 两阶段补偿。

### 002-C 审计收尾 ✅（A-1 + B-1~B-5 + C-4 一半 + 主会话补的 provider 删除）

**A-1 选了「方案 2 补偿」，但补偿按资源类型分开实现**：

| 资源 | 补偿方式 |
| --- | --- |
| profile 行（占着邮箱） | 置终态 `provisioning_failed` **并把 email 改写成 `provisioning_failed:<account_id>`**（不含 `@`，永远匹配不上规范化邮箱） |
| 邀请令牌（claim 途中已 burn） | 新增 `account_invitation_repo.restore_invitation()` 条件写归还，**只归还本次烧掉的那一行**，不延长过期时间 |
| provider 身份（已建成的 Cognito 用户） | `_delete_provider_account()` 撤回 |
| 编号 | **不回收**（项目规则定死，认了） |

**它否掉另两条方案的理由都站得住，且是读代码读出来的**：
- 否掉方案 1（重排顺序）：对 `claim` 不成立。把 burn 推迟到 provider 之后就是放弃单次使用的并发闸 ——
  `test_并发两次认领同一令牌只有一个能建号` 会直接红。**burn 必须在最前面，所以失败后唯一出路是把票还回去。**
- 否掉「删 profile 行」这种补偿：`user_repo.PROFILE_WRITER_REGISTRY` 是闭合清单，
  而它的扫描器只认 `Put`/`Update` 字面量与 `update_item` 调用 ——
  **一个 `Delete` 操作会静默绕过这道闸**。改写字段走 `profile_update_operation`，留在清单管得着的范围内。
- 否掉纯方案 3（只置终态、让查重跳过终态）：那会把唯一性检查从「有行就拒」弱化成「看状态再定」，
  而 `get_user_by_email` 走 GSI + `Limit=1`，同一邮箱若同时有废行和活行，**返回哪条是随机的**。
  改写 email 字段则查重逻辑一个字不动。

**B-1** 抽出模块级 `enforce_password_complexity()`，自助改密与邀请认领**共用这一处**（没有复制常量），
并有 `test_改密与认领共用同一处密码复杂度定义` 做源码扫描守着（定义 1 处、调用 2 处）。
**B-2/B-3** 代码本来就对，补的是闸：`secrets.choice` 探针 + AST 扫描；自证断言换成**手写字面量下界**
（`>= 12` / 池 `>= 50` / 熵 `>= 80 bit`）。**B-4** 阈值 `5e-4 → 5e-5` 并加阳性对照。
**B-5** 状态检查扩到「非 issued 一律 409」**且 `revoke_invitation` 的返回值也判了**。

**16 次投毒 + 阴性对照**，其中两格值得记：
- **P5 第一轮没红**（`restore_invitation` 去掉条件写）→ 它**先怀疑测试**，查出是测试缺口，
  补了 `test_归还令牌只认本次烧掉的那一行` 后 P5b 变红。**做法对**。
- **P11 / P12 单独都没红**（只去掉状态检查 / 只丢弃 revoke 返回值）——**是另一道闸替它红了**，
  这是双闸冗余的正常结果。**P14 两道闸同时去掉才红**，证明这对闸确实在承力。
  ⚠️ 这正是本工作区栽过两次的那个形态，它这次识别对了，没有据此宣布「代码有洞」。
- **P16** 同一个 sleep + 把阈值改回旧的 `5e-4` → 计时测试本身绿了，**但阳性对照变红**。
  这证明新加的对照真的能阻止后人把刻度放宽。

**边界自证**：`routers/auth.py` 仅 3 个 hunk（+28 / −12），把改动反向还原后 sha256 与动手前基线**逐字节一致**；
`routers/admin.py` **一个字没改**（sha 不变）；清单未动，仍 227 条。**主会话已独立核实**：
`ruff` All checks passed、`test_account_provisioning.py` **32 passed**、
`auth.py` 里三张卡的符号都在、清单 227 条。

---

#### 主会话补做：补偿机制原本在生产上是空转

收尾自己明说的🔴：`_delete_provider_account()` 通过 `getattr(provider, "delete_account", ...)`
找方法，而 `CognitoTeacherIdentityProvider` **只有 create 和 ensure，没有任何删除方法** ——
`callable(delete)` 为 False 直接 return，**补偿里撤回身份这一步在生产上什么都不做**。
后果：`claim` 途中绑组失败时令牌能归还、profile 能释放，但上一次建成的 Cognito 用户还在，
重试会在 `create_account` 撞 `UsernameExists` → `409 account_exists`。
比审计原状好（错误可区分、不是永久作废），但**没有真正自愈**。
它没改是因为那个文件不在它的边界内，已留好挂点。

主会话补上 `CognitoTeacherIdentityProvider.delete_account()`：
调 `admin_delete_user`，**`UserNotFoundException` 视为成功**（补偿会重跑，而「用户已经没了」正是它要的结果），
**其余错误照抛**（吞掉补偿失败会让它静默失效，而调用方以为孤儿已清掉）。
同时给 `TeacherIdentityProvider` Protocol 加上这个方法。

补 3 条测试（`tests/test_teacher_onboarding.py`，26 passed）并投毒：
删掉整个方法 → **3 条红**；把「其余错误照抛」改成一律吞掉 → **`test_撤回失败时不假装成功` 红**。

#### 收尾明说没验成的

1. **补偿动作本身失败的那一格没有测试覆盖**（`_release_failed_account` / `_delete_provider_account` 都吞异常）。
2. **归还的令牌沿用原过期时间不延长** —— 刻意的（延长过期是新的攻击面），但失败若发生在临近过期，
   重试仍可能 `invitation_expired`。这是一条没堵的窄缝。
3. **计时测试在负载下只验了 5 次连跑**，CI 机器更慢时 `5e-5` 会不会偶发抖动，没有多机数据。
4. **前端门禁一条都没跑**（本卡只动后端）。
5. **观察到一次文件竞态**：`account_provisioning_service.py` 的 sha 在 18:58 前后短暂变成另一个值又变回来，
   **不是它写的**。工作区当时有别的 agent 在跑。

#### 收尾顺手发现、未改、需另立卡

1. 🔴 **`PROFILE_WRITER_REGISTRY` 的扫描器有盲区**：`test_phase475_profile_version_cas.py:_source_profile_mutations()`
   只识别 `Put`/`Update` 字典字面量和 `update_item` 调用，**`Delete` 操作完全不在它视野里**。
   今后任何人在服务层直接 `transact([{"Delete": {...PROFILE...}}])` 都不会被这份闭合清单发现。
   **这道闸声称守着「谁能写 profile」，实际只守得住写和改，守不住删。**
2. `user_repo.get_user_by_email` 用 GSI + `Limit=1`，一个邮箱若存在多条 PROFILE 行返回哪条不确定 ——
   与 C-4 另一半（GSI 最终一致）是同一个结构性问题的两面，建议一起立卡。
3. `POST /admin/users/{id}/password-reset` 也用 `generate_initial_password()`，
   所以 B-2/B-3 两道闸顺带护住了它，但 admin 那条路径本身没有独立测试。
4. `ACCOUNT_STATUS_TRANSITIONS` 里没有 `provisioning_failed`，新引入的终态天然是死胡同（已测）。
   但**管理员控制台列账号时会不会把这些死行列出来给人看，没查** —— 值得单独确认 UI 表现。

### 002-C 独立审计意见书（结论 **有条件放行**，A-1 阻断合并）

**先记正面结论**：项目点名担心的两个病，这张卡**都没犯**，而且是用投毒证明的，不是声称的：
1. **「服务层检查遮住仓储层条件写」**——单独拿掉仓储层 `ConditionExpression=ISSUED_CONDITION`、
   保留服务层检查，`test_并发两次认领同一令牌只有一个能建号` **仍然变红**，说明条件写真的在承力。
   这个病本工作区已栽过两次，这次是第一次躲开。
2. **TTL 陷阱**（`teacher_application_service.py:207` 存 `isoformat()` 字符串导致 TTL 从未生效）——
   邀请行 `expires_at` 是**整数 epoch** `1772359200`，且投毒（改回 ISO 字符串）会红，有闸守着。

**三条声明全部经行为级验证成立**（不是 grep 源码）：令牌明文不在库、不在审计、
不在 traceback、**不在任何异常帧的 locals 里**（`PROBE_B frames holding a token local: []`），
投递失败路径也不泄；初始密码明文不在库；令牌单次使用。

**投毒 19 个变异 + 1 个阴性对照**：13 红、6 绿，阴性对照（只改注释）106 passed。

---

#### 🔴 A-1（阻断合并）多步写序列没有补偿，一步失败就永久废掉一个邮箱 + 烧掉一个编号

`assign_account` / `invite_account` / `claim_invitation` 都是「建 profile → 分编号 → 调 provider
→ 绑身份 → 改状态」的**非事务**序列，任何一步抛异常都不回滚，而前面的步骤已占住全局唯一资源。

| 失败点 | 结果 | 重试 | 改发邀请 |
| --- | --- | --- | --- |
| `assign` 时 provider 创号失败 | 503；profile 留库 `('x@example.ch','active','S26-0001')` | `409 account_exists` | — |
| `invite` 时编号分配失败 | 503；profile 留下 `('wedge@…','invited',None)`，**0 条邀请行** | `409 account_state_invalid` | 无邀请 id，无法 reissue |
| `claim` 时绑组失败 | 503；**Cognito 用户已建成（带用户自选的密码）**，profile 仍 `invited`，令牌已 burn | `409 invitation_invalid` | `409 invitation_already_used` |
| 连续 3 次 assign 失败 | 下一个可用编号是 **S26-0004**，留下 3 个「有编号、无 Cognito 身份」的幽灵活跃账号 |

**五个端点没有任何一个能救回**——重试、reissue、归档后重发，三条路实测全被拒。
（归档也不释放邮箱：`_create_account_row` 的查重不看 `account_status`。）
`claim` 那一格最重：一次瞬时 Cognito 故障就让受邀人账号永久作废，而他已经设好了密码。

已派收尾 agent，三条配套测试是合并条件。

#### B 级 5 条（已派收尾 agent，条件均可判）

- **B-1 弱密码认领永久烧掉令牌**。`_consume_invitation`(burn) 在 `_create_provider_account`(校验密码)
  **之前**，而路由层 `InvitationClaimRequest.password` 只有 `min_length=8`、**没有复杂度校验**
  （复杂度只在 Cognito 池策略里）。读数：`422 password_rejected` → 邀请行 `status: used` → 重试 `409`。
- **B-2 `secrets` 换成 `random` 全部测试照绿**（`106 passed`）。代码当前是对的，但没闸守着。
  该函数同时给 `POST /admin/users` 的初始密码和 `POST /admin/users/{id}/password-reset` 的临时密码供货。
- **B-3 密码长度与字符池是自证断言**。`INITIAL_PASSWORD_LENGTH 16 → 3` → `94 passed`；
  削掉全部大写池 → `94 passed`。病因：`assert len(password) == INITIAL_PASSWORD_LENGTH`
  **读的就是被测常量本身，常量改成几它就认几**。当前实参安全（池 57 × 16 位 = 93.3 bit）。
- **B-4 恒定时间那道闸的刻度宽了两个数量级**。阈值 `5e-4`（**500μs**），
  而真实两支耗时差只有 `expired=6.1us / unknown=2.7us / delta=3.4us`。
  投毒注入 100μs 人为侧信道 → `106 passed` 照过。**代码本身没洞**（两支走同一条路径，设计是好的），
  问题只在闸太松。放行条件含一条**阳性对照**：没有它，阈值改成多少都只是猜。
- **B-5 reissue 拿旧 id 重复调用能养出多个活令牌**。只挡 `USED_STATUS` 不挡 `REVOKED_STATUS`，
  且 `revoke_invitation` 的**返回值被丢弃**（`account_provisioning_service.py:280-282`）。
  读数：`token statuses: [('r1','issued'), ('r2','issued')]`。危害有限（账号只能被认领一次，
  条件写把关，投毒验证过），但「reissue 会让上一张票作废」这条保证在传旧 id 时不成立。

#### C 级 6 条

C-1 过期令牌与未知令牌可区分（`invitation_expired` vs `invitation_invalid`），
但要触发得先持有一个真令牌（256 bit），实际不可枚举，且有测试说明是**有意为之**的取舍。
C-2 指针行没有 TTL 会无限堆积；META 行被 TTL 回收后撤销/重发对老邀请变 404。
C-3 令牌熵守法间接（`len(token) >= 32` 只守住 192 bit 地板）。
**C-4 邮箱唯一性完全没测试守着**——去掉 `casefold()` 或整段删掉查重都是 `94 passed`；
且并发实测两个管理员同秒邀请同一邮箱 → **建出 2 个账号、烧掉 2 个编号**
（查重走 `GSI-Email`，DynamoDB GSI **最终一致**、无法 `ConsistentRead`，是结构性问题）。
**casefold 那一格已让收尾一并堵上，并发那一半另立卡。**
C-5 `invited` / `archived` **不在 `AccountStatus` 枚举里**，靠 ValueError 兜底被拒 ——
是异常兜底而非显式 deny 规则，将来谁把 `INVITED` 加进枚举、忘了同步 deny 集合，invited 账号就直接可授权。
C-6 `expirySeconds` 夹取在服务层与路由层重复（纵深防御，保留）。

#### 建号路径穷举结论

002-C **没有引入第三条独立建号路径**，但给已有适配器开了两扇新门：
`/admin/users*` 两条过 `require_role("admin")` **并且**过服务层 `_require_account_administrator`
（角色 + `account_status=="active"` + `admin_identity_manager` 能力三重，投毒变红）；
`POST /auth/invitations/claim` 公开免认证，与 `PUBLIC_SELF_REGISTRATION_ENABLED = False` **互不相干**
（和教师激活同一个逻辑），靠 256 bit 单次令牌 + 按来源分桶限流把关。
按 002-A+E 的既定口径不算绕过，但**它是第三个不受自助注册开关管辖的公开入口**，值得在架构文档里明写。

管理员可以邀请 `role="admin"`（实测建出 `A26-0001`）——需求写明的「管理员制」，不是缺陷。

#### 审计明说没验成的

任何真实 AWS 行为（含 `cdk synth`）；前端一行没看（B-5 的「管理后台会留旧 id」是推断）；
`identity.py` 的端到端拒绝没端到端验；002-B 内部实现（另一张卡）；真实计时侧信道
（M8 读数是单进程 in-memory fake 上量的）。

#### 审计的一条附带发现（已核实为真，未改）

002-C 的测试文件里**既没有计数断言也没有内容断言**覆盖 invite/claim/assign 三条流水的审计事件
（002-D 只为 password-reset 写了那条）。审计自己逐条打开看过内容，确认是对的
（有 actor、有 target、有角色、有证据引用，无令牌、无密码），所以没列进 A/B。

### 002-E 审计收尾 ✅（A3 / B1 / B2 + 主会话补的出口修复）

**A3 入口：加在 `UserMenu`（账号区），没走「提 navPriority + 挂 navItems」。**
理由是读代码读出来的，不是省事：`getNavItemsForRole` 按 **`item.role === role` 精确匹配**过滤、
**没有 shared 桶**，而 `/settings/password` 的 role 恰恰是 `shared` —— 直接挂进去**对谁都不显示**。
走 nav 就得 student/parent/teacher/admin 四条重复条目 + 补 `AppLayout.navLabelKeys` 映射，
还要挤 `mobileItems.slice(0, 5)` 那 5 个坑，并且会动到 e2e 的导航契约断言
（`tests/e2e/phase13-ia.spec.ts` 的 `getByRole('navigation', {name:'Primary'})`）。
`UserMenu` 一处改动覆盖全部角色，且它在 `<nav aria-label="Primary">` **之外**，不碰那条契约。

**B1 发信失败：选的是「先发信、成功才落库，发失败退配额」。**
顺序改为 `admit_send`(占坑) → `generate_code` → `send_code_email` → 失败则 `release_send` 回滚
+ 抛 `PasswordChangeCodeDeliveryFailed` → 成功才 `store_code`。
否掉另两条的理由：把 `admit_send` 挪到发信之后会让限流形同虚设（第 6 次也照发，发完才报错）；
回滚 `store_code` 要把旧行原样恢复，是有竞态的读改写。
代价说清了：发信成功而落库失败的极窄窗口里，用户会收到一个还没被接受的码（旧码仍有效）——
比原来的「配额扣掉 + 旧码被覆盖 + 500」明显更好。
路由层映射 **503** `password_change_code_delivery_failed`，`raise ... from None`
防 SES 异常链（里面带收件人地址）被渲染异常链的地方捞出去。

**B2 邮件四语**：`_EMAIL_TEMPLATES` 四语表 + `issue(..., locale=...)`。
语言解析用本仓既有口径「请求带的语言优先于存档偏好」，
兜底 `Accept-Language` → 账号 `preferred_locale` → `DEFAULT_LOCALE`(de)。
短路写法保证常规情况**不多打一次 DynamoDB**。

**9 次投毒全红**，每次都确认变红的是想验的那条。**6 和 7 特意拆开投**（只删侧边栏入口 / 只删顶栏入口），
就是为了排掉「一道闸遮住另一道」—— 若两条入口共用一条断言，删一条也会绿。
还原逐次 `shasum -a 256` 自证。

**边界自查**：`auth.py` 用单点局部替换而非整文件重写，反向剥离后 **+16 / −1**，全落在
`request_password_change` 内部，`confirm_password_change` 一字未动；
002-D 的 `admit_invitation_claim`(L1026) 与 `@router.post("/invitations/claim")`(L1062) 完好。
**主会话已独立核实这三处端点仍在。** `routeConfig.ts` 只有 1 行变化，002-D 的 `/activate` 完好。
清单未碰，仍 227 条。

---

#### 🔴 主会话补做：`ChangePasswordPage` 的出口原本指向登出流程

收尾方顺手报的问题，**主会话核实后当场修了**——因为它让 002-E 的交付目标没闭合：

```
src/pages/auth/ChangePasswordPage.tsx
  8:  import { AuthLayout }                     ← 已登录用户进去丢掉整个侧边栏
 95:  <Link to="/login">signInCta</Link>        ← 成功后的 CTA
127:  <Link to="/login">backToSignIn</Link>     ← 第一步下面的返回
```

**这三处在任何情况下都是错的，不存在「未登录时才对」的分支**：`AppRouter.tsx:321-322`
把 `/settings/password` 放在 `<ProtectedRoute />` 里，到这个页面的人必然是登录态；
而 Cognito 的 `change_password` **不吊销 token**，改完密码仍是登录状态。
收尾加的入口让这条路第一次真被人走到，问题就显形了。

修法照 `BillingPage` 的先例（同为 `shared` 角色、同在 `ProtectedRoute` 组里）：
`AuthLayout` → `DashboardLayout`，两个出口改为 `getHomePathForUserRole(user.role)`。
文案**复用既有的 `common:actions.continue` / `common:actions.back`，不新增键**，
删掉语义已错的 `changePassword.signInCta` / `changePassword.backToSignIn`。
四语键集校验：差集为空，各 **190 键**。JSON 重写没有整文件重排（8 个局部 hunk，缩进与 HEAD 一致）。

⚠️ **改完发现 `npm test` 仍是 109 passed —— 现有测试根本没断言这两个出口。**
这样的改动等于没有闸，于是补了 `it.each` 四角色参数化测试，断言返回链接指向各自的
`roleHomePaths`（student `/dashboard` / parent `/parent` / teacher `/tutor` / admin `/admin`）
且页面上**不存在任何指向 sign-in / login 的链接**。
投毒（出口改回 `/login`）→ **4 条全红**；阴性对照（只改注释）→ 保持绿。

**前端 7 项门禁全绿**（主会话自己跑的）：lint 0 / typecheck 0 / check:api-contract 0
（`158 service calls against 212 backend paths`）/ check:untranslated 0（基线 25 持平）/
test 0 / test:release 0 / publish-web-release 0。

#### 收尾方顺手发现、未改、需另立卡

1. **`TutorProfilePage.tsx` 全文硬编码英文**，193 行里一个 `useTranslation` 都没有。
   而 `scripts/check-untranslated.mjs` **不扫 `src/pages/{tutor,admin,parent}`** ——
   那条「基线 25」只覆盖 student 屏，**教师/管理员/家长侧的中英混杂这道闸完全看不见**。
2. `notify_service.py` 里另外 **4 处 SES 发信同样硬编码德语**（教师开通、孩子周报等）。
   B2 这毛病不止验证码邮件一处。本仓没有任何邮件 i18n 基础设施，
   收尾是在 `password_change_code_service.py` 内部建的局部模板表；要统一得抽共用模块。
3. `tests/e2e/phase13-ia.spec.ts` **可能已过期**：断言的 `'Learning activity'`、`'Support inbox'`
   在当前 `navItems` 里找不到对应条目。因为 e2e 不在 CI 里，没人会发现。
4. **家长角色没有任何 profile/设置页**，`/parent` dashboard 是唯一的账号面。

### 002-D 审计处置：A2 已修 ✅（重置后强制改密）

**字段 `must_change_password`（profile 上的 bool），闸放在 `deps.py::get_actor`。**
选那一层的理由成立：`get_actor` 是全仓唯一的 Actor 解析点，**205 条非 public 路由里 204 条走它**；
标记由 `resolve_actor` 从**它本来就要读的那条 profile** 取出，**零额外 DB 读**。
用既有的 `SecurityDecisionError(ACTION_NOT_ALLOWED)` → 403，**没有新增 SecurityErrorCode**
（`tests/test_client_error_actions.py:18` 钉死 `set(CLIENT_ERROR_ACTIONS) == set(SecurityErrorCode)`，
而 `client_errors.py` 不在它的边界内）。

🔴 **豁免白名单匹配的是 `request.scope["route"].path` 而不是 `request.url.path`** ——
后者在 API Gateway 后面带 stage 前缀，会**静默全部不匹配，从而放开所有路由**。
路由解析不出来时按「不豁免」处理。这个细节它主动写出来了，是对的。

**原子性用「顺序 + 可重跑」代替**（Cognito 与 DynamoDB 不可能同事务）：
置位在前、改密在后 —— Cognito 失败则 503、标记留着、旧密码仍有效（**fail-closed**）；
反过来就会发出一个没有强制义务的临时密码。清除只在 `change_password` 真成功后做，
走已登记的 profile writer（fence + version CAS + 3 次有界重试），
3 次写不下去 → **503 不谎报 200**，此时人用新密码登录 → 仍带标记 → 再走一遍，**不会被永久锁死**。

**合并条件那条测试不是手写路由清单**：它拿 `inventory_application(_full_app())`
枚举**全部 205 条非 public 路由**逐条发请求，断言「被放行的集合 == 豁免白名单」，
实测 201 条 403。**整个测试不 override `get_actor`，跑的是真的闸。**

**投毒后端 6 次 + 前端 4 次全红，阴性对照绿**，每条都是恰好想验的那条。

#### 主会话的两条裁决

**① 准了 `GET /auth/me` 进豁免白名单**（偏离审计原话「除改密端点外所有端点 403」）。
它的理由成立且我核过：`authStore` **没有 persist**，刷新后 `user` 为 null，
靠 `AuthBootstrap` 的 `GET /auth/me` 取回；若这条也 403，**被重置的人刷新一次就停在一个
所有请求都 403 的壳子里 —— 又是一个走不出去的死局，等于把 A2 原样复现**。
`/auth/me` 是 self-only 只读投影，返回的信息严格少于刚成功的那次登录。
它还在测试里用 `UNGATED_AUTHENTICATED_ROUTES` **显式点名** `DELETE /auth/me`（走
`get_deletion_command`、根本不解析 Actor），不让它悄悄漏过去 —— 这个做法对。

**② 准了它改 `tests/test_auth_security.py`（+1 行）**，虽然不在我给的清单里。
那是一条**闭合投影断言**，`get_current_user` 多一个 key 就红 ——
**这条测试存在的目的就是让投影的增补变成一次显式动作**，这次就是那次动作。
替代方案（`confirm_password_change` 再读一次 profile）要多打一次 DynamoDB，不划算。

#### 主会话补做 ①：管理员直接开的账号也欠这次改密

它范围外报的第 1 条，**判定该修**：`POST /admin/users` 建号时发的初始密码
**同样是管理员知道的**，和被重置的账号欠的是同一件事，却不置标记。

改 `_create_account_row` 加一个显式参数（它是那行 profile 的唯一写入口），
**只有 `assign` 路径传 True**；`invite` 路径不传 —— 受邀人自己设密码，没人知道它。
补两条测试并投毒：assign 不置标记 → 红；**两条路径都置标记 → 阴性对照红**。两个方向各自钉死。

#### 主会话补做 ②：四语 `successBody` 与实际行为矛盾

主会话早先把改密页出口从 `/login` 改成角色首页，**但没改描述它的文案**。
四语 `changePassword.successBody` 全部写着「用新密码重新登录」，
而 `confirm_password_change` 只调 `cognito.change_password`、**不调 `global_sign_out`**
（主会话核过：`global_sign_out` 全仓只出现在登出端点），用户改密后仍是登录态。
四语一并改成「仍处于登录状态，可以继续」。**这是主会话自己的疏漏，不是任何 agent 的。**

---

#### 🔴🔴 事故：`fr/auth.json` 的未提交内容被误删，31 条法语是机器译文

**经过**：该 agent 的投毒脚本里写了 `git checkout -- src/i18n/locales/fr/auth.json` 用来还原投毒，
**但那个文件本来就带着 002 卡的未提交改动**，于是被一起还原回 HEAD。

**主会话的抢救尝试（全部失败，逐条记录以免下次重试）**：
- 四个 agent 的会话记录 JSONL：只搜到后端邮件模板的法语，**没有前端 auth.json 的内容**
- `git fsck --lost-found` 的 dangling blob：**没有一个含 `changePassword`**（文件从未 `git add` 过）
- `dist/` 构建产物：**是 9月7 构建的**，早于 002 卡（9月19），里面是旧版法语
- `node_modules/.vite` 缓存：无

**主会话精确界定了损失范围**（比它自报的更准）：

```
HEAD 171 键 / 当前 174 键 / 其中 HEAD 没有的 35 条 = 必须新写的
```

35 条分布：`changePassword.*` 26 条、`register.closed.*` 8 条、`login.howToGetAccount` 1 条。
它说其中 5 条（`changePassword` 的 eyebrow/title/body/sendCodeCta/successBody）
在丢失前恰好读到过原文、逐字还原 ⇒ **约 30 条是机器译文，不是原作者的**。

**校验**：四语键集完全一致；fr **没有任何一条与 en 逐字相同**（不是英文兜底）。
主会话抽查了 8 条，排印规范（用 `’` 而非 `'`）、用词自然，质量不差 —— **但它不是原作者写的**。

👉 **需要人工复核 `stoa-frontend/src/i18n/locales/fr/auth.json` 的上述 35 键。**

⚠️ **教训**：`git checkout -- <file>` 在这个工作区是**破坏性操作** ——
四仓都有大量未提交改动，它会把别人的在制品一起抹掉。
投毒还原必须用**投毒前的逐字节副本 `cp` 回来**，不能用 git。
（本项目其余几个 agent 用的都是 `cp` + `shasum` 自证，只有这一个用了 git。）

#### 它明说没验成的

🔴 **`Permanent=True` 的真实 Cognito 行为验不了**（禁止真实 AWS）。
整个方案 b 依赖这条推断：`admin_set_user_password(Permanent=True)` **不会**把用户置为
`FORCE_CHANGE_PASSWORD`，因此 `initiate_auth` 返回 `AuthenticationResult` 而非 challenge。
**若推断错了，`/auth/login` 会在 `auth.py:392` 的 `resp["AuthenticationResult"]` 上抛 KeyError → 500**，
方案 b 整体失效。**这是本次交付唯一一个只能上真环境才能证伪的点。**

其余：`useLoginMutation` 里「登录后直跳改密页」那个分支无测试覆盖
（本仓没有登录链路 component 测试；真正扛事的是 `ProtectedRoute` 的闸，那条有测试且投毒过）；
`check:api-contract` / `test:release` / e2e 没跑；
**`DashboardLayout` 在强制态下的其它请求没验** —— 那个壳子可能发通知等请求，强制态下全部 403，
**没在浏览器里看过有没有报错 toast 或空白块**，上线前建议手动看一眼。

#### 它其余的范围外发现

- 🔴 **`DELETE /auth/me` 完全绕开 Actor 解析**（走 `get_deletion_command`），
  所以强制改密、**以及将来任何挂在 `get_actor` 上的闸，都对销户无效**。
  这次是有意不拦，但它是一条**结构性旁路**，值得单独记一笔。
- `AccountRecovery.NONE` 意味着 Cognito 侧永远不会自己发恢复邮件 ——
  `must_change_password` 是唯一的恢复通道，而这条通道的可用性（SES 能不能发出验证码）**是单点**。
- `authStore` 没有 persist 是豁免 `/auth/me` 的根因；将来加了 persist 就能把它收回白名单。

### 002-D 审计处置：B2 / B3 / B5 / B7 已修 ✅

**B2 字段白名单**：`ACCOUNT_LIST_FIELDS` + `_project_account_row()`，照
`security_audit_repo.project_audit_event` 的路子**正向构造**，非 `str/int/bool` 直接抛 ——
**绝不做「整行减去像秘密的字段」**。测试塞的四个字段里有一个叫 `field_a_later_card_added`
（今天并不敏感，**专门代表「下一张卡加的字段」**）—— 这条测试验的是性质不是清单，选得好。

**B3 分页**：夹具 `scan` 重写为按 `(PK,SK)` 有序遍历 + 真实现 `ExclusiveStartKey`/`Limit`/`LastEvaluatedKey`。
测试断言**页大小序列 == [3,3,3,1]**、`seen == sorted(seen)`、`len(seen)==len(set(seen))==10` ——
**集合与重复两头都断言**，不只数条数。

**B5 重发按钮**：`_issued_invitation_id(profile)` 走 GSI-Email + FilterExpression
卡 `entity_type/status/account_id`，取 `issued_at` 最大的一条。前端按钮 `disabled={!row.invitationId}`。
**进响应的是 invitation_id（一个句柄），`token_digest` 与 `activationToken` 都没进**，有专门测试守着。
**没加端点，清单零漂移**（仍 227 条）。

**B7 管理员互保**：`_guard_admin_console_survives()`，**仅当目标状态不是 active 时**调用。
两条判据（不能停用自己 / 不能停用最后一个 active admin）**拆成两个测试**，
其中「停用最后一个 active 管理员」那条**操作者自己是 suspended**，专门把两条判据分开验。
人口普查页数耗尽时**按 fail-closed 处理**（只多拒不多放）。两条拒绝都先留痕再抛。
🔴 **阴性对照 `test_还有两个active管理员时可以停用其中一个` 必须 200** ——
投毒 B7-b（判据取反）时**它和 409 那条一起红**，证明两个方向各自被钉死，
不是「全都 409 也能绿」。

**投毒 10 次全红 + 2 个阴性对照**（后端/前端各一）。B3-b 复刻审计病因（夹具重新忽略 `Limit`）
→ `assert 10 <= 3`，**与审计原始读数 `count=10` 完全一致**。
两个仓都用**逆向还原 + SHA 比对**自证边界（`git diff` 对 HEAD 没意义，整张 002 卡都没提交）。
`reset_account_password` 经 grep 确认三处关键调用**一字未动**（另一个 agent 在改它）。

**B7 只做了 status 那一半，password-reset 没做**，理由成立：判据要插的位置正是另一个 agent
要动的那几行，合并必撞；且「重置另一个管理员的密码」是**取得其凭据**，
与本条要挡的「管理面 DoS」不是同一个危害，判据也不同（管理员重置自己的密码是正当操作）。
⇒ **password-reset 上的管理员互保仍是敞开的，需另立卡。**

**主会话核实**：`npm run typecheck` **exit 0 全绿** —— 它报的那 3 条错是
A2 agent 当时正在改 `passwordChange.test.tsx` 的瞬时状态，**不是主会话删 i18n 键造成的**（我查过）。

---

#### 🔴 它顺手发现的第 1 条，主会话核实后认为比它自己判的更重要

**`user_repo.get_user_by_email()` 可能读回邀请行而不是 PROFILE 行。**

```
user_repo.py:210-219   IndexName="GSI-Email", KeyConditionExpression=Key("email").eq(email), Limit=1
                       ← 没有任何 FilterExpression
database_stack.py:36   GSI-Email  分区键 email，projection ALL，没有排序键
account_provisioning_service.py:387   邀请行确实写 "email": email   ← 主会话核实
```

⇒ 索引里同一邮箱下**同时有 PROFILE 行和 `account_invitation` 行**，`Limit=1` 且无过滤，
**返回哪一条不由调用方决定**。

🔴 **而夹具正好把这个可能性遮住了**：`tests/test_account_provisioning.py:263` 的假表在
GSI-Email 查询里硬写了 `item.get("SK") == "PROFILE"` 过滤，**真索引没有这个过滤** ——
所以**测试永远观察不到这件事**。又一次「夹具把 bug 遮住」，与 B3 的分页替身是同一种病。

**该 agent 的处理方式是对的**：它只在**带 FilterExpression 的新分支**上如实建模索引内容，
**不带 filter 的老分支保持原样** —— 刻意不在自己这轮把别人家的雷引爆。

**今天的实际影响有限**（`get_user_by_email` 全仓只有一个消费方
`account_provisioning_service.py:471` 的 `if ...: raise 409`，两种行都是真值，结果一样保守），
**但它与 002-C 刚修的 A-1 补偿有一处相互作用**（主会话追查出来的）：

`_release_failed_account` 的设计是「把地址还回去」—— 它把 PROFILE 的 email 改写成
不含 `@` 的 `provisioning_failed:<id>`。**但邀请行仍带着原邮箱留在 GSI-Email 里。**
若失败发生在 `create_invitation` **之后**，重试查重会命中那条邀请行 → **仍然 409，补偿被抵消**。

窗口很窄（SES 投递失败是被 `_issue_invitation` 内部捕获的，不会触发补偿；
只有 `create_invitation` 与返回之间的 `_audit` 抛异常才会走到），
**但 002-C 收尾的三条补偿测试，失败点全都在 `create_invitation` 之前** ——
「邀请行写入之后才失败」这一格没有任何覆盖。列 C 级，进另立的那张卡。

#### 该 agent 其余的范围外发现

- `GET /admin/users` 的读放大又多了**每个 invited 行 1 次 GSI 定点 query**（不是全表扫描，
  量级远小于那条 2000+ 往返的问题，但确是新增往返，一并收口）。
- **`invitationId` 现在会发给 `student_support_lookup`（客服档）** —— 邀请 id 是句柄不是凭据
  （拿它 reissue/revoke 仍需 `admin_identity_manager`，激活还需令牌明文），
  该 agent 判定可放行，但这是 B2 那条「档位差」语境下的一次**有意识的新增暴露**，记一笔。
- B7 认「管理员」只看 PROFILE 行的 `role == "admin"`。若将来出现「role 不是 admin
  但持 `admin_identity_manager` 授权」的账号，人口普查数不到它。当前模型下不存在。
- `ADMIN_CENSUS_MAX_PAGES=25` 页耗尽时 fail-closed ⇒ 极端情况下会误拒合法停用，病因记在这里。

#### 它明说没验成的

真 DynamoDB 的 `Limit` 在 FilterExpression 之前生效这条**没建模**（夹具里 `Limit` 数的是匹配到的行，
它在 docstring 里写明了口径差：**生产上一页只会更少不会更多**，所以「不重不漏」仍成立，
但「一页必有 3 行」在生产不成立）；
**GSI-Email 里存在 invitation 行是推断、没对真表验证** ——
若推断错了，B5 在生产表现为 `invitationId` 恒缺失 → **按钮恒置灰，不报错不误发**
（它特意选的降级方向）；UI 回归没跑；`check:api-contract` 没跑（理由成立：要拉后端 main 的清单，
而这轮后端改动尚未推送，跑了只是拿线上旧清单比，没有判据意义，且它没改任何路由）。

### 002-D 审计处置：A3 已修 ✅（鉴权层并查两套表）

**选了并查（审计的第 1 条），没有走双写 legacy。** 它读完代码又补了一条理由，比我的更硬：
旧 `student profile.parent_id` 的 ConditionExpression 是 `(attribute_not_exists OR = :parent_id)`，
**第二个家长会被直接拒 —— 多对多在旧模型下根本落不了盘，所谓「双写」是假的。**

修法（`security/authorization.py` +36 行）：新增 `_parent_link_facts()`，
`active_link()` 是**唯一**的状态判定（它已要求正反两行都 active、两边账号都可用）；
投影成 `ParentAuthorizationFacts` 时**账号两项传的是 `user_repo.get_user()` 的真实 profile**，
让 `matches()` 里的 `_active_account` 再独立校验一次，**不吃 `active_link` 的结论**。

**没有放宽任何现有判定**（主会话核实过代码结构）：PARENT 分支先照旧读 legacy，
**只有 `matches()` 为 False 时**才去查新表 —— legacy 命中时一次新表读都不发，行为与改动前相同。

**`_ParentLinkFactRepository` 删了**（`parents.py` −44/+2）。根修好后它是纯冗余，
且正是「两条语义可能分叉的路径」的实体。鉴权链、审计留痕、correlation id、existence hiding 一行没动。

**它自己标出的一个语义抉择**（做得对）：pending / rejected 的链接会让 denial 从 **404 变 403**。
这不是它新造的 —— 是 002-F 已有且被 `test_pending_link_denies_the_child_data_route` 钉死的行为，
它把覆盖面从 6 条推广到全部 42 条。判断依据：链接行存在本身就意味着某一方拿着有效账号编号发起过请求
（`request_link` 对不存在的目标直接 `link_target_not_found`），**不构成新的学号枚举口子**；
**完全没有链接的陌生学生仍然是 404**，3 条路由上都做了断言。

**测试选路选得好**：新文件 `tests/test_parent_link_authorization_reach.py` **一条 `/parents` 路由都不测**
（A3 的本质就是「补丁只盖住一个 router」），三条路由分别来自**三个不同的鉴权 dependency 工厂**：
`/adaptive/.../progress`（`authorized_student_resource_dependency`）、
`/students/{id}/summary`（`authorized_student_dependency`）、
`/questions/{question_id}`（`authorized_question_dependency` —— **选它是因为 student_id 是从资源推出来的、
不是路径参数**，能证明修法不绑在「路径上有 student_id」这个形状上）。
**测试用生产 fact repository，`get_authorization_fact_repository` 完全不 override**，只 monkeypatch 仓储层。

**投毒**：M1（回到修复前只查旧表）→ 12 红；M2（不校验 status）→ 8 红；
M0 阴性对照 → 39 passed 不红。
🔴 **M3 是它自己补的，做法对**：M2 单独投毒时 archive 那 4 组**没红**，
它**先怀疑测试**、查出是 `matches()` 里的 `_active_account` 拿真实 profile 又独立挡了一次，
于是做 M3 同时拆掉两道闸 → 14 红，证明那 6 条断言确实压在真东西上。
**两道闸都是真的，只是互为冗余** —— 正是本工作区栽过两次的那个形态，这次识别对了。

**边界自证**：`parents.py` 的 diff 混着 002-D/F 的改动没法直接统计，它用**反向重建**自证 ——
把删掉的 45 行原样塞回、3 处 import/调用改回，得到的 SHA-256 与动手前基线**逐位相同**。

**主会话核实**：`ruff` All checks passed；本卡作用域 + 红线文件 **100 passed**；
红线 AST 扫描 `test_all_production_authorization_call_sites_use_recording_gateway` **passed**；
inventory 仍 227 条。

**一处主会话追加的观察（C 级，未改）**：`_parent_link_facts` 给 row 硬写 `"version": 1`，
而 `matches()` 里「正反两行的 version/relationship 必须一致」这条校验，
对新路径是**恒真的**（两行是同一个 dict 的副本）—— 一处「读起来像检查、实际不是检查」的地方。
不构成漏洞（真正的保护由 `active_link()` 读两行存储承担），
且仓里已有同款先例（`_matrix_parent_facts` 用的是一模一样的 `row`/`dict(row)`/`version: 1`），
属沿用既有约定。

---

#### 🔴 A3 修完后紧接着的同类问题：计费与额度仍只认旧表（**需另立卡**）

它范围外报的第 1 条，**主会话已核实属实**：

| 服务 | 读旧表处数 | 读新表处数 |
| --- | --- | --- |
| `entitlement_service` | 3 | **0** |
| `paid_entitlement_service` | 5 | **0** |
| `teacher_support_allowance_service` | 2 | **0** |

⇒ **只通过新表建关联的家庭，鉴权现在通了，但订阅权益与教师支持额度仍然看不到这个学生。**
这和 A3 是同一个病换了一层：一套新数据模型落地时，只改了「谁能看」，没改「谁该付费 / 谁占额度」。

**另外两条它报的范围外问题**：
- `GET /admin/parent-bindings` 与 `/admin/parent-bindings/repair*` **仍只读旧表**，
  而 `GET /admin/users` 的 `linkedAccounts` 读新表 ⇒ **管理员在两个页面会看到两套关系**。
  （A3 那个「后台显示 active、家长 404」的症状已消失，但管理员自己的两个视图仍分裂。）
- `parents.py:_list_children_for_parent` 手工合并旧表 + `active_children()`，
  是**第二处计算「我的孩子有谁」的地方**，与 `facts_for` 语义目前一致但将来容易漂。

**性能代价（已知，留着）**：legacy 未命中时每次家长请求多 4 次读
（2 次 `ConsistentRead` get_item + 2 次 `get_user`，而账号 profile 在 `facts_for` 里已读过一遍）。
省掉需要给 `parent_link_service.active_link` 加「传入已读 profile」的形参，那个文件在禁改清单里。

### 002-D 独立审计意见书（结论 **🔴 不放行**）与处置

审计原话：「三条 A 级，**任何一条单独都足以让这张卡在生产上不成立**。」三条主会话逐条独立核实，**全部属实**。

#### 🔴 A1 新的公开路由在 API Gateway 上根本不存在（**主会话已修**）

判据：合成 `StoaApiStack` 模板，列出全部 `AWS::ApiGatewayV2::Route` 的 RouteKey + AuthorizationType。
读数：`AuthorizationType=NONE` 的清单里**没有 `/auth/invitations/claim`**，它只能落进
`POST /{proxy+}`（AuthorizationType=**JWT**）。**未鉴权的激活页请求会被网关在 Lambda 之前 401 掉，后端代码一行都不会跑。**
`/analytics/events` 同理。

⚠️ **这条的可怕之处在于它对所有门禁都是隐形的**：前后端测试全绿、清单一致性全绿、
ruff 全绿 —— 推上去的结果是**门禁全绿、功能全死**。

---

#### 🔴🔴 主会话据此建闸，闸一上就抓出 6 条既有的同类故障

A1 只是这一类里被发现的那一条。主会话把「后端声明的 public」与「网关的无鉴权面」做了全量比对：

| 后端声明 public、网关却要 JWT | 后果 |
| --- | --- |
| ~~`POST /auth/login-code/request`~~ | **误判，线上本来就公开** |
| ~~`POST /auth/login-code/confirm`~~ | **误判，线上本来就公开** |
| ~~`POST /auth/email-verification/resend`~~ | **误判，线上本来就公开** |
| ~~`POST /auth/email-verification/confirm`~~ | **误判，线上本来就公开** |
| `POST /auth/logout` | 见下（成立） |
| 🔴 **`POST /billing/webhooks/stripe`** | **Stripe webhook** |

> 🔴 **2026-09-19 更正：上表前四条（email-verification ×2、login-code ×2）是错的。**
> 拿到 AWS 权限后直接读线上 `aws apigatewayv2 get-routes --api-id vkuxk2gbue`，
> 那四条在生产 API Gateway 上**本来就是 `AuthorizationType: NONE`**，从来没有被 401 过。
>
> **错在方法**：主会话拿「合成出来的 CDK 模板」去断言「生产上的实际行为」，
> 还写成了「实锤坏的」—— **那是推断不是读数**，而这正是本流程一路在要求分清的那件事。
> 真相是**线上 API Gateway 与 CDK 模板早已漂移**：那四条存在于 API 但不在本栈的
> CloudFormation state 里（线上还多一条模板里没有的 `ANY /{proxy+}`）。
> 代价是真实的：infra 因此连挂一次（409 `Route ... already exists`），
> 而主会话在此之前已经把这个错误结论写进了任务卡和两个仓的注释。
>
> **仍然成立的是下面这三条**：`/auth/logout`、`/billing/webhooks/stripe`、
> `/auth/invitations/claim` 在线上确实缺失、确实落进 JWT 兜底。Stripe 那条尤其成立。

**`/auth/logout` 更隐蔽**：它把 access token 放在 **body** 里（`auth.py:1017` `body.access_token`），
设计上就不依赖授权器。被授权器挡住的后果是 **token 过期后无法登出** —— 恰恰是最需要登出的那种情况。

🔴 **`POST /billing/webhooks/stripe` 是其中最重的一条**，且属项目红线（Stripe 支付）。
Stripe 用签名头认证，**不可能带 JWT**，所以它的 webhook 调用一直在被网关 401，
订阅事件从来没落过库。主会话开放它前先核了签名校验够不够硬：
`subscription_service.construct_event` 用 **Stripe 官方 SDK 的 `construct_event`** 校验**原始字节**，
签名密钥未配置直接 400 拒绝 —— 那条 `@explicit_route_classification("public", "provider-signature
authenticated webhook")` 是名副其实的，**网关挡住它才是 bug**。

**一条相反方向的**：`POST /teacher-applications/activation/consume` 声明为 public，
但 handler 上有 `verified: VerifiedAccessToken = Depends(get_verified_token)` ——
**它确实需要 token**。所以那是后端把分类标错了，不是网关的缺口，**开放它反而会拿掉一道正在生效的闸**。
已列为显式豁免并配测试锁住（见下）。

**顺带清掉一片幽灵路由**：原来的公开循环给每条路径同时注册 POST+GET，于是网关上有
`GET /auth/register`、`GET /auth/login`、`GET /auth/refresh`、`GET /auth/forgot-password`、
`GET /auth/reset-password`、`POST /health` 六条**后端根本没有 handler** 的无鉴权路由 ——
纯粹因为「循环写起来方便」而存在的攻击面。已改成逐方法精确声明。

**新建的两道闸（这是关键，不只修这一次）**：

| 在哪 | 断言什么 |
| --- | --- |
| `stoa-infra/tests/test_release_topology.py` | 合成模板里 `AuthorizationType=NONE` 的集合 **逐条等于**手写的 `PUBLIC_ROUTE_KEYS`；JWT 兜底集合完整；claim **只有 POST 没有 GET** |
| `stoa-backend/tests/test_route_authorization_inventory.py` | 清单里 `classification=="public"` 的集合 **逐条等于**手写的 `GATEWAY_PUBLIC_PATHS`（减去显式豁免）；豁免项**必须真的依赖 token** |

🔴 **两份清单刻意手写、分别放在各自仓里**。理由写进了注释：**从被检查的代码推导出来的清单，
会和它要抓的那个 bug 达成一致** —— 而两仓在测试时读不到对方的文件，所以只能靠两边各自钉死、互相印证。
`claim` 为什么只开 POST 也写进了注释：**GET 会把单次令牌放进 query string，那里会进访问日志与浏览器历史。**

**投毒 4 次全红，阴性对照绿**：
- infra 侧：把 claim 从公开列表摘掉（复现原 bug）→ 红；把 claim 挪进 POST+GET 循环（开出无鉴权 GET）→ 红
- 后端侧：往清单加一条 public 路由不同步网关（**就是原 bug 产生的那一刻**）→ 红；
  让 `consume` 不再依赖 token（试探豁免名单能不能当挡箭牌）→ 红
- 阴性对照（只改注释）→ 两侧都绿

> ⚠️ 记一次**我自己的投毒失误**：第一次投「豁免名单」那一毒时，我改的是**断言本身**
> （换成一句恒真的 `"POISON" not in source`），测试当然绿。**那证明不了任何事** ——
> 投毒必须改代码，不是改测试。重做后才红。

---

#### 🔴 A2 管理员重置密码后，被重置的人登不进来（**已派 agent**）

`admin_set_user_password(Permanent=False)` 把用户置为 `FORCE_CHANGE_PASSWORD`
（AWS 文档行为，审计标为**推断**），之后 `initiate_auth` 返回 challenge 而非 `AuthenticationResult`，而
`auth.py:392` 是**无条件**取 `resp["AuthenticationResult"]["AccessToken"]`，
且全仓 `grep "NEW_PASSWORD_REQUIRED|ChallengeName|respond_to_auth"` → **0 命中**（主会话复核过）。

实测：login → **503**「Public authentication is unavailable」；forgot-password → **410**。
**用户拿着管理员给的临时密码，登录只会拿到一个不透明的 503，而自助找回已被关死。**

审计的投毒佐证最说明问题：把 `Permanent=False` 改成 `True` → 测试**变红**。
⇒ **现有测试钉的是「传了什么参数」，完全没有钉「传了之后用户还能不能登录」。**

**主会话已定走方案 (b)**（`Permanent=True` + 自有 `must_change_password` 状态位），不走 (a)（补完 challenge 链路）。理由：
1. 现成的自助改密流程要的正是「旧密码 + 邮箱验证码」，而被重置的用户**手里就有旧密码**（那个临时密码），一行不用改就能接上；
2. (a) 需要新开一条公开端点 —— **而 A1 刚证明这个仓在「新开公开路由」这件事上会漏**；
3. Cognito 的 challenge session 在这个仓里没有任何先例。

#### 🔴 A3 新建的亲子关联对鉴权层完全不可见（**已派 agent**）

两套 key space **零交集**（主会话核实）：

```
鉴权事实唯一来源 authorization.py:495-520 → user_repo  → PK=USER#{parent_id}   SK=CHILD#{student_id}
002-F 的新表 parent_link_repo.py:64-69               → PK=PARENT#{parent_id} SK=CHILD#{student_id}
```

审计数出带 `parent_oversight` purpose 或 `parent_binding` resource_type 的路由共 **42 条**。
**主会话补充一个审计没说清的细节**：002-F 在 `parents.py:769` 的 `_ParentLinkFactRepository`
盖住了 `/parents/me/children/{id}/*` 那 6 条，其余全漏 ——
所以准确说法是「**一个局部补丁盖住了一个 router，其余全截**」，而非实现方自述的「3 个端点 404」。

**最难受的一点**：`GET /admin/users` 的 `linkedAccounts` 读的是**新表**，
所以后台把一个完全无效的关联显示成 `status: active` —— **管理员看到绿的，家长点进去 404。**

#### B 级 7 条

- **B1 `/admin/users/parent-links` 的分类不是「完全一致」**，而是把亲子绑定的专用 capability
  换成了更宽的身份管理 capability。审计用单 capability actor 实测出对照表；
  inventory **自己生成的文件**就推翻了「分类完全一致」这个声明（四个字段里三个不同）。
  更深一层：`classify_admin_route` 给 `/admin/users` 前缀的 `target_keys` 是 `("user_id",)`，
  而 parent-links 的 `parent_id`/`student_id` 在 body 里，`_target()` 只读 query/path params，
  取不到 ⇒ **鉴权决策与审计行里记的资源是 `"global"`，不是被绑定的那对父子**。
  🔴 **最关键的一组读数**：把它**收紧**回 `parent_binding_repairer` → 红；
  把 `/admin/users` 写操作**放宽**成 `student_support_lookup` → **红的是同样那两条**。
  ⇒ **现有测试只能发现「和 JSON 文件不一样」，分不清收紧和放宽。**
  当初若直接把 inventory 生成成 `parent_binding_repairer`，同样全绿。
- **B2 `GET /admin/users` 把 PROFILE 行原样吐出**，无字段白名单。实测 `stripe_customer_id` /
  `cognito_sub` / `password_change_code_hash` 原样出现在响应体里。
  该路由 capability 是 `student_support_lookup`（**客服档，比 identity_manager 低一档**），
  却能读整行 ⇒ **任何后续卡往 PROFILE 行加字段都会自动发布给客服档**。前端只渲染 7 个字段，
  所以这是**接口层泄漏，看页面看不出来**。（已派 agent）
- **B3 limit/cursor 从未被真实验证**：替身 `scan()` 忽略 `Limit`/`ExclusiveStartKey`、
  从不返回 `LastEvaluatedKey`，实测 `limit=2` 返回 `count=10`、`nextCursor=null`。
  **分页正确性在现有夹具下无法证伪。**（已派 agent）
- **B4 认领时弱密码烧掉令牌** —— **已由 002-C 收尾修掉**（`enforce_password_complexity` 已用在
  `InvitationClaimRequest` 上，主会话核实 `auth.py:1011`）。两份独立审计各自独立发现了同一条。
- **B5 后台「重发邀请」按钮必然失败**：`AdminAccountsPage.tsx:99` 传的是 `row.userId`（账号 id），
  而后端查的是 `ACCOUNT_INVITATION_ID#{invitation_id}` 指针行，**永远不匹配** → 404。
  而 `GET /admin/users` 的响应里**根本没有 invitation id**。（已派 agent）
- **B6 同邮箱重复开户无存储层护栏** —— 与 002-C 审计的 C-4 是同一件事，两份审计独立发现。
  另立卡。
- **B7 管理员之间零互相保护**：实测任何持 `admin_identity_manager` 的管理员都能重置/停用
  另一个管理员，**包括把所有其他管理员停掉**。单人可执行的管理面 DoS。（已派 agent）

#### 🔴 限流分桶键：设计成立，但实现方给的理由是错的

这是主会话列的最高优先级，审计查得很实：
- mangum 读数：`api_gateway.py:150` `source_ip = request_context["http"]["sourceIp"]` → `:171 client = (source_ip, 0)`
- 全仓唯一的 `cloudfront.Distribution` 在 `frontend_stack.py:61`，`domain_names=["app.stoaedu.ch"]`，
  三条 behavior **全部是 s3_origin，没有任何 HttpOrigin**；`api_stack.py:487` 的注释自己写着
  「HTTP API v2 does not support direct WAF WebACL association…applied at the CloudFront layer in FrontendStack」

⇒ `request.client.host` 拿到的是**真实客户端 IP**，全局 DoS 那个 A 级场景**不成立**。
**但成立的原因不是实现方说的「网关观测到的地址天然可信」，而是「这条链路上压根没有反向代理」。**
这是**拓扑依赖的正确性**：哪天给 api.stoaedu.ch 挂上 CloudFront（`api_stack.py` 的 WAF 注释已在暗示这条路），
所有用户立刻塌进极少数几个桶，**而没有任何测试会因此变红**。

⚠️ **审计明说的限制**：`api.stoaedu.ch` 这个域名**在 CDK 里根本不存在**（全仓只有 `app.stoaedu.ch`）。
所以它只能证明到「CDK 管的那部分链路上没有 CloudFront」。**若有人在 IaC 之外手工挂了 CloudFront
或 edge-optimized 自定义域名，结论立刻反转** —— 这条只能由能看 AWS 控制台的人确认。

配套两条也核实了：**fail-closed 属实**（计数器写不进去 → 429 而非放行）；
`SET … ADD` 顺序在替身里**确实有断言**（不是宽松替身）。
⚠️ 但审计指出：那个 AssertionError 会被 `admit_invitation_claim` 的 `except Exception` 吞成 429 ——
**表达式写错的生产表现是「所有人都被限流」，而不是报错。**

#### 审计留痕：通过（计数 + 内容都查了）

dump 整张假表（7630 字节）比对三个刚铸出的秘密：`temporaryPassword` / `activationToken` /
`initialPassword` **present 全部 False**。审计行 6 条逐条打开看过内容，`evidence_reference`
存的是 invitation_id **不是 token**。`project_audit_event` 是**正向构造**不是脱敏
（只从 `SAFE_AUDIT_FIELDS` 取，非 `str/int/bool` 直接 ValueError）。
⚠️ 但白名单里有 `reason_code` 和 `evidence_reference` 两个**自由文本口子** ——
当前调用点不含秘密是**调用点的性质，不是白名单的保证**。

#### C 级 7 条

C1 过期令牌错误码有意设计（未知/已用二者实测**逐字节相同**）；
C2 按编号发起关联是存在性预言机，但有 **5 次/24h** 上限（fail-closed、按 requester 分桶、
在业务调用之前计数），穷举 9999 个编号需约 **2000 账号·天**，实现方那条频次上限**属实且位置正确**；
**C3 激活令牌进 URL query 且读完不 `replaceState`**（全仓 0 处），`index.html` 与 `vercel.json`
都没有 `Referrer-Policy` —— 好消息是**埋点不会泄漏它**（`analyticsClient.ts:151` 只发
`window.location.pathname`，从不带 `search`/`href`），真实风险只剩浏览器历史与用户转贴；
C4 后台把令牌/临时密码渲染成普通 `<p>`、无自动清除（纯 useState 不落存储，
且**只在投递失败时才显示邀请令牌**，这点做对了）；C5 审计行两套形状；
C6 `RoleRoute.tsx` 有未翻译硬串 `Loading account...`；
**C7 `insights_version` 让 `cdk synth` 直接抛异常** —— 见下。

#### 🔴 C7 主会话已修：`cdk synth` 原本根本跑不起来

审计说它「挡在 A1 必须做的 infra 改动前面」，主会话核实后发现**比这更严重**：
`stacks/api_stack.py:69` 的 `insights_version=VERSION_1_0_317_0`（BUG-008 登录延迟调查留下的
**未提交**改动）在 eu-central-2 不受支持，**4 条 infra 测试一直红着，`cdk deploy` 根本跑不起来**。

主会话穷举了 CDK 认识的全部 **17 个** Insights 版本，分 x86_64 / arm64 两组实测：

| 架构 | 可用版本 |
| --- | --- |
| x86_64 | 178 / 229 / 273 / 275 / 295 / **317** / 333 / 391 / 404 / 498（10 个） |
| **arm64（本项目）** | **只有 `VERSION_1_0_498_0` 一个** |

`317` 在 x86_64 下是好的 —— 所以裸测会通过，**只有在 arm64 上才炸**。改成 `498` 后
infra 测试 **4 红 → 24 passed → 27 passed**（含新增 3 条）。

#### 审计自己的两次还原事故（它主动报告了）

1. 一轮投毒被 600s 超时打进后台并被杀，`finally` 没跑完 → `generate_initial_password` 的
   `secrets.choice(alphabet)` 一度留成 `alphabet[0]`
2. 另一次 restore 用了 `replace(new, old, 1)`，把 `_consume_invitation` 的两个错误分支换了位置

**它自己定位并逐字修回，最终 SHA 自证**。写在这里是因为「**投毒还原失败且没人发现**」
正是这套流程要防的失败形态。

#### 审计明说没验成的

任何真实 AWS 调用；**`api.stoaedu.ch` 的真实解析路径**（限流结论依赖它）；
A3 那 42 条路由具体哪几条会失效（没逐条跑）；
「rejected 是终态、管理员也无法覆写」—— **它的夹具没写 `ACCOUNT_NUMBER#` 认领行，
`studentNumber` 解析不出来，第一步就 404，场景构造失败**，结论只来自代码读数；
前端 29 个 e2e spec 与 `npm run build`。

### 002-D 管理后台与端点接线 ✅（待审计）

**16 条新路由**。管理后台全部落在 `/admin/users*`：签发邀请 / 直接建号 / 重发 / 撤销 /
**重置密码** / 改状态 / 改资料 / 列表 / 建亲子关联。公开的 `POST /auth/invitations/claim`。
家长侧与学生侧各 4 条自助关联请求端点（发起 / 列表 / 确认 / 拒绝），
自助发起**以编号为准**（`studentNumber` / `parentNumber`），不接受 user_id。

002-F 留的窄注入点已接上：`routers/parents.py` 模块级
`set_account_number_resolver(account_numbering_service.resolve_account_id)`，
有测试守着不退化成拿用户号当编号猜。**没放进 `main.py`**（该文件禁改）。

**路由授权清单主会话已独立核实**：`git diff --numstat` → **293 insertions, 0 deletions**，
210 → **227** 条（16 条本卡 + 1 条 analytics），既有行零漂移属实，用的是官方脚本
`scripts/generate_route_authorization_inventory.py` 重生成。

**主会话另核实**：`ruff check .` → All checks passed；
`test_account_admin_endpoints.py` + `test_admin_authorization.py` → **178 passed**；
27 条 403 参数化确实存在（9 条命令 × student/parent/teacher），
且配了阴性对照 `test_管理员本人不会被这道判据挡住`——证明判据不是「全都 403」。

**它自己报的投毒 9 次**，其中前端 B「给归档账号也放出重新激活」**第一轮绿 = 假绿**，
查下去是 fixture 里根本没有 archived 账号，补数据后才红。**它自己发现并纠正了**。

**一处刻意的路径偏离，已交审计核实**：`POST /admin/users/parent-links` 而非设计里的
`/admin/parent-links`，理由是后者要改 `security/admin_authorization.py` 的 `classify_admin_route()`
（全局单点、不在其边界内），挂在 `/admin/users` 前缀下由既有规则直接命中同样的
`admin_identity_manager` + `MANAGE_PRIVILEGE`。⚠️ **「为了不动全局单点而把路由塞进更宽的前缀」
是典型的把闸放松换方便**，已要求审计逐字段核对分类是否真的一致、有没有哪一项变宽。

**限流的分桶键已列为审计最高优先级**：它按 `request.client.host` 的 sha256 分桶，
明确不用 `X-Forwarded-For`（理由是边缘可伪造）。**这个理由在直连服务器时成立，但这套链路是
CloudFront → API Gateway HTTP API → Lambda(Mangum)** —— 若 Mangum 拿到的是边缘节点 IP 而非
真实客户端 IP，15 分钟 10 次就会变成**全局 DoS 开关**：一个人刷满，全世界都认领不了账号。

**越界 1 处，主会话已准**：新建 `src/pages/auth/ActivateAccountPage.tsx`（不在给它的前端清单里）。
不建的话邀请信里的 `/activate?token=` 是死链、整卡主流程不通。新文件、无重叠。
它的四语文案放在 **`admin.json` 的 `activation.*`** 下（当时 `auth.json` 封给 002-A+E）——
主会话已确认 `src/i18n/index.ts` 是**全量静态导入、无懒加载无门控**，未认证用户照常拿得到，
**只是语义放错，不是功能或安全问题**。列为 C 级债，后续搬回 `auth.json`。

**它明说没验证的**：SES 邀请信**一次真发都没做过**（红线禁真实 AWS 写，只有替身断言
`send_email` 调用恰好 1 次、正文含 `/activate?token=` 且 token 与返回值一致）；
前端 e2e 29 个 spec 没跑、页面没在真实浏览器里打开过；
`test_lambda_dist_build.py` 那 8 条没在 Python 3.12 下复验。

**它自报的已知限制**（已转审计判定严重性，不照单全收）：
1. 🔴 **半截可见性**：`/adaptive/parents/me/children/{id}/progress` 与
   `/students/{id}/{learning-profile,questions,summary}` **仍不认新链接表**，
   通过新表建立关联的家长点进去会 **404**。按主会话「不得自行扩面」的要求原样留着。
   **这意味着新建的亲子关联目前只有一部分页面能用**，审计需判 A 还是 B。
2. `rejected` 终态无管理员覆写路径、pending 行永久占位，只加了频次上限（24h/5 次）压骚扰面。
3. `GET /admin/users` 解析绑定按行查，页上限 200 行时读放大明显（仅管理员可调、有界）。

**在哪能看到**：管理员导航 **Accounts**（`/admin/users`，原 placeholder 已换成真页面）；
激活页 `/activate?token=...`。

### 002-A + 002-E 独立审计意见书与处置 ✅

结论：**有条件放行**。3 条 A 级、5 条 B 级、7 条 C 级。

审计**穷举了全部建号入口**逐个判定，结论：自助注册确实关死了（410 在闸门常量处直接抛，
`provider.calls == []` 有测试守；Cognito 侧 `AllowAdminCreateUserOnly: True` 是第二道闸）。
教师邀请激活 `POST /teacher-applications/activation/claim` 是**第二条建号路径**，
但它要一个只能由挂了审核人依赖的 `review_application` / `reissue_invitation` 铸造的单次令牌，
实质就是邀请制，**不是绕过**。002-C 的 `account_provisioning_service` 目前**零 router 接线**，外部碰不到。

7 次投毒 6 次变红，SHA 自证还原干净。**唯一的假绿是 PBKDF2 轮数**，见 B3。

**A1 后端 ruff 门禁被本卡弄红（主会话已清）。** 实现方删掉了
`test_new_public_account_profiles_and_provider_attributes_start_free_trial`，
那四个 import 没跟着删 → `tests/test_plan_identity_contract.py` 4 条 F401。
审计用 `git show HEAD:` 取同名文件跑同一份配置对照，确认是本卡引入而非既存。
**后端门禁口径是 `ruff check . && pytest -q`，push 即部署**，这条会直接把 verify job 打红。
主会话已删掉那 4 行 → ruff 全过、该文件 14 passed。
⚠️ 实现方的自检报告**只谈了 pytest、完全没提 ruff**——后续每张卡的自检必须带 ruff 读数。

**A2 `account_recovery=NONE` 已就绪，但「找管理员重置」在产品里不存在 → 已转 002-D。**
`grep -in "password" src/stoa/routers/admin.py` **零命中**；
`admin_set_user_password` 全仓只出现在 `teacher_identity_provider.py:66,76`（教师激活内部用）。
CDK synth 读数 `AccountRecoverySetting: {'RecoveryMechanisms': [{'Name': 'admin_only'}]}` —— 自助恢复已拿掉，
而后台没有任何端点能替别人重置。**这本来就是需求原文「改密……管理员也可以操作」的一部分，是漏项不是新增**，
已 SendMessage 转 002-D 实现（要求进路由授权清单、留审计痕、与自助改密共用密码策略常量、投毒验证）。

> 🔴 **由此产生一条上线次序硬约束**：`stoa-infra` 把 `account_recovery` 改成 `NONE` 的那次
> `cdk deploy`，**必须排在 002-D 的管理员重置端点上线之后**。后端 410 与前端页面可以先上
> （它们只是关掉入口，不销毁恢复能力）。审计对「只有一个 admin 忘了密码算不算锁死」的判断：
> **不是彻底锁死，但对非技术管理员等同锁死**——AWS 控制台的
> `aws cognito-idp admin-set-user-password` 是另一套凭据，通道客观存在，但不在产品内，
> 且本工作区 SSO（profile `stoa`）至今没配好，这条唯一的逃生通道自己就处在「配置不完整、
> 容易打错账号」的状态。

**A3 `/settings/password` 没有任何入口 → 已派收尾 agent。**
全前端没有一个 `<Link to="/settings/password">`，`navItems` 里也没条目，
`routeConfig.ts:326` 是 `navPriority: 'hidden'`。登录页的「忘记密码」已改成纯文本（这是对的）。
配合 A2 等于**用户改密码这件事目前没有任何可走的路**。

**B1 SES 发信失败时配额已扣、旧验证码已被覆盖、异常直接 500 → 已派收尾 agent。**
`issue()` 的顺序是 `admit_send()` → `store_code()` → `send_code_email()`，
路由层只捕获 `PasswordChangeCodeRateLimited`，SES 的 `ClientError` 一路冒成 500。
审计用假 SES 实测：`QUOTA ROW WRITTEN: True / CODE ROW WRITTEN: True`。
后果是 SES 一挂，用户点 5 次全拿 500、配额烧光，且每点一次还覆盖掉上一个仍然有效的验证码。

**B2 验证码邮件只有硬编码德语 → 已派收尾 agent。** 产品是 de/en/fr/it 四语，
`locale_service` 就在 `auth.py` 的 import 里。按本项目「内容型文案的语言混杂也算翻译 bug」的既定口径算 bug。

**B3 PBKDF2 成本无人守（主会话已补断言）。** 审计把 `_DIGEST_ITERATIONS` 从 100_000 改成 **1**，
`test_password_change_code.py` **16 passed 全绿**——现有断言只看「存的不是明文」「能比对上」，没有一条看成本。
主会话补 `test_摘要成本不会被悄悄调低`，投毒 → 红（耗时也从 0.56s 掉到 0.19s，印证成本是真的）。
顺带量到单次 pbkdf2 100k = **7.0 ms**，不会让改密接口超时。

**B4 部署次序：backend → frontend（硬约束）。** 前端 CI 的 `check:api-contract` 是 `curl` 拉
**backend main** 的 `route-authorization-inventory.json` 比对，本地跑用的是本地副本所以本地绿。
前端若先推，CI 拉到还没有 `/auth/password-change/*` 的清单 → 必红。

**B5 路由清单的替代判据只覆盖原测试的一半（记录，不拦）。**
原测试两段断言：①运行时投影 == checked JSON（已覆盖，210 条逐条一致，审计独立复现过）；
②每条路由的 OpenAPI `x-stoa-authorization` 扩展与投影一致（**当时验不了**，因为 `app.openapi()`
被 analytics 卡住）。审计判「不等价但没掩盖本卡引入的漂移」，成立。**analytics 现已修好（见下），这 5 条要复跑。**

**C 级 7 条**进优化清单：单码 5 次尝试的计数会被「重新请求验证码」重置（实际预算 25 次/小时，
对 10⁶ 空间无威胁，非漏洞但要写进注释）、`confirm` 先消费验证码再校验旧密码（旧密码打错一次码就废）、
`test_plan_identity_contract` 的断言从 `==` 放松成 `<=` 丢掉了「至少写一次」那半边、
死代码残留（`RegisterForm.tsx` 已无人 import、`routeGroups.ts` 全项目无人 import 却仍列 `/forgot-password`、
`httpClient.ts` 的 `PUBLIC_AUTH_PATHS` 仍列两个已 410 的路径——**已确认 `/auth/password-change/*` 不在该集合里，会正常带 Authorization 头，无害**）、
`POST /auth/register` 仍声明 `identifiers: ["parent_id"]` 该清掉、
`test_registration_and_admin_repair_have_one_logical_relationship_writer` 名实不符、
`/auth/password-change/request` 对已登录会话构成旧密码探测预言机（需先持有有效 token，Cognito 自身有锁定，风险低）。

**审计明说没验成的**：`cdk deploy` 把已存在用户池的 `account_recovery` 从 `EMAIL_ONLY` 改成 `NONE`
对**在途重置流程**的实际行为（无 AWS 凭据，只能从模板层确认目标状态）；
Cognito 在 `AllowAdminCreateUserOnly=true` 后对 `SignUp` 的真实错误码；
**SES 真实发信——至今一次真发都没做过**，而 B1 的风险正建立在「SES 可能发不出去」上；
`cdk diff`；前端 e2e 29 个 spec 与 `npm run build`（本来就不在 verify job 里）。

**主会话对审计的一处更正**：报告称 `account_deletion_repo.py` 出现过
`SyntaxError: closing parenthesis ')' ... line 233`、判断是 002-D 写到一半——**不是**，
那是主会话处置 002-F 的 A2 时投毒删错了括号范围，几秒内已还原。
它据此得出的「并发期间全量读数不可信」结论仍然成立，但成因不是 002-D。

### 🔴 既存阻断：analytics 路由炸掉 8 条测试（主会话已修）

`src/stoa/routers/analytics.py` 是 09-17 第三方加的（BUG-008 登录延迟调查），**从未提交**，
`main.py` 里注册它的 2 行也没提交 —— 所以 main 上 CI 一直是绿的，
**但这批 002 一 push，那 8 条就会红、deploy job 被拦。**

病因：`@explicit_route_classification("public", ...)` 没给 `allowed_identifiers`，
而扫描器从 body 观察到 `sessionId`，于是 `inventory_application()` 直接抛
`ValueError: POST /analytics/events: explicit identifier declaration must exactly match observed identifiers`。
这一条 ValueError 连坐 8 条测试（`test_route_authorization_inventory.py` 5 +
`test_practice_privacy.py` / `test_phase473_practice_authorization.py` / `test_billing_recheck_apis.py` 各 1），
因为它们都要调 `app.openapi()` 或做全量清单投影。

修法（主会话自己改，路由表属全局单点）：
```python
allowed_identifiers=("sessionId",),
identifier_scope="command-local",
```
`sessionId` 是浏览器生成的、不是任何人的账号 id；`command-local` 是本仓对公开路由带 identifier 的既定要求
（`auth.py:348`、`teacher_applications.py:80,104` 同款）。
读数：`validate_application_inventory` → `failures: none`，那 4 个文件 **8 failed → 1 failed**。
剩的 1 条是 checked JSON 里缺 `/analytics/events` 条目，已转 002-D 在重新生成清单时带上。

**顺带记一笔待办（不在 002 范围）**：`/analytics/events` 是**公开未认证即可往 DynamoDB 写行**的端点，
`sessionId` 由客户端任意指定，没有任何频次限制。09-17 就有的设计，需另立卡评估。

### 002-F 家长-学生多对多关联 ✅（服务层与授权层，端点未加）

**查实：旧机制在数据模型层就不支持多对多。** `user_repo.build_parent_binding_transaction()` 写的学生
profile 投影上只有**一个** `parent_id` 字段，条件是
`(attribute_not_exists(#parent_id) OR #parent_id = :parent_id)` ——
第二个家长绑同一个学生会被这条 ConditionExpression 直接拒掉，**父母两人各一个账号在旧模型下根本写不进去**。

**处置：新表是新关系的唯一写入口，旧表只读、不再新增，可见性取两者并集（OR）。**
没有拆旧机制，理由是 `parent_binding_status` 被 8 个模块读（`entitlement_service`、
`paid_entitlement_service`、`teacher_support_allowance_service`、`students.py`、`account_deletion_repo` 等），
**计费与教师支持额度都挂在它上面**。退场需要另立迁移卡。

**越权防护放在授权层而不是 handler 里**：`parents.py` 加了 `_ParentLinkFactRepository` 包住注入的
fact repository，接进现有授权决策链。选这一层的理由——`_parent_child_read` 是
`/parents/me/children/{id}/*` **全部 6 个读端点的唯一咽喉**，改一处全覆盖不漏调用路径；
且审计留痕、correlation id、existence hiding 一条都没绕过（AST 扫描所有 router 的
`test_all_production_authorization_call_sites_use_recording_gateway` 依旧绿）。

行为细节：**陌生学生返回 404 而非 403**（仓里既有的 existence hiding，防止家长拿接口枚举学号），
**pending 返回 403**。两种都有测试断言。

原子性走 `TransactWriteItems`（复用 `account_deletion_repo.transact()`），
两条 Put 条件都是「不存在才写」；状态迁移前一致读两行，两边都必须处在 `expected_status`，
否则抛 `ParentLinkConflict`，**不做单边收敛**。

读数：`tests/test_parent_student_links.py` **12 passed**（审计处置后 **18 passed**），ruff 全过。
投毒三个全红过并有 SHA 还原证明：①去掉 `status==active` 过滤 → 6 failed，越权判据 `assert 200 == 403`；
②自助发起直接 active → 8 failed；③一次事务换成两次顺序 put → 1 failed，第一条真的残留了。

**它发现并转告了一条不在既存名单里的失败**：全量 `3042 passed, 9 failed`，
第 9 条 `test_phase475_parent_binding_transaction.py::test_registration_and_admin_repair_have_one_logical_relationship_writer`
是**另一个 agent 改 `auth.py` 删掉 `put_parent_student_relationship` 造成的**，已转告该 agent 处理，
并叮嘱不许用删断言或 skip 的方式让它变绿——那条测试守的是「亲子关系只有一个逻辑写入口」这个性质。

**主会话补做**：002-B 加 `resolve_account_id(account_number) -> str | None`（反查持有者，
家长按学号关联孩子的入口），F 留的窄注入点接上即可。补了 2 条测试，投毒（改成瞎猜）验证会红，还原后 12 passed。

**F 明说的缺口**（边界外，需后续处理）：计费与额度仍只认旧 `parent_binding_status`，
**通过新表建立的关联目前不会让学生进入家长的付费额度池**；无并发测试；
自助发起时「编号不存在」与「编号存在但角色不对」返回同一错误码但**耗时不同**（未做常数时间）；
真实 DynamoDB 的 `TransactWriteItems` 条件语义未实测（用内存假表模拟）。

### 002-F 独立审计意见书与处置 ✅

结论：**有条件放行**。2 条 A 级、3 条 B 级、5 条 C 级。A、B 两级**主会话已全部处置并逐条投毒验证**。

**A1 链接行会污染 `GSI-StudentId`（已修）。** `database_stack.py:43-47` 那个 GSI 的键是
`(student_id, created_at)`，而链接行两个字段都带 —— 于是它会被投影进「列某学生的题目」这个索引，
下游 `students._question_rows` 不做二次过滤，原样吐出的行里带着**对方的 user_id**。
修法：`parent_link_repo.py` 里 `created_at`/`updated_at` 全局改名 `linked_at`/`link_updated_at`，
service 层三处同步改，字段表上方写明为什么不能叫 `created_at`。
补 `test_链接行不带_GSI_StudentId_的排序键`，投毒（改回 `created_at`）→ 13 条全红。

**A2 注销扫描漏判正向行（已修）。** 正向行 `PK=PARENT#<家长>/SK=CHILD#<学生>`，
学生靠 `student_id` 和 SK 都能找到，**家长两样都找不到** —— 后果是这一行活过它自己家长的注销，
反向行却被墓碑，留下的正是这套配对机制要防的半截链接。
修法：`account_deletion_repo.py` 的 `CROSS_ACCOUNT_IDENTITY_REFERENCE_REGISTRY` 加
`"parent_student_link": (frozenset({"parent_id"}), frozenset(), frozenset())`。

⚠️ **没有照抄审计的建议修法。** 审计原话是加 `{"parent_id", "created_by", "updated_by"}` 三个字段，
但 `created_by` 往往是管理员 —— 照抄会让注销一个管理员顺手扫走他**仅仅签发过**的所有亲子链接。
只收 `parent_id`。补两条测试：正/反两行 × 删家长/删学生四种组合全为 True，
**外加阴性对照**「删管理员不命中」。投毒（改掉 registry 键名）→ 正例红、阴性对照保持绿。

**B1 `active_link` 对半截链接的行为无测试（已补）。** 两行虽靠事务一起写，读侧不能因此假定它们总成对存在
——半截行可以由手工写库、注销只墓碑一侧、将来某条新写路径漏行留下，而放行只需要一次误判。
补 `test_只有单侧行时可见性不成立`，删掉反向行后断言 `active_link()` 为 None **且路由非 200**。
投毒（`active_link` 不查反向行）→ 红。

**B2 预读与事务提交之间的窗口无测试（已补）。** `transition_link` 先一致读两行再条件写，
中间那段只能靠条件写兜住。补 `test_预读之后状态被改掉则拒绝迁移`，在假表 transact 上挂 hook，
预读之后把两行状态改成 rejected，断言抛 `ParentLinkConflict`。

> 🔴 **这条投毒第一次没红，是假绿，而病因在假表不在产品代码。**
> `FakeLinkTable` 只按子串 `"attribute_exists(PK)" in condition` 判断，
> 把 `AND #status = :expected_status` 整段摘掉后子串照样命中，于是它照旧做状态比对 ——
> **假表分不出条件写里有没有状态校验**，这条测试因此证明不了它声称的东西。
> 修了假表（条件里出现 `#status = :expected_status` 才比对状态），重投毒 → 红。
> 对应十条判据第 7 条：校验一张表要用消费它的那个机制，宽出来的部分正是死条目藏身处。

**B3 `actor_id == created_by` 这道闸（保留 + 已补测试）。** 审计指它在正常流程下永不触发，
要求「补测试或删掉并说明」。**判为保留**：`_confirmer_side` 完全信任 `initiator_role`，
一旦那个字段被写反，第一条闸会把发起方认成应确认方，**只剩这一条拦得住自确认**。
补 `test_发起方自己不能确认哪怕_initiator_role_被写反`，直接构造矛盾的 pending 行触发第二个条件。
投毒（摘掉 `or actor_id == link.get("created_by")`）→ 红。

**B4 骚扰路径 —— 需求方决策，未改代码。**
`rejected` 是终态、`pending` 永久占位，两者合起来是一条骚扰路径：
A 向 B 发关联请求，B 拒绝后这对组合永远停在 `rejected`；而若 B 不理会，
`pending` 行会永久占着那个 `(parent, student)` 键位，**双方都无法再发起**。
- 不答复会阻塞什么：不阻塞当前开发，但上线后用户点错一次就再也建不了这条关联，只能找管理员。
- 我方倾向：给 `rejected` 加冷却期后可重发（如 7 天），`pending` 加 TTL 自动过期（如 14 天）。
  理由是两个终态都不可逆这件事，用户侧没有任何自救动作。
- 现状：**代码按「终态不可逆」实现**，改动另立卡。

**C 级 5 条不拦合并**，进优化项：常数时间比较、并发测试、
计费额度仍只认旧 `parent_binding_status`、`FakeLinkTable` 与真实 DynamoDB 条件语义的保真度、
错误码粒度。

**已通过 SendMessage 拦住正在跑的 002-D 四条**：①A1 修好前不要接创建链接的端点；
②吊销旧绑定不再等于吊销可见性，`admin.py:2394` 必须同时处理新表；
③「唯一咽喉」只对 `/parents` 成立，**不得自行扩面去修别处的 404**；
④`request_link` 必须挂频次限制。

### 002-B 编号体系 ✅

落点：`services/account_numbering_service.py`、`db/repositories/account_number_repo.py`、
`tests/test_account_numbering.py`（新增三个文件，未接线）。

算法是「**游标给起点，条件写定胜负**」：游标只决定从哪个序号开始探，
唯一的正确性来源是占号那一步的 `ConditionExpression="attribute_not_exists(PK) AND attribute_not_exists(SK)"`。
游标读脏、丢失、落后都只多几次重试，不会发重号——有测试把游标 item 直接删掉，
验证下一个号仍是 `A26-0004` 而非回到 `A26-0001`。**没有用「查最大值 +1」。**

两种 item：占号行 `PK=ACCOUNT_NUMBER#S26-0001 / SK=ACCOUNT_NUMBER`（append-only，仓储层
根本没有 delete 接口）；游标行 `PK=ACCOUNT_NUMBER_SEQUENCE#student#2026`（仅提示，非真源）。
编号不是 account 行主键，account 行主键仍是 Cognito sub。

**投毒第一轮抓到了它自己的假绿测试**（这是本卡最有价值的部分）：
把条件写改成无条件写后，并发测试**没有变红**——50 个线程虽同时起跑，但 fake table 的读在锁内、
读完立刻返回，线程被 GIL 串行化，**从来没有两个线程同时握着同一个游标值**。
按要求重写测试：把同步屏障移到「读完游标、返回之前」，强制 50 个线程全部拿到同一个游标值后才放行。
重跑投毒 → 4 条红，`assert 4 == 50`。条件写确实是唯一挡住重号的闸。

投毒 2（把 `parent:"A"` / `admin:"P"` 对调）印证了内容型判据的必要性：
**纯计数的并发测试依然全绿**（50 个号仍互不重复），只有内容型判据变红。
角色配错在计数断言里完全看不出来。

读数：`ruff` 全过，`pytest tests/test_account_numbering.py -q` **10 passed**；
另跑四个扫源码目录的清单类测试 **112 passed**，新增文件没触雷。

**未验证**：真实 DynamoDB 上的行为（本地用带锁的 fake table，按单 item 原子 CAS 语义实现，
但它不是 DynamoDB）；按红线要求未执行任何真实 AWS 命令。

**留给接线卡的问题**：同一个 `account_id` 重复调用 `allocate_account_number` 会拿到**两个号**——
本卡范围内没有「一个账号只能有一个编号」的约束。
已作为硬要求写进 002-C 的 prompt：接线时在写 account 行的同一事务里用
`attribute_not_exists(account_number)` 兜住，并用测试守住。


## 001 修复 2026-09-16 两轮黑盒测试反馈的全部问题

- 状态：已完成 ｜ 日期：2026-09-17 起，2026-09-18 完成
- 提出：「看一下测试反馈文件夹里的新反馈文档，确认问题修改方案，开始并行修复所有问题」

**要做什么**：修复 `stoa-docs/测试反馈/` 三份文档中的全部 19 个问题。
来源：`STOA_功能测试错误清单 (1)(1).md`（BUG-01~16 + 2 条附注，是另一份清单的超集）与
`网站问题反馈文档(1).md`（注册流程 3 个问题）。

**前置事实**（已核实）：

- 上一轮 i18n 修复**已提交但未推送**（前端 `c490f09`、后端 `c893c2b`），生产环境尚未生效。
  BUG-05 中「历史记录标签 Question asked」「Profile 副标题」「家长账号说明」等已在该提交中修完，
  本卡只处理其中尚未覆盖的部分。
- BUG-11 根因：`src/i18n/locales/de/legal.json` 整个文件 3163 字符**零变音字符**，源文件即 ASCII 转写，
  非渲染问题。
- BUG-05 plan/status 根因：`src/lib/displayLabels.ts` 的 `getSubscriptionPlanLabel(plan, t?)` 中 `t` 可选，
  三处调用点未传 `t`，回落到硬编码英文。
- BUG-04 根因：`MathRenderer` 只解析 LaTeX `$...$`，text 段原样输出，Markdown `**粗体**` 从未被处理。
- 注册问题 2 根因：`RegisterForm.tsx:140` 对 student 角色**无条件**要求 parentName/parentEmail，不看 age。
- 后端工作区有他人未提交的 `analytics.py` 改动混在 `src/stoa/main.py` 里，任何人不得提交该文件。

**计划**（六组并行，文件集互不重叠）

- [x] A. 法律文本德语变音字符：重写 `de/legal.json` 的 ä/ö/ü/ß，核查 fr/it 重音符号（BUG-11）
- [x] B. 注册流程：age 校验、成年人免填家长信息、422 前后端对齐、逐字段错误提示、分步标题、链接样式
      （注册问题 1/2/3 + BUG-14/15/16）
- [x] B2. **422 响应体回显明文密码**（B 组发现的既有安全缺陷，因 B 组改动而暴露面变大，主会话自己修）
- [x] C. 聊天与渲染：空输入拦截、Markdown 加粗渲染、AI 回复语言一致、学科名大小写、学科选中反馈
      （BUG-02/03/04/06/09）
- [x] D. 剩余语言混杂：plan/status 标签、High/Low 优先级、后端课程数据变音字符（BUG-05 未覆盖部分）
- [x] E. 帮助中心与引导页：德语翻译、内部运营口吻文案、Kontakt 空 mailto（BUG-12/13）
- [x] F. 交互细节：角色切换登出、通知面板外部点击、悬浮徽章遮挡、忘记密码入口、页脚重复链接
      （BUG-01/07/08/10 + 附注 2）
- [x] G. **独立审计**（红线卡专用，与实现 agent 上下文不共享）：审 B / C / F 三组
  - [x] G-F. F 组审计完成 → **有条件放行**，1 条 A 级、3 条 B 级、9 条 C 级
  - [x] G-F 处置：A-1 补全忘记密码（已完成）；B-1/B-2/B-3 主会话自己修（已完成并投毒验证）
  - [x] G-B. B 组审计 → 不放行，A1 崩溃已修、A2 表述已纠正、B3 已补测试
  - [x] G-C. C 组审计 → 有条件放行，A1/B1/B4/B5 已处置
- [x] 汇总校验：前端 7 项门禁全绿、后端提交状态全绿，已提交并推送

**门禁口径**（照 `.github/workflows/deploy-production.yml` 的 verify job，push main 即触发，
逐项都得过，不是只跑 lint）：
`npm run lint` → `npm run typecheck` → `npm run check:api-contract`（拉后端 main 的
route-authorization-inventory.json）→ `npm run check:untranslated` → `npm test` →
`npm run test:release` → `node --test tests/release/publish-web-release.test.mjs`。
注意 **api-contract 门禁拉的是后端 main 分支**，所以后端必须先于前端推送。

**红线判定**（依据 skill `software-delivery-process`「审计与开发必须互不通气」）：
skill 明写主会话既派活又验收就是「自己检查自己」，失败形态是全绿，且在本工作区已漏过三次。
故以下三组完成后**另起独立审计 agent**（不共享实现 agent 的上下文）复核，主会话只做合并把关：

| 组 | 是否红线 | 理由 |
| --- | --- | --- |
| B 注册 | 是 | 动后端注册 schema 与鉴权路径 |
| C 聊天 | 是 | 动 AI 助手的语言防线 + 渲染层要防 XSS |
| F 交互 | 是 | 动 token、会话、401 拦截器 |
| A 法律文本 | 否 | 纯文案 |
| D 语言混杂 | 否 | 纯文案与展示层 |
| E 帮助中心 | 否 | 纯文案 |

**待确认**：

- BUG-01 的 `Testing as` 面板仅对 `@test.stoaedu.ch` 测试账号显示（`devSessions.ts` 的
  `TEST_ACCOUNT_PATTERN`），真实用户看不到。是修切换逻辑，还是在生产环境彻底隐藏？
  我方倾向：两者都做——修掉登出，同时确保生产构建不挂载。
- BUG-12 提到 `/support` 与 `/onboarding` 的试点运营口吻文案（「部分流程是简化实现」等）未登录可见。
  改写口径属产品决策，我方倾向先补德语翻译并弱化内部口吻，不删内容。
- BUG-10 忘记密码：后端已有 `/auth/forgot-password` 与 `/auth/reset-password` 路由，前端登录页缺入口，
  按补入口处理。

**记录**：

**A 组（BUG-11）已完成**：变音字符丢失是源文件问题，三个语言文件都有。
`de/legal.json` 改 35 处，`fr/legal.json` 重音被整篇剥光、改约 80 处（`Confidentialite`→`Confidentialité`、
`l'acces a`→`l'accès à` 等），`it/legal.json` 丢词尾重音、改 6 处（`puo`→`può`、`qualita`→`qualità`）。
ß 的取舍：不用瑞士 ss 写法，依据是全仓 de 既有惯例统一用 ß（`de/errors.json` 的 `Groß- und Kleinbuchstaben`、
`de/auth.json` 5 处 `abschließen`）。四语键结构核验一致（26 键）。

主会话补做了**全量扫描**（A 组只查了 legal.json）：按「变音字符数 vs 可疑 ASCII 转写词数」过一遍
de/fr/it 全部 13 个命名空间，命中项全是误报（Quelle、query、Aktuell、Kontoeinrichtung 等合法词与标识符）。
结论：**变音字符丢失只存在于 legal.json**，其余文件干净，无需再处理。
唯一异常是 `de/support.json` 仅 2 个变音字符——与 BUG-12「support 页没德语翻译」同因，归 E 组。

遗留（未处理，待判断）：`fr/legal.json` 的 `terms.title` 与 `terms.eyebrow` 都是 "Conditions"，
en 同样是 "Terms"/"Terms"，属原文重复，非本次 bug。

**E 组（BUG-12 / BUG-13）已完成**：`/support` 与 `/onboarding` 共 48 条硬编码英文接入 i18n，四语补齐
（德法意译文由 agent 自译，**未经母语者复核**）。9 处内部试点口吻的句子改写成面向用户的表述，
"pilot / pilot team" 措辞全部去掉，`Tutor` 统一为 `Teacher`，改前改后清单见本次会话记录，待产品复核。
BUG-13 选 mailto 带地址 + 预填主题，收件人用 `info@stoaedu.ch`（`brandContact.ts` 里唯一有据可查的邮箱，
**不是**测试文档举例的 `support@`，若该地址真实存在改一处即可），两个按钮都加了
`target="_blank" rel="noopener noreferrer"`，并把邮箱明文显示出来。
`/support`、`/onboarding` 公开可访问确认为有意设计（在 `ProtectedRoute` 之外、用 `MarketingLayout`），
未加登录墙——原问题只是公开页写了内部措辞，已改掉。

**新发现的技术债（本卡不处理，建议另开卡）**：`contact` i18n 命名空间**根本没在
`src/i18n/index.ts` 注册**，`src/pages/contact/`、`src/hooks/contact/`、`src/services/contact/`
三个目录全空，只剩孤儿组件 `src/components/contact/ContactSuccessMessage.tsx` 在用未注册的
`useTranslation('contact')`，路由表里也没有 `/contact`。站内联系表单只有残骸没有实现——
这也解释了主会话早先扫描到的 `contact:form.requestId`、`contact:form.success` 两个键缺失。
E 组因此判定不能把 Kontakt 按钮指向站内表单（会是死链），改用 mailto。

**边界导致的妥协**：`/onboarding` 的文案被放进 `support.json` 的 `onboarding.*` 子树，
因为主会话给 E 组划的边界只允许改 support.json。将来若要拆出独立的 `onboarding.json`，
需同步改 `src/i18n/namespaces.ts` 与 `src/i18n/index.ts`。

**B 组（注册 6 问）已完成**：422 根因查实并有复现证据——后端 `RegisterRequest` 是
`extra: "forbid"`，而前端固定多传 `acceptedTerms`/`termsVersion`/`acceptedAt`/`utm` 四个模型未声明的字段，
pydantic 直接 `extra_forbidden`。前端只显示状态码是因为 `httpClient` 把 `detail` 当字符串或 `{message}` 处理，
而 422 的 `detail` 是**数组**，两个分支都不命中，落到 axios 的 `"Request failed with status code 422"`。
修法：后端补声明这 5 个字段 + `normalize_registration_age()`；前端 `toUserFacingError` 解析 detail 数组。
Age 前端 `normalizeAgeInput` 去前导零/截 3 位、`isValidAge` 校验 1–120，后端 `normalize_registration_age` 同校验。
家长信息改为 `age < ADULT_AGE(18)` 才必填。逐字段红框校验、分步标题、链接样式一并做完。
新增唯一文件 `src/components/auth/FieldError.tsx`（10 行，避免 4 个 Step 组件重复同一段标记）。

B 组留下、**主会话判定要在本卡内处理**的一条：FastAPI 的 422 响应体 `detail[].input` 会把整个
请求体原样回显，**包含明文 password**。这是 FastAPI 默认行为、非本次引入，但 B 组新加的 age 校验
让注册路由触发 422 的概率明显变高，等于放大了这条既有缺陷的暴露面。
按 skill 第 10 条「评估一个修复要连出事时还看不看得见一起评」，不另开卡，见 B2。

B 组其余待确认：后端不强制要求 `age`（为兼容现有大量不传 age 的测试），绕过前端直接调接口可跳过家长校验；
成年学生的家长字段选择「标记选填」而非隐藏（后端 `_prepare_parent_student_relationship` 依赖 parentEmail
建亲子关系，成年学生仍可能想绑定）；18 岁阈值前后端各一个常量，跨仓无法共用，改动要两边都动；
学生资料步骤默认值仍是演示数据（age 14、`Kantonsschule Zürich Nord`、`Grade 8`），不在本次范围。

**D 组（BUG-05 剩余）已完成，并推翻了主会话给它的预设方向**：

「Brueche」**不是**数据错字，**线上 DynamoDB 不需要跑迁移**。证据：种子数据
`stoa-backend/scripts/seed_practice.py` 里德语标题全部带正确变音字符（`"Brüche"`、`"Mathematik"`、
`"Brüche verstehen und kürzen"`），全仓 grep `Brueche` 只命中一处 `tid = "brueche"`，即 **topic_id 的
ASCII slug**。真实链路是 slug 泄漏到界面：`adaptive_learning_service.py` 的 `_continuation_candidates()`
直接 `label=topic_id`，`useWeakTopicsQuery` 也有 `topic.label || topic.topicId.replace(...)` 的兜底，
两者把 `brueche` 原样送到 UI，再被 CSS `capitalize` 渲染成 "Brueche"。故后端零改动。

另一处更隐蔽的成因：`StudentProfilePage.tsx` 的 `getProfileBillingSnapshot()` 里
`statusLabel: entitlement.newUsageAllowed ? 'Active' : 'Paused'` 是**直接写死的英文字面量**，
根本没走 `displayLabels`——这才是 "STATUS → Active" 的真来源，`common:status.subscription.active`
这条键一直存在却无人使用。同类问题：`progress.weakTopicReason` 四语都有但全代码库零引用，
组件直接渲染了后端 `rec.rationale` 的英文 prose。

`displayLabels.ts` 的 `t` 参数已从可选**改为必填**，全库漏传的只有那 3 处，改完 tsc 通过，
此后编译器能挡住新的漏传。

D 组留下的技术债（建议另开卡）：前端现在维护两张对照表——英文 rationale 原文 → i18n key、
topic slug → 译名。更彻底的修法是后端在 adaptive 推荐响应里下发 `topicId` 与机器可读的 `rationaleKey`，
与上一轮给 learning-history 加 `kind`/`source` 是同一个模式。要动 `adaptive_learning_service.py`
与 `routers/adaptive.py`，超出 D 组边界。

**F 组（BUG-01/07/08/10 + 附注）已完成**。BUG-01 的根因是一条完整证据链，不是「store 没同步」：

1. `stoa-infra/stacks/auth_stack.py:56` Cognito `access_token_validity=Duration.hours(1)`，
   且前端全仓**没有任何 refresh token 逻辑** → access token 一小时后必然失效且无法续签。
2. `devSessions.ts` 的 `rememberSession()` 把裸 accessToken **永久**写进 localStorage，
   面板里那条 `teacher` 可能是几小时前加的，早就死了。
3. 旧 `adopt()` 先 `pinTabToSession(过期token)` 再 `queryClient.clear()`，`clear()` 让页面上所有
   observer 立刻重发请求，而 `window.location.assign()` 不中断正在跑的 JS → 这批请求带着刚 pin 的
   过期 token 打出去，401。
4. `httpClient.ts` 401 分支 `clearAuth()` + `assign('/login')`，覆盖掉前面那次去 `/tutor` 的跳转。
5. `authStore.clearAuth()` 同时删 tab token **和** localStorage 的共享 token → 原本有效的 student
   会话被一起清掉，所以后退和再访问 `/chat` 都回登录页。

修法（克制，未放宽任何鉴权）：`adopt()` 改为先用**裸 fetch** 打 `/auth/me` 探活（故意绕开 httpClient，
否则探测本身就触发 401 清场），活的才 pin，死的就删掉该条并提示；删掉 `queryClient.clear()`
（全仓无 `persistQueryClient`，缓存纯内存，整页重载本来就清空，留着只制造抢跑请求）；
401 分支在本 tab 有 pin token 时只 `releaseTab()` 不 `clearAuth()`，普通用户路径行为完全不变。
测试补了两条针对性用例：过期会话不 pin 不跳转且共享 token 不变、401 时只解绑 pin 不清共享会话。

**生产挂载结论（重要）**：`AppRouter.tsx:294` 的 `<RoleSwitcher />` **原先没有任何环境判断**，
只靠邮箱正则挡着，组件代码是打进生产包的。F 组在组件内部加了
`if (isProductionFacing || !isTestAccount(user?.email)) return null`。
遗留：组件仍会被打进生产 bundle，只是不渲染；要彻底剔除需在 AppRouter 里按环境条件 `lazy` 引入。

BUG-07 因项目未装 `@radix-ui/react-popover`，改用 `pointerdown` + `Esc` 监听，不引入新依赖。
BUG-08 悬浮徽章改成 36px 圆点、默认 `translate-x-[45%] opacity-50` 半隐在屏幕右缘，hover/聚焦才弹出，
未动 AppLayout（不为 dev-only 组件在生产布局里留死白）。
附注 2 删掉 `AppFooter` 里硬编码的 "Über STOA"，保留走 `stoaContactInfo.homepageUrl` 的 "Zur STOA Website"
（仓里没有 `/about` 路由可指）。

****B2（主会话自己做）已完成**：422 回显明文密码。按「判据 / 读数 / 结论」记：

- 判据：`POST /auth/register` 传合法 password + 非法 age，检查响应体是否含该密码明文。
- 读数：修复前 `响应体里出现明文密码: True`，响应体含
  `"password":"SuperSecret123!"`；修复后 `False`，只剩
  `{"type":"value_error","loc":["body"],"msg":"Value error, Age must be..."}`。
- 结论：已堵住。**推断部分**——前端 B 组的 `toUserFacingError` 解析只用 `loc` 与 `msg`，
  两者都保留，所以展示不受影响；这一条是读代码推断的，没有跑端到端验证。

修法：`src/stoa/security/errors.py` 新增 `redacted_validation_errors()`（该模块定位本就是
「对外最小暴露投影」），在 `main.py` 注册全局 `RequestValidationError` handler，丢掉 `input` 与 `ctx`。
影响面是**全局所有路由的 422 形状**，故跑了后端全量测试确认。

后端全量读数（含 B 组 + B2 改动）：`16 failed, 3013 passed`。不只比总数，**逐文件比了失败分布**：
`test_lambda_dist_build.py` 8、`test_route_authorization_inventory.py` 5、`test_practice_privacy.py` 1、
`test_phase473_practice_authorization.py` 1、`test_billing_recheck_apis.py` 1 —— 与上一轮会话记录的基线
逐个文件吻合，无新增失败。这 16 条的成因上一轮已查实并留证：8 条 `lambda_dist_build` 在干净基线上
也失败；其余 8 条由工作区里**第三方未提交的 `analytics.py` 注册**触发（上一轮用「只恢复 analytics 注册、
不含任何自己的改动」的对照跑出同样失败，确证与本卡无关）。

**C 组（BUG-02/03/04/06/09）已完成**。

BUG-03 的证据分两半，C 组自己把读数与推断分开写了，如实照录：

- **读数（确定）**：`**Hinweis:**` 硬编码在 `conversations.py:2247`
  `hint = ("\n\n**Hinweis:** " + hints[0]) if hints else ""`，在 Python 里拼在模型输出**之外**，
  与 `language` / `student_locale` 完全无关——所以**任何非德语回答都会被塞进这一个德语词**，
  测试报告里「英文回答 + Hinweis:」的混杂正是这里，与模型无关。全仓 grep `Hinweis` 只此一处。
  另一读数：**不存在物理专用分支**，全后端只有一份 `SYSTEM_PROMPT`、聊天只有一个入口
  `_execute_message_command`，学科只影响两个字符串，没有一处涉及语言。
- **推断（未证实）**：物理走英语的机制性解释是提示词不稳——原语言指令埋在 Rules 第 5 条且只给裸
  ISO 码 `de`，整份提示词与 JSON 样例值全是英文。C 组明说这是机制性解释，**不是能在代码里指到的
  一行 if**。修法（语言指令提为独立 `OUTPUT LANGUAGE:` 段 + 结尾复述 + 把 `de` 渲染成 `German (de)`）
  **只能靠线上实测验收**，本地无法用真实 Bedrock 验证。

BUG-04 的防护有测试背书：加粗解析只在 `parseSegments` 切出的 `text` 段上跑，LaTeX 段原封不动；
正则要求 `**` 内侧紧邻非空白，`2 ** 3 ** 4` 不会被吃；加粗内容走 React children 由 React 自动转义，
**完全没碰 `dangerouslySetInnerHTML`**。补了 4 个用例，含「公式旁散文加粗而公式不受影响」
（断言 `.math-inline` 内无 `<strong>`）与「加粗段内的 `<img src=x onerror=...>` 不生成节点」。

BUG-02 保留了三种带入上下文（practice / questionBank / upload）为空输入的合法路径，只拦「无上下文
且输入为空」，另加 60 秒去重。BUG-06 后端改用 `subject_metadata()["label"]` 并新增
`_placeholder_conversation_titles()` 同时容忍新旧两种占位形态；前端新建 `conversationTitle.ts`，
只在标题**恰好等于**占位符时才替换，学生自己的问题标题原样不动。

**BUG-06 存量数据不回填**：历史里已写死的 `math – Grade 8` 在库中保持原样，展示层渲染成
`Mathematics – Grade 8`。要改存量标题本身需另开卡做数据迁移。

待产品确认：学科徽章文案由 `Active/Foundation` 改为 `Full support / Basic support`
（`chat.json → subjectRollout`），四语已填，叫法可改。

**⚠️ 主会话的调度失误（记下来，下次别再犯）**：C 组报告 tsc 有 1 条错误
（`StudentProfilePage.tsx:327`）、vitest 有 1 条失败（`RoleSwitcher.test.tsx`），而 D 组与 F 组
各自完成时报的都是全绿。原因是我**在实现 agent 尚未全部跑完时，就派出了被明确允许「投毒改代码
再还原」的审计 agent**，C 组跑门禁的时刻正撞上审计在改 `RoleSwitcher.tsx`。
后果：各组「自己那次全绿」都不能作为合并依据。必须等全部 agent 结束、确认投毒已还原后，
由主会话在稳定工作区重跑完整门禁，以那次读数为准。

**BUG-10 只做了入口，页面仍是占位**：`/forgot-password` 路由与
`src/pages/auth/ForgotPasswordPage.tsx` 都已存在，但正文写着 "Password reset is not available from
this page yet."，**没有接后端已有的 `/auth/forgot-password` 与 `/auth/reset-password`**。
F 组只加了登录页入口指向该路由。补全页面需一个验证码表单 + 一个重置页 + 约 12 条四语文案，
估半天，**建议单独立卡**。


---

### G-F：F 组独立审计意见书（摘要与处置）

结论：**有条件放行**。审计自己做了投毒验证，并用 SHA + diff-of-diffs + `git status` 三重证明还原干净，
还原后复跑 `12 files / 85 tests` 全绿。

**A-1（必须修，已阻断合并）忘记密码入口通向硬编码英文的占位页**

- `src/pages/auth/ForgotPasswordPage.tsx` 两句话是写死的英文，**完全没走 i18n**；
  而新加的链接文案是翻译过的（de "Passwort vergessen?"、fr、it）→ 德法意用户点母语链接落到全英文页。
- `src/app/router/routeConfig.ts:326` 作者自己标了 `status: 'placeholder'`。
- **翻译其实早就备好了**：四语 `auth.json:148` 都有 `forgotPassword.{title,body}`，与页面写死的英文逐字对应，
  是既有的**无人引用死键**。
- **护栏扫不到**：`scripts/check-untranslated.mjs` 的 `ROOTS` 不含 `src/pages/auth`，
  所以 `npm run check:untranslated` 通过对这一页没有任何证明力。
- **后端早已实现**：`auth.py:892` `/forgot-password`、`auth.py:914` `/reset-password`，
  且 `httpClient.ts` 的 `PUBLIC_AUTH_PATHS` 已把两条列为免鉴权。缺的只有 `authApi.ts` 里的调用函数与页面表单。
- 处置：**做完整实现**，不取「只接 i18n」的最小方案。理由：后端与白名单都已就绪，这不是新功能而是接线；
  只改语言会留下一个诚实的死胡同，等于 BUG-10 实际没修。

**B-1（实测证据）一次网络抖动就永久删掉存档角色会话**

`devSessions.ts` 的 `sessionIsLive` 把**所有**失败都当「会话已死」（500/502/429 → `response.ok` 为 false；
断网/DNS/CORS/超时 → `catch` 返回 false），`RoleSwitcher` 拿到 false 就 `forgetSession()` 硬删 localStorage，
原始 token 不再出现。审计把 fetch mock 改成 `mockRejectedValue(new Error('offline'))` 并断言
`readSessions()` 长度为 0，**测试通过** → 断网点一次角色按钮，界面显示「会话已过期」（是谎话）且存档被删。
放行条件：只在**确知被拒**（401/403）时删，其余一律保留并提示「现在连不上服务器」。

**B-2 探活没有超时，失败时把面板锁死**

`sessionIsLive` 无 `AbortController` 无超时；`adopt()` 在 await 前 `setBusy(true)`，请求挂起则
`setBusy(false)` 永不执行，所有角色按钮 `disabled={busy}` 永久禁用且无 loading 提示，只能刷新页面。

**B-3（投毒缺口）普通用户的 401 路径零测试覆盖**

审计把 `httpClient.ts` 的分支整个换成无条件 `releaseTab()`（即任何用户 token 过期后都不再清会话），
**85 条测试全绿**。新增的两条用例只覆盖「有 pin」一侧，「无 pin」侧（`clearAuth`）无人把守。
放行条件：补一条对称用例——不设 pin、localStorage 有 token，触发 401，断言 token 被清且
`isAuthenticated === false`。

**审计确认为「站得住」的部分**（逐行比对得出，非默认接受）：普通用户 401 行为与改动前**逐字节一致**
（`clearAuth()` 原样包进 else，跳转语句在 if/else 之外未动；`stoa_tab_access_token` 全仓只有两处写入，
未用过切换器的用户该键恒为 null）；pin 过期后能自行恢复且不死循环（401 → releaseTab → /login → 整页重载
→ 回落共享 token → EntryPage 重定向回角色首页）；`isProductionFacing` 无法因环境变量漏配而误判为 false
（`getRuntimeConfig()` 未初始化时抛异常而非返回 undefined，且 `parseOrigin` 有「生产域名不得声明 staging」
的硬约束，生产下只可能为 true）。

**C 级（不拦合并，进优化清单）**：C-1 生产屏蔽的 true 分支从未被任何测试执行过（`tests/setup.ts` 把
`@/lib/env` 全局 mock 成 `isProductionFacing: false`）；C-2 裸 fetch 绕过 API 契约护栏
（`check-api-contract.mjs` 只扫 `src/services`，而 `sessionIsLive` 住在 `src/lib`）；
C-3 pin 状态下停在 `/login` 时 store 会留死 token 且被 websocket 读取；C-4 丢 pin 后可能静默回落到
另一个会话；C-5 折叠徽章丢了角色标识，且注释说的「半隐」与实际 CSS 不符（实测只裁掉约 0.2px，
遮挡下降来自去掉文字标签与 `opacity-50`，不是位移）；C-6 触摸屏能点开但靶子 36px 低于 44px 建议值；
C-7 通知面板关闭修复零测试；C-8 `navigation.about` 成孤儿键；C-9 RoleSwitcher 提示文案硬编码英文。

**审计明说没验成的**：`isProductionFacing` 在真实生产构建中为 true（只读代码，测试里被 mock）；
pin 过期后的完整恢复链路（跨整页重载，jsdom 无法走通）；C-3 窄边界未构造；
后端 `/auth/me` 对过期 token 是否确实返回 401（未打真实请求）；
BUG-08 的视觉验收（没跑起应用，只能说遮挡面积从约 100×36 降到 36×36，
**「不再遮挡任何可点击元素」这条没有验证**）；BUG-07 的交互与德语换行未跑浏览器；跨标签页隔离未验。

### G-F 处置（主会话自己做，B-1/B-2/B-3 已闭环）

**B-1 + B-2**：`devSessions.ts` 的 `sessionIsLive(): boolean` 改为 `sessionLiveness(): 'live'|'refused'|'unknown'`，
并加 `AbortController` + 8 秒超时。只有 **401/403** 才判 `refused` 并删存档；500/断网/超时一律 `unknown`，
**保留存档**并提示「连不上服务器，稍后再试」。`RoleSwitcher.adopt()` 按三态分别处置，`unknown` 分支
照样 `setBusy(false)`，面板不再被挂起的请求锁死。

**B-3**：补 `still signs out a tab that holds no pin` —— 不设 pin、localStorage 有 token，触发 401，
断言 token 被清、`isAuthenticated === false`、跳转到 `/login`。

**三条新闸都做了投毒验证（没红过的闸不算闸）**：

| 投毒 | 改坏了什么 | 读数 |
| --- | --- | --- |
| 1 | `httpClient` 401 分支换成无条件 `releaseTab()` | **红** 1 failed／14 passed，红的正是新补的对称用例 |
| 2 | `sessionLiveness` 的两个 `unknown` 全改回 `refused` | **红** 2 failed／15 passed，红的正是新补的断网与 500 两条 |

两次投毒均已还原并核验（`grep` 确认分支与 `return 'unknown'` 原样回到位），还原后 17 条全绿。
投毒 1 复现的正是审计投毒 C 的缺口，投毒 2 复现的正是审计投毒 D 的缺口 —— 两个缺口现在都有闸了。

**A-1 未处置完**：已派 agent 做完整实现（不取「只接 i18n」的最小方案）。

### ⚠️ 两个 agent 因 API 限流中断，已重派

- 「A-1 补全忘记密码」：中断在刚开始读代码，**无任何改动产出**，已从头重派。
- 「G-B 注册流程审计」：中断前最后一句是 **「Found a crash. Let me harden the probe and enumerate all
  cases.」** —— 它在构造 age 输入探测后端时发现了一个**崩溃**，但没来得及说清是什么。
  重派时已把这条线索写进 prompt，要求把找出该崩溃当作重点任务，并穷举
  `222222`/`"026"`/`0`/`-1`/`121`/`1.5`/`"abc"`/`null`/不传/`10**100`/布尔 等边界，
  判断哪个会让服务端抛未捕获异常（500）而不是干净的 422。**此事未了结前不能合并。**

### A-1 处置已完成（忘记密码流程补全）

后端真实契约（查实，非臆造）：`ForgotPasswordRequest{email}`、
`ResetPasswordRequest{email, confirmationCode, newPassword}`，两者都是 `extra: forbid`；
响应固定 `PasswordResetResponse{status}`，取值只有 `"accepted"` 与 `"confirmed"`。
**重置码走 Cognito 验证码邮件，不是带 token 的重置链接**（`auth.py:900` `cognito.forgot_password`、
`auth.py:923` `confirm_forgot_password`），所以做成「邮箱收码 + 新密码」两步页面，
与已有的邮箱验证流程同构，而不是 `?token=` 落地页。

**账号枚举防护与后端对齐**：后端 `_OPERATION_MAPPINGS[FORGOT_PASSWORD]` 把 `UserNotFoundException` /
`UserDisabledException` / `NotAuthorizedException` 全部归为 `recovery_accepted`，
路由 `if failure.publicly_accepted: return status="accepted"` —— 账号存在与否返回**完全相同的 200**。
前端据此只给一种成功文案「若该邮箱存在账号，重置码已在路上」，不区分 status，不做「该邮箱未注册」提示；
reset 侧 `UserNotFoundException` 与验证码错误同样被折叠成 `password_reset_request_invalid`，前端也只给一句。

落点：`authApi.ts` 新增两个调用函数；`ForgotPasswordPage.tsx` 占位页改真表单；
新建 `ResetPasswordPage.tsx`；`AppRouter.tsx` 只加一条 Route 与 lazy import；
`routeConfig.ts` 把 `/forgot-password` 从 `status: 'placeholder'` 改为 `'core'`、新增 `/reset-password`；
新建 `tests/component/passwordReset.test.tsx`（4 条用例）。
新密码复用 `isCompliantPassword`，邮箱复用另一组刚加的 `isValidEmail`，**没有改 validation.ts**。
i18n：`auth.json` 新增 `forgotPassword`（重写，吸收了原有两个死键）与 `resetPassword`，
四语各 171 键、顺序与路径完全一致，德语用真变音（`Prüfen`/`Passwörter`/`ungültig`）。

门禁：lint / tsc 通过，`npx vitest run` **13 文件 92 用例全绿**。

**A-1 明说没做的**：后端侧一律没碰（邮件模板、验证码有效期、Cognito 密码策略、限流阈值都在后端/Cognito 配置里），
因此页面文案没写「验证码 X 分钟内有效」这类会说谎的句子；
**真实邮件收码 → 重置这条链路没有实跑验证过**，只验证了前端契约字段与后端模型逐字对齐。

遗留待定：`src/app/router/routeGroups.ts` 的 public 列表没加 `/reset-password`。
该文件**全仓无任何引用**（grep 只命中自身），是删是补留给后续判断。

---

### G-B：注册流程独立审计（结论 **不放行**）与处置

审计全程未改产品代码，两次 `git stash push/pop` 前后 `git diff` 的 md5 恒为
`676737e37392b5b7d1f2d8f4693224d0`，工作区无留痕。

#### A1（已修）`profile.age = ""` → 未捕获 ValueError → HTTP 500，且崩在 Cognito 建号之后

上一位审计中断前说的那个**崩溃就是这个**。B 组新增的 `validate_onboarding_profile` 显式把空串排除在
校验之外（`if raw_age is not None and raw_age != ""`），于是 `""` 原样穿过 pydantic，落到
`auth.py:537` 的 `int(age)` 抛 `ValueError`，路由没捕获 → 500。

审计穷举了 `profile.age` 与 `parentProfile.childAge` 两套、模型层 + HTTP 层双通道，覆盖
`17/18/1/120/121/0/-1/222222`、`"026"/"0222222"/""/"   "/"abc"/"9"×10000/"+17"/"17\n"/" 17 "/"²"/
阿拉伯-印度数字/全角数字`、`1.5/18.0`、`true/false`、`null`、字段缺失、`10**100`、`10**5000`、
`[17]`、`{"a":1}`，外加 raw body 绕过 JSON 编码器的 `NaN/Infinity/1e400/畸形 JSON`——
**除 `""` 外全部是干净的 422**，`""` 是唯一能穿过校验层的输入，也是唯一崩溃源。

危害：`/auth/register` 是公开未鉴权端点，崩溃点在 `cognito.sign_up` **之后**、
`start_or_resume_public_registration` **之前**，每打一次就在 Cognito 留一个没有 profile 的用户，
该邮箱后续再注册会走 `UsernameExistsException` 的 resume 分支。一行 curl 可复现，可用来污染用户池。

主会话修法（两处，堵的是那一类洞不是那一个洞）：
1. `models/user.py`：空串/纯空白判为「未填」，直接把 key 从 profile 里 `pop` 掉，而不是绕过校验。
2. `routers/auth.py:537`：`profile["age"] = int(age)` 改为 `profile["age"] = age`——
   validator 已把 age 规范成 int，这次转换是冗余的，却正是崩溃点。

#### A2（部分成立，主会话的表述确实错了）「既存失败」的说法

审计用 `git stash` 验证：带全部工作区改动 `8 failed/502 passed`，全部改动入栈后 **`510 passed`**，
仅把 `analytics.py + main.py` 入栈（保留 `user.py`/`errors.py`）同样 `510 passed`。
据此指出：「与本组无关」是对的，但**「既存」是错的**——这是未提交的 `analytics.py` 引入的**新回归**，
按「既存」放行等于把新回归当背景噪音。

**主会话复核后认为审计只对了一半**，因为它用 `-k "auth or register"` 筛选，筛不到
`lambda_dist_build`。精确读数如下：

| 失败组 | 干净 HEAD | 移除 analytics、保留我全部改动（= 即将提交的状态） |
| --- | --- | --- |
| `test_lambda_dist_build.py` 8 条 | **8 failed** | 8 failed → **真·既存失败** |
| route inventory / practice_privacy / phase473 / billing_recheck 共 8 条 | 全绿 | **117 passed 全绿** → **analytics 引入的新回归** |

结论：**我要提交的内容本身不含 analytics**，所以推送后的仓库里那 8 条回归不存在；
`lambda_dist_build` 那 8 条是真既存、与本卡无关。今后表述一律按上表分两类写，不再笼统说「既存失败」。
仍需提醒 `analytics.py` 的作者补 `explicit_route_classification` 声明并重新生成 route inventory 的
checked JSON 再合并，否则那 8 条会随它一起进主干。

#### B3（已修）本组代码零测试覆盖，且 422 安全修复从未被执行过

审计 grep 出 `normalize_registration_age` / `MIN_REGISTRATION_AGE` / `redacted_validation_errors`
在 `tests/` 下**零命中**；更要命的是 `tests/test_auth_security.py` 里所有注册用例都自建
`FastAPI()` 而**不加载 `main.py`**，所以 B2 那个「422 不回显请求体」的 handler
**在整个测试套件里一次都没被执行过**——回退了也不会有测试变红，属于典型的「没红过的闸」。

主会话在 `tests/test_auth_security.py` 补了三组（写在模型层，避免依赖 Cognito 桩的完整性）：
`test_registration_refuses_every_bad_age`（10 组参数）、
`test_registration_reads_a_blank_age_as_unanswered`（2 组）、
`test_registration_requires_parent_contact_for_a_minor`，
以及**用真实 `stoa.main.app`** 的 `test_validation_failures_never_echo_the_password`。

三道闸全部投毒验证：

| 投毒 | 改坏了什么 | 读数 |
| --- | --- | --- |
| 1 | `main.py` 的 `redacted_validation_errors(...)` 换回裸 `exc.errors()` | **红** 1 failed |
| 2 | `user.py` 的空串分支改成 `if False:` | **红** 2 failed |
| 3 | 未成年家长校验条件改成 `if False:` | **红** 1 failed |

三次均已还原并 grep 核验（`redacted_validation_errors` 2 处、`raw_age.strip()` 1 处、
`age < ADULT_AGE` 1 处全部在位），还原后 `tests/test_auth_security.py` **51 passed**。

#### 待处置

- [ ] **B2** 后端 422 的原因文案是写死英文，会直接渲染进德/法/意界面
      （`"Age must be a whole number between 1 and 120."`、
      `"Referral Code: Extra inputs are not permitted"` 这类还把内部字段名翻给了终端用户）。
      前端 `errors:ageInvalid` 四语齐全但只用于客户端校验路径。**主会话接着做。**
- [ ] **B1（需产品拍板，不是技术问题）** 后端把 `age` 当可选字段，客户端不传 age / 传 null 时
      「未成年必须填家长联系方式」整条规则不执行。审计实测：`age 17 无家长 → 422`，
      而 `no age at all, no parent → 503`（503 表示已过校验打到 Cognito）。
      当前实现等于「age 缺失按成年处理」，**但没人拍过板**。
      两个选项：(a) `role=student` 时 age 改必填，缺失即 422；(b) 明确认可「缺失按成年处理」。

#### C 级（进优化清单，不拦合并）

- **C1** `auth.py:487-489` 的 `student_profile` / `parent_profile` 赋值逻辑是反的：`role=="student"` 时
  `student_profile` 恒等于 `body.profile`，`body.studentProfile` 永远读不到。新 validator 会校验
  `studentProfile`，于是老客户端发 `studentProfile:{age:17}` 现在会被 422 挡掉，而以前路由只是静默忽略。
  **既有 bug，本次没引入也没修，建议单开卡。**
- **C2** 前后端对「数字」定义不一致：后端 `text.isdigit()` 接受阿拉伯-印度数字与全角数字（`٢٦`→26、
  `１７`→17），前端 `replace(/\D/g,'')` 把它们清成空。不构成安全问题（值域仍受 1–120 约束），
  建议后端改 `re.fullmatch(r"[0-9]{1,3}", text)`。
- **C3** `normalizeAgeInput` 静默改写粘贴内容：`"1.5"`→15、`"-1"`→1 且都判为合法，用户不会察觉。
  建议非法字符出现时给一次提示。审计同时确认**「粘贴/自动填充/程序化 setState 绕过清洗」不构成洞**——
  age 是受控输入，不经 onChange 的写入下一次渲染就被回滚，且提交闸门 `isValidAge` 与清洗函数相互独立。
- **C4** 500 响应不带 CORS 头（由 `ServerErrorMiddleware` 在 CORS 之外生成），浏览器里表现为网络错误
  而非服务端错误，会掩盖 A1 那类故障。
- **C5** `RegisterPage` 的 `heading` 状态是裸 `string`，键集与类型之间没有编译期联系，
  漏一个语言只会在运行时显示原始 key。

#### 审计明说没验成的

浏览器自动填充的真实事件行为（推断，但不影响结论——提交闸门与清洗函数独立，这一点是量到的）；
A1 的线上可达性（未查移动端与 `stoa-frontend/backend/` 那个 SQLite demo 后端会不会发 `""`）；
A1 的孤儿账号后果（按代码顺序推断，**没有真打 AWS 确认残留用户**，按纪律未跑任何真实 AWS 命令）；
500 在 CloudWatch 里的日志是否会带出栈帧局部变量里的 `body.password`（本地 TestClient 复现不了
Lambda 日志格式，**建议实际发一次再看日志**）；后端全量 pytest（只跑了 `-k` 与两个指名文件）；
前端 e2e；任何浏览器内的视觉验证。

---

### G-C：聊天与渲染独立审计（结论 **有条件放行**）与处置

#### A1（已修）BUG-06 在 math 上完全没修好，3/4 学科靠巧合通过

后端 `_default_conversation_title()` 改成返回展示标签 `Mathematics – Grade 8`，前端
`conversationTitle.ts` 却仍用**原始 subject id** 拼占位符 `math – Grade 8`，两者不等 → 原样返回英文标题。
`physics/german/english` 的 label 小写后恰好等于 id（`Physics`→`physics`），**只有 `math` 的
id(`math`) 与 label(`Mathematics`) 不同**，所以只有它炸——极易在评审中漏掉。

审计用真实德语 bundle 跑 `conversationDisplayTitle` 的实测表：

```
stored="Mathematics – Grade 8"  subject=math       -> "Mathematics – Grade 8"  ← 新后端输出，没本地化
stored="math – Grade 8"         subject=math       -> "Mathematik – Grade 8"   ← 旧形态，正常
stored="Physics – Grade 8"      subject=physics    -> "Physik – Grade 8"       ← 巧合正常
```

而且**不是短暂的**：`_adopt_question_as_title()` 全仓唯一调用点在非流式 `POST /{conv_id}/messages`，
`stream_message` 没有调用它，而前端只走 `/messages/stream`（无非流式回退）——
所以前端建的会话标题**永远停在占位符**，德/法/意学生在侧栏和头部一直看到英文，
而学科选择区显示 `Mathematik`，正是 BUG-06 原文描述的症状、方向还反了。

主会话修法：采纳审计建议 1，后端 `_default_conversation_title` 改回 `f"{subject} – {grade}"`。
依据是 BUG-06 原文就要求「**统一在展示层做处理，与后端存储的原始值解耦**」，
前端对 id 形态的处理本来就是对的。`_placeholder_conversation_titles()` 按每行自己的 subject 值计算，
早期存 `Mathematics` 的行仍被正确认出。

> 遗留：`_adopt_question_as_title` 缺失于流式路径是**既存缺陷**（本次 diff 只改函数体没动调用点），
> 但它是 A1 能长期可见的前提，建议同批补上——**本卡未做**。

#### B1（已修）「不碰公式」那条断言是空的

审计投毒 3（`renderKatex(katex, seg.value.replace(/\*\*/g,''), ...)` 让加粗吃掉 LaTeX 段）
**18/18 依然全绿**——原断言只检查 `.math-inline` 内没有 `<strong>`，公式内容被破坏它照样通过。
主会话补 `hands the formula to katex with its own asterisks intact`：渲染 `$a ** b$`，
断言 `.math-inline` 的 textContent 含 `∗∗`（KaTeX 把 `*` 渲染成 U+2217 星号算子）。
复现审计投毒 3 验证：**红，1 failed / 18 passed**；还原后 19 passed。

#### B4（已修）提示词自相矛盾，正是 BUG-03 的拉力方向

`SYSTEM_PROMPT` 在「every value is written in German」之后，紧跟着示范了 5 个**英文**值
（`"Step 1: ..."`, `"Final answer"`, `"Hint..."`, `"Exercise..."`, `"Topic"`）——few-shot 与 instruction 直接冲突。
已改为语言中立占位 `"..."`；同时把撞车的 `- Use language appropriate for the student's grade level.`
（原意指语域）改成 `- Use a reading level appropriate for the student's grade.`

#### B5（已修）零新增后端测试，而现成夹具就在手边

审计指出 `tests/test_ai_service_prompt.py` 早有 `_CapturingBedrockClient` + `_system_prompt(**kwargs)`，
**加一行断言就能把语言参数钉死**。主会话补了两组共 10 个用例：
`test_answer_language_is_named_in_the_system_prompt`（de/en/fr/it/de-CH/DE 六种取值）与
`test_answer_language_does_not_vary_by_subject`（四个学科）。
投毒（把 `OUTPUT LANGUAGE:` 改成 `OUTPUT LANG:`）→ **红，10 failed / 12 passed**；还原后 22 passed。

#### G-B 的 B2（已修）后端 422 英文原因渲染进德法意界面

后端 `security/errors.py` 新增 `validation_message_code()`，`redacted_validation_errors` 给
「预期要让人读的」消息附加稳定 `code`（`age_out_of_range` / `parent_contact_required`）；
前端 `toUserFacingError` 增加可选的按码翻译器，`errors.json` 四语新增 `backend.*` 段，
`RegisterForm` 传入 `t('errors:backend.<code>', {defaultValue: ''})`。
三条路径实测：有译器+已知码 → 德语；无译器 → 英文原文；未知码 → 英文原文（都兼容）。

#### 审计的 XSS 对抗结论（25 例，跑在真实 KaTeX 上，全部通过）

`**<img src=x onerror=...>**`、`**<script>**`、`**<svg onload=>**`、`**</strong><img>**`、
`**a" onmouseover="**` 全部渲染为纯文本，`window.__pwn` 未被置位、无标签注入。
核实「没碰 `dangerouslySetInnerHTML`」属实——文件里唯一一处在 `MathRenderer.tsx:156`，
是**既存**的 KaTeX 输出路径，parse 失败分支有 `escapeHtml()` 兜底，本次未改动。
ReDoS 实测最坏 180000 字符 25.2ms、`'*'×100000` 17.8ms，无灾难性回溯。

#### 未修的已知限制（记录在案）

- **B2** 无空格乘方 `2**3**4` 会被吃掉（文本变 `234`、`3` 被加粗）；有空格的 `2 ** 3 ** 4` 安全。
- **B3** 跨公式的加粗不生效：`**text $x$ text**` 与 `**$x$**` 字面渲染，因为 `parseSegments` 先切段、
  `**` 被拆到两个 text 段里各自落单。模型输出 `**Die Einheit ist $m/s^2$**` 这类会中招。**BUG-04 只修了一半。**
- **C1** `***bold***` → `*bold*`；`*****` → `*`，与 markdown 语义不符，纯观感。
- **C2** 去重窗口 60 秒 < 报告里的「间隔几分钟」；原始重复条目是空会话，已被空输入拦截根治。
- **C3** 命中去重时静默丢弃学生这次输入（只切会话+清空输入框，无提示）。
- **C6** `get_hint_answer` 的语言修复目前是**死代码**，全仓无生产调用方。
- **C7** BUG-09 两个 rollout 状态都改成 `variant="outline"`，仅靠文字区分，视觉上不再可辨。

#### 审计明说没验成的

BUG-03 的线上实际效果（环境无 SSO，不能调真实 Bedrock；只能证明「语言名进了 prompt、prompt 结构合法、
JSON schema 完好」，**不能证明模型会照做**）；A1 的线上表现（三个读数串起来的推断，
建议合并前用本地 dev 起前后端点一次新建对话确认）。
审计**已量到**的：全仓 grep 确认 `**Hinweis:**` 等硬编码语言标签**没有残留**
（`src/` 下只剩 `_HINT_LABELS["de"]` 这个合法翻译表条目，`get_hint_answer` 的 `"Aufgabe: "` 前缀已删）。

---

### 合并前门禁读数（稳定工作区，所有 agent 已结束）

前端 7 项按 `.github/workflows/deploy-production.yml` 的 verify job 逐项跑：

| 门禁 | 读数 |
| --- | --- |
| `npm run lint` | 通过 |
| `npm run typecheck` | 通过 |
| `npm run check:api-contract` | `checked 149 service calls against 196 backend paths / every call maps to a backend route`（比基线多的 2 个正是 A-1 新增、映射到已有路由） |
| `npm run check:untranslated` | **25 条（原基线 132）**，已把 `scripts/untranslated-baseline.json` 更新为 25 守住战果 |
| `npm test` | 13 files / **93 tests** 全绿 |
| `npm run test:release` | 32 pass / 0 fail |
| `node --test tests/release/publish-web-release.test.mjs` | 3 pass / 0 fail |

翻译守卫的闸**验过会红**：`UNTRANSLATED_BASELINE=24`（低于实际 25）时退出码 **1**，等于 25 时 0。
（第一次测量误判成「不会红」，原因是在管道里取 `$?` 拿到的是 `tail` 的退出码，已纠正。）

剩余 25 条未翻译项中 **13 条集中在 `src/components/chat/TeacherHelpStatusCard.tsx`，
而该组件全仓零引用（含测试）——是死代码**，用户看不到，属守卫的假阳性。
按「不擅自扩大范围」**本卡不删**，记入优化清单。
其余各条：`ChatPage.tsx:312/339` 是发给老师的消息不是 UI 文案（误报）；
`LessonPage.tsx:112/206`、`StudentProfilePage.tsx:136`、`MistakeReviewCard.tsx:46`、
`ChatInput.tsx:78` 是既有未翻译项，不在本卡的 19 个问题内。

---

### 推送前复查（用户叫停推送后追加），又修了 5 项

**1. BUG-04 原来只修了一半 —— 跨公式的加粗完全不生效**

G-C 审计标为 B3。实测 `**Die Einheit ist $m/s^2$ hier**` → `strong=null`，整串带星号原样显示。
成因：`parseSegments` 先切段，`**` 被拆到两个 text 段里各自落单。
**而助手讲数学物理时几乎必然这么写**，所以 BUG-04 在它最常见的场景下是坏的。

修法：不再逐段配对，改为把公式替换成 NUL 占位符拼成整串、在整串上配对、再按序把公式放回去
（`MATH_PLACEHOLDER` + `expandFormulas()` + `renderSegments()`）。实测：

| 输入 | strong | strong 内公式 | 判定 |
| --- | --- | --- | --- |
| `**Die Einheit ist $m/s^2$ hier**` | 有 | 1 | 跨公式加粗成功 |
| `**$x$**` | 有 | 1 | 单独公式也能加粗 |
| `the **unit** is $a ** b$ here` | 有 | 0 | 公式里的星号未被吃 |
| `2 ** 3 ** 4` | 无 | 0 | 乘方不吃 |
| `$$**bold**$$` | 无 | 0 | 块公式内不动 |

投毒（把渲染退回旧的逐段配对）→ **红，2 failed / 19 passed**；还原后 21 passed。
**因为重构了渲染路径，XSS 面重验了 7 例**（含跨公式新路径的
`**跨公式 $x$ <img src=x onerror=...>**`）：零执行、零注入。

**2. 流式路径从不认领会话标题 —— 侧栏里每个会话都叫 `math – Grade 8`**

`_adopt_question_as_title()` 只在非流式 `POST /{conv_id}/messages` 里调用，
而**前端只走 `/messages/stream`**。两条路径用的是同一个 `_execute_message_command`，
只差这一行。后果：客户端开的每个会话标题永远停在占位符，学生无法区分——
这也是 BUG-02 佐证里「大量标题完全相同的条目」的另一半原因。
（G-C 审计把它标为「既存缺陷、是 A1 能长期可见的前提」，本卡一并修掉。）

补了 `test_every_message_route_adopts_the_question_as_title`：函数本身早有单元测试，
但**没有任何测试验证端点会调用它**——典型的「配了不等于在跑」。
投毒（摘掉流式路径那行）→ **红**；还原后通过。

**3. 附注 1 浏览器原生校验提示不跟随页面语言 —— 可控，已修**

原文档判为「非产品直接可控，除非改为完全自定义的 JS 校验并接管 HTML5 校验」。
实况是**自定义校验早就存在**（登录页 `loginSchema.safeParse` + `errors` 状态逐字段显示，
注册页 `validateCurrentStep` 的 `FieldErrors`），只是 `required` 让浏览器抢先弹出系统语言的提示。
给两个 `<form>` 加 `noValidate` 即可让自定义校验接管；`required` 保留给辅助技术。

**4. `2**3**4` 的行为：不改，钉住**

G-C 审计标为 B2「内容损坏」。核实后**不采纳**该定性：`2**3**4` → `2<strong>3</strong>4`
**符合 CommonMark**，主流渲染器行为一致；而 Python 风格乘方的典型写法 `2 ** 3 ** 4`（带空格）
已被 `(?=\S)` 前瞻保护，且助手被要求用 LaTeX 写公式。
按审计给的第二个放行条件补测试把行为钉住，并在注释里写明依据。

**5. 去重命中时静默丢弃学生的输入 —— 已给提示**

G-C 审计 C3。原实现只切会话 + 清空输入框 + return，**零提示**。
而重问的学生通常正是以为第一次没发出去的那个人，他会看到输入框被清空却毫无解释。
改为切过去的同时 `toast.info(t('duplicateReopened'))`，四语补齐。

#### 本轮读数

前端：lint 通过、tsc 通过、`npx vitest run` **13 files / 96 tests** 全绿、
翻译守卫 25 条（基线 25）退出码 0。
后端（移除第三方 analytics、即实际提交的状态）：
`-k "conversation or ai_service or auth or register or locale"` → **685 passed**。

#### 仍未修，明确列出

- **需产品拍板**：G-B 的 B1——后端把 `age` 当可选字段，客户端不传 age / 传 null 时
  「未成年必须填家长联系方式」整条规则不执行。当前等于「age 缺失按成年处理」但没人拍过板。
- **建议另开卡**：`src/components/chat/TeacherHelpStatusCard.tsx` 是**零引用死代码**
  （剩余 25 条未翻译项里 13 条在它里面，属守卫假阳性）；`contact` i18n 命名空间从未注册、
  配套三个目录全空；`src/app/router/routeGroups.ts` 全仓无引用；
  `auth.py:487-489` 的 `student_profile`/`parent_profile` 赋值逻辑是反的（既存 bug）。
- **只能线上验收**：BUG-03 的实际效果（本地无 SSO、调不到真实 Bedrock，
  只能证明语言名进了 prompt、prompt 结构合法、JSON schema 完好，**不能证明模型会照做**）；
  A-1 密码重置的真实邮件收码链路（未端到端联调）。
- **未做的观感项**：`***bold***` → `*bold*`；rollout 徽章两态都用 outline 仅靠文字区分；
  折叠徽章丢了角色标识；前后端对「数字」定义不一致（阿拉伯-印度数字/全角数字）；
  `normalizeAgeInput` 静默改写粘贴内容；500 响应不带 CORS 头。

---

### 合并记录（2026-09-18）

推送前重跑完整门禁，以这次读数为准：

| 门禁 | 读数 |
| --- | --- |
| `npm run lint` | ✅ |
| `npm run typecheck` | ✅ |
| `npm run check:api-contract` | ✅ |
| `npm run check:untranslated` | ✅ 25 条（基线 25） |
| `npm test` | ✅ 13 files / **96 tests** |
| `npm run test:release` | ✅ pass 32 |
| `node --test tests/release/publish-web-release.test.mjs` | ✅ pass 3 |
| 后端 pytest（移除第三方 analytics 的提交状态） | ✅ **685 passed** |

提交：

- 后端 `9484fd7` Validate registration input and answer in one language
- 后端 `8158a12` Adopt the question as the title on the streaming route too
- 前端 `c3702b2` Fix the defects the manual test round reported（67 文件，+2687 / −394）

三次提交都**刻意不含** `src/stoa/routers/analytics.py` 与 `main.py` 里那两行 analytics 注册——
那是第三方未提交的改动，完好保留在工作区。已验证：提交内容在不含 analytics 时
route inventory 相关测试全绿；若 analytics 作者直接提交，会带进 8 条失败，
需先补 `explicit_route_classification` 声明并重新生成 route inventory 的 checked JSON。

两仓均 push 到 main，即触发生产部署（后端 Lambda、前端 S3+CloudFront）。
后端先推，因为前端的 api-contract 门禁拉的是后端 main 分支的路由清单。

### 上线后验证（2026-09-18）

两个部署均 success：后端 run 35330573273（4m56s）、前端 run 35330853734（5m51s）。
**但部署成功不等于修复生效**，逐项量了线上：

| 验证项 | 方式 | 读数 |
| --- | --- | --- |
| 后端健康 | `GET /health` | `{"status":"ok","version":"0.1.0"}` |
| **B2 安全修复：422 不再回显密码** | 直接打生产 `POST /auth/register` | 响应体仅 `{"type","loc","msg","code"}`，**无 password** |
| B 组 age 校验 | 同上，传 `age:222222` | `422` + `Age must be a whole number between 1 and 120.` |
| G-B 的 B2 错误码 | 同上 | 响应带 `"code":"age_out_of_range"`（前端按码翻译的依据） |
| BUG-11 德语变音字符 | 抓线上 i18n chunk 比对 | `Datenschutzerklärung`/`Schülerinnen`/`können`/`gemäß` **全部在线**；旧的 `Datenschutzerklaerung`/`koennen`/`gemaess` **全部消除** |
| BUG-12 帮助中心德语 | 同上 | `Hilfe` 等在线；`Controlled pilot`/`pilot team`/`Pilot expectations`/`validates demand` **全部去除** |
| 新增文案 | 同上 | `Passwort vergessen?`、`Gib ein Alter zwischen 1 und 120 ein.`、`Das hast du schon gefragt` 均在线 |

打生产注册接口是安全的：422 发生在 `cognito.sign_up` **之前**，不会创建账号、不污染用户池。

**刻意没在线上测的**：A1 那个空字符串崩溃。修复后 `age:""` 是**合法输入**（等于未填），
会真的走到 Cognito 创建账号，所以只能靠本地的模型层穷举 + 三次投毒验证，不在生产上验。

**未能用 bundle 验证的**（打进了共享 chunk，继续挖收益递减）：BUG-13 的 mailto 收件人、
BUG-04 的跨公式加粗实现、BUG-01 的角色切换。这三项本地测试已覆盖（含投毒），
但**线上交互行为没有实际点过**。
